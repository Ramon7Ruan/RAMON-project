# 架构设计

> 项目：金融知识复习 App（代号 Recall）
> 版本：v1.0 ｜ 2026-09-21
> 状态：**P1 内容架构 + P4 系统架构** 合并交付
> 前置约束（已确认）：本地运行 · 有 App 图标 · 不经过浏览器打开 · **启动时可联网更新内容库**

---

## 0. 设计总则

四条贯穿全部架构决策的原则：

| 原则 | 含义 | 违反它会怎样 |
|---|---|---|
| **离线优先** | 任何网络行为都是"增强"，不是"前提"。断网时应用必须完整可用 | 一断网整个 app 打不开 = 自用工具不可接受 |
| **内容与进度分离** | 内容可整包替换，进度永不丢失 | 更新内容就丢复习记录，你不会再用它 |
| **单页单概念** | 一个页面只承载一个概念，渲染单位是 concept 而非列表 | 页面变成"知识流"，复习失去锚点 |
| **Block 组合即讲解方式** | 讲解方式由数据声明，不由代码硬编码 | 所有概念页长得一样，退化成电子书 |
| **视觉值 token 化** | 所有颜色/圆角/间距/字号/字体栈只定义在 `styles/tokens.css`，组件只用 `var(--*)` | 后续换视觉风格（v1.1 二次元）会变成重写而不是换皮 |

---

## 1. 系统架构

### 1.1 进程模型

Electron 双进程，严格隔离。

```
┌─────────────────────────────────────────────────────────┐
│  Main Process（Node 环境，拥有全部权限）                  │
│  ├─ 窗口管理        创建 / 恢复窗口尺寸与位置             │
│  ├─ 存储层          node:sqlite（Electron 内置）→ content.db / user.db │
│  ├─ 内容更新器      联网检查 / 下载 / 校验 / 原子替换      │
│  ├─ 导入导出        内容包导入、user.db 备份恢复           │
│  └─ 应用菜单与自动更新（应用本体，非内容）                 │
└──────────────────────┬──────────────────────────────────┘
                       │  contextBridge + ipcRenderer
                       │  （contextIsolation: true, nodeIntegration: false）
┌──────────────────────┴──────────────────────────────────┐
│  Renderer（React，无 Node 权限）                          │
│  ├─ 路由 / 页面                                           │
│  ├─ Block 渲染器库   （11 种讲解方式）                     │
│  ├─ 复习调度器       取卡片 / 评分 / 更新进度              │
│  └─ 状态层           页面状态、筛选、主题                  │
└─────────────────────────────────────────────────────────┘
```

**为什么渲染层不给 Node 权限**：内容来自网络（热点事件包），必须假设内容不可信。渲染层只通过受限 IPC 拿数据，永远不直接碰文件系统。

### 1.2 模块划分

| 层 | 模块 | 职责 | 依赖方向 |
|---|---|---|---|
| 存储 | `db/content.ts` | 内容库只读查询 | ← 无 |
| 存储 | `db/user.ts` | 进度读写、复习日志 | ← 无 |
| 领域 | `content/repository.ts` | 把两个库合并成"概念 + 我的状态"视图 | → 存储 |
| 领域 | `review/scheduler.ts` | 间隔重复算法（纯函数，无 IO） | ← 无 |
| 领域 | `update/updater.ts` | 版本比对 / 下载 / sha256 校验 / 原子替换 | → 存储 |
| 表现 | `blocks/registry.ts` | Block 类型 → React 组件 的注册表 | ← 无 |
| 表现 | `pages/*` | 页面组装 | → 领域 |

**关键约束**：`review/scheduler.ts` 必须是**纯函数**（输入上次状态 + 本次评分 + 当前时间，输出新状态），不碰数据库。这样复习逻辑可以脱离 Electron 独立做单元测试。

### 1.3 IPC 通道契约

| 通道 | 方向 | 入参 | 返回 |
|---|---|---|---|
| `content:listConcepts` | R→M | `{domain?, track?, filter?}` | `ConceptSummary[]` |
| `content:getConcept` | R→M | `{id}` | `Concept \| null` |
| `content:search` | R→M | `{q, limit}` | `SearchHit[]` |
| `content:meta` | R→M | — | `{version, updatedAt, counts}` |
| `content:checkUpdate` | R→M | `{silent}` | `UpdateInfo \| null` |
| `content:applyUpdate` | R→M | `{downloadUrl, sha256}` | `{ok, reason?}` |
| `content:importPackage` | R→M | `{filePath}` | `{ok, reason?}` |
| `progress:getAll` | R→M | — | `Record<conceptId, ConceptState>` |
| `progress:set` | R→M | `{id, state}` | `void` |
| `progress:review` | R→M | `{id, rating}` | `ConceptState` |
| `progress:reset` | R→M | `{scope}` | `void` |
| `backup:export` | R→M | `{targetPath}` | `{ok}` |
| `backup:import` | R→M | `{filePath}` | `{ok}` |

**所有通道都是异步 Promise**；主进程侧全部 try/catch，永不把异常抛回渲染层导致白屏。

---

## 2. 存储架构

### 2.1 三个数据区

```
~/Library/Application Support/Recall/
├─ content.db     内容库（可整包替换，只读使用）
├─ user.db        我的数据（进度/收藏/笔记/设置，永不覆盖）
└─ cache/
   ├─ pending/    下载中的内容包
   └─ last-check  上次检查更新的时间戳
```

**为什么必须分两个文件**：内容更新是"整个库替换"操作，如果进度和内容混在一张表里，替换内容就等于删除进度。分开之后，`content.db` 可以随意重写，`user.db` 完全不受影响。两者通过 `concept_id` 字符串关联，而不是数据库外键。

### 2.2 content.db 结构

```sql
CREATE TABLE meta (
  key   TEXT PRIMARY KEY,     -- 'version' | 'updated_at' | 'schema_version'
  value TEXT NOT NULL
);

CREATE TABLE concepts (
  id          TEXT PRIMARY KEY,   -- 'econ.macro.monetary-transmission'
  domain      TEXT NOT NULL,      -- econ | finance | hotspot
  track       TEXT NOT NULL,      -- macro | micro | ... | event | data
  title       TEXT NOT NULL,
  one_liner   TEXT NOT NULL,      -- ≤40 字，概念页首屏唯一的一句话
  key_points  TEXT NOT NULL,      -- JSON array，1–3 条关键结论，速览页展示用
  difficulty  INTEGER NOT NULL,   -- 1 | 2 | 3
  tags        TEXT NOT NULL,      -- JSON array
  recipe      TEXT NOT NULL,      -- JSON array，如 ["prose","flow","case"]
  blocks      TEXT NOT NULL,      -- JSON array，见 §3.2
  links       TEXT NOT NULL,      -- JSON array，见 §3.3
  source      TEXT,               -- 事实来源（热点与数据类必填）
  updated_at  TEXT NOT NULL,
  order_index INTEGER NOT NULL
);

CREATE TABLE tracks (
  key        TEXT PRIMARY KEY,
  domain     TEXT NOT NULL,
  label      TEXT NOT NULL,       -- '宏观经济'
  sort       INTEGER NOT NULL
);

-- 全文检索：正文抽平成一段纯文本喂给 FTS5
CREATE VIRTUAL TABLE concepts_fts USING fts5(
  id UNINDEXED, title, one_liner, tags, body_text,
  tokenize = 'unicode61'
);
```

**为什么 blocks 存 JSON 而不是拆表**：block 的字段随类型变化（`curve` 有 series，`calc` 有 inputs），拆表要么留一堆空列，要么做 EAV。渲染时永远是"整页一次读"，没有按 block 查询的需求。JSON 列是正确取舍。搜索需求由 FTS5 单独满足。

### 2.3 user.db 结构

```sql
CREATE TABLE concept_state (
  concept_id   TEXT PRIMARY KEY,   -- 逻辑关联 content.db，不建外键
  status       TEXT NOT NULL,      -- unread | reading | known | fuzzy | review
  ease         REAL NOT NULL DEFAULT 2.5,   -- SM-2 难度因子
  interval_days REAL NOT NULL DEFAULT 0,
  due_at       TEXT,               -- 下次应复习时间
  reps         INTEGER NOT NULL DEFAULT 0,
  lapses       INTEGER NOT NULL DEFAULT 0,
  last_review  TEXT
);

CREATE TABLE review_log (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  concept_id TEXT NOT NULL,
  rating     INTEGER NOT NULL,     -- 0 忘了 / 1 模糊 / 2 记得
  at         TEXT NOT NULL
);

CREATE TABLE favorites (concept_id TEXT PRIMARY KEY, at TEXT NOT NULL);
CREATE TABLE notes (concept_id TEXT PRIMARY KEY, body TEXT, updated_at TEXT);  -- 首版不启用，表预留
CREATE TABLE settings (key TEXT PRIMARY KEY, value TEXT NOT NULL);
```

**孤儿数据策略**：内容更新后某个 `concept_id` 消失时，`user.db` 里它的记录**不删除**，只在查询时被忽略。理由：内容可能被误删后又加回来，进度不该因此蒸发。`user.db` 的清理只在你显式点"重置进度"时发生。

---

## 3. 内容架构（Schema 冻结）

### 3.1 三层模型

```
Domain（3）        经济学 econ / 金融学 finance / 世界热点 hotspot
  └─ Track（9）    macro micro econometrics | equity bond fund quant | event data
      └─ Concept   一个概念 = 一条 concepts 记录 = 一个页面
```

### 3.2 Concept 源文件（YAML）

```yaml
id: econ.macro.monetary-transmission
domain: econ
track: macro
title: 货币政策传导机制
one_liner: 央行降息，为什么企业不一定马上借钱？   # ≤40 字
key_points:                                       # 速览页首屏展示，1–3 条，每条 ≤30 字
  - 三道关：银行负债端 → 信贷定价 → 企业预期
  - 漏损多发生在最后一关，取决于预期而非利率
difficulty: 2
tags: [利率, 政策, 机制]
recipe: [prose, flow, case, pitfall]              # 本页用哪几种讲解方式
source: 中国人民银行货币政策执行报告 2024Q3
updated_at: 2026-09-21
order: 12

blocks:
  - type: prose
    body: |
      央行降息，只是把资金的「批发价」压低了。要变成企业账上的「零售价」，
      中间要经过银行负债端、信贷定价、企业预期三道关，每一道都可能漏损。

  - type: flow
    steps:
      - {label: 政策利率下调, note: 7 天逆回购利率}
      - {label: 银行资金成本下降, note: 负债端传导}
      - {label: 贷款利率下行, note: LPR 报价调整}
      - {label: 企业投资意愿回升, note: 取决于预期回报率}

  - type: case
    title: 2024 年 LPR 下调之后
    numbers:
      - {label: 1 年期 LPR 累计下调, value: 20bp}
      - {label: 同期企业中长期贷款增速, value: 回落}
    body: 利率降了，但企业中长期贷款并没有同步放量，说明卡点在第三、四步。

  - type: pitfall
    wrong: 降息一定会刺激企业投资
    right: 只有当投资预期回报率高于贷款利率时，企业才会借钱
    why: 传导链条的后半段取决于预期，而不是资金价格本身。

links:
  - {to: econ.macro.liquidity-trap, reason: 对比：传到不动时的极端情形}
  - {to: finance.bond.yield-curve, reason: 延伸：利率如何影响债券定价}
```

### 3.3 Block 类型全表（讲解方式的词汇表）

**首版实现 P0 级 8 种，其余 3 种按需启用。**

| # | type | 讲解方式 | 必需字段 | 渲染技术 | 优先级 |
|---|---|---|---|---|---|
| 1 | `prose` | 定义 + 直觉 | `body` | 排版（支持粗体/高亮） | P0 |
| 2 | `formula` | 公式 + 符号人话注释 | `latex`, `symbols[]` | KaTeX | P0 |
| 3 | `curve` | 可拖参数的二维曲线 | `xLabel`,`yLabel`,`series[]`,`controls?` | SVG + 滑块 | P0 |
| 4 | `dataviz` | 真实历史数据图 | `chart`,`series[]`,`source` | 自绘 SVG（见 §4.1.1） | P0 |
| 5 | `compare` | 易混概念辨析表 | `columns[]`,`rows[][]` | 表格 | P0 |
| 6 | `flow` | 机制传导链路 | `steps[{label,note}]` | SVG 箭头链 | P0 |
| 7 | `timeline` | 事件时间轴 | `events[{date,title,note}]` | 纵向时间轴 | P0 |
| 8 | `pitfall` | 常见误区正误对照 | `wrong`,`right`,`why` | 双列对照 | P0 |
| 9 | `case` | 真实微案例 | `title`,`body`,`numbers?` | 引用卡片 | P1 |
| 10 | `scale` | 数量级锚定 | `items[{value,meaning}]` | 刻度尺 | P1 |
| 11 | `calc` | 迷你计算器 | `inputs[]`,`outputs[]` | 表单 + 实时计算 | P1 |

**`recipe` 字段是硬约束**：每个概念必须声明 3–5 种，且**全库任意两个概念的 recipe 组合不得完全相同**。这条在内容质量门禁里自动校验。

**`key_points` 是分区首屏的唯一数据来源**：点进「经济学」后看到的就是它，完整解析在「详情」后面。规范：

- 1–3 条，每条 ≤30 字
- 必须是**结论**，不是话题。写「短端由政策决定，长端由预期决定」，不写「介绍利率的决定因素」
- 不得与 `one_liner` 重复（`one_liner` 负责"勾人"，`key_points` 负责"给答案"）
- 门禁校验：条数 1–3、单条 ≤30 字、与 `one_liner` 不重复

> 为什么单独设一个字段而不是从 blocks 里自动抽取：自动抽取得不到稳定的"结论句"，而速览页的价值恰恰在于**每张卡都是一句可以直接记住的结论**。这个字段必须人工写。

### 3.4 三大区的差异化约束

不是建议，是**内容门禁里的检查项**。

| 区 | 必须命中 | 讲解重心 |
|---|---|---|
| `econ` | `curve` 或 `flow` 至少出现一次 | 机制与均衡直觉 |
| `finance` | `formula` / `calc` / `dataviz` 至少出现一次 | 可计算、可比、可查 |
| `hotspot` | `timeline` 必须出现，且 `source` 必填 | 时间、因果、分歧 |

---

## 4. 内容更新机制（联网）

### 4.1 核心原则

**离线优先，联网增强。** 启动路径里**没有任何一步会等待网络**。网络只影响"内容是不是最新"，从不影响"能不能用"。

### 4.1.1 存储引擎：改用 `node:sqlite`（实施期修正）

原定 `better-sqlite3`，实施时改为 **Electron 内置的 `node:sqlite`**。理由：

| 维度 | better-sqlite3 | node:sqlite（采用） |
|---|---|---|
| 原生模块 ABI | 需针对 Electron 重新编译（`@electron/rebuild`），升级 Electron 就要重编 | 随 Electron 提供，**零编译** |
| 测试与运行时一致性 | 测试跑 Node、运行跑 Electron，两边 ABI 不同 | 两边**同一个引擎**（Electron 44 内置 Node 24 / SQLite 3.53） |
| FTS5 | 支持 | 支持（已实测中文预分词检索可用） |
| 成熟度 | 更成熟 | Node 标记为 experimental |

代价是它带 `experimental` 标记、API 面更窄（无自定义函数等）。本项目只用「建表 / 准备语句 / 执行 / 读行」，够用；换来的是**彻底避开原生模块重编译这一类最容易在换机器、换 Electron 版本时爆发的问题**。

> 同理，图表也**没有引入 Chart.js**，而是自绘 SVG（`src/renderer/blocks/charts.tsx`）。原因是图表颜色必须走视觉 token（`var(--*)`）才能满足 A11，而 Chart.js 的颜色走 JS 配置、与 token 层脱节；自绘还顺带省掉了约 200KB 依赖。

---

### 4.2 分发格式：两个裸文件（不用压缩包）

**已定（2026-09-21）：不用 zip。** 原因很直接——GitHub Release 的附件走 `objects.githubusercontent.com`，大陆访问长期不稳定，而 jsDelivr 也代理不了 Release 附件。改成两个裸文件后，两者都能走 jsDelivr。

```
content-dist/
├─ manifest.json     ~1KB    版本头，用于"检查更新"（不下载正文）
└─ concepts.jsonl    ~200KB  首行为 meta，之后每行一个 concept
```

**manifest.json**

```json
{
  "version": "2026.09.28",
  "updatedAt": "2026-09-28T10:00:00+08:00",
  "schemaVersion": 1,
  "counts": { "econ": 24, "finance": 24, "hotspot": 20, "total": 68 },
  "file": "concepts.jsonl",
  "sha256": "<concepts.jsonl 的 sha256>",
  "size": 214733
}
```

`counts` 用于在 UI 上显示「世界热点 新增 4 条」这种具体提示。

**concepts.jsonl**

```jsonl
{"_meta":{"version":"2026.09.28","updatedAt":"2026-09-28T10:00:00+08:00","schemaVersion":1,"counts":{"econ":24,"finance":24,"hotspot":20,"total":68}}}
{"id":"econ.macro.monetary-transmission","domain":"econ","track":"macro",...}
{"id":"econ.macro.liquidity-trap",...}
```

首行为 `_meta`，作用是**让手动导入只需要选一个文件**——不必让用户同时选 manifest 和正文。两个文件里的 `version` 必须一致，由 `build-package` 保证。

### 4.3 分发地址与缓存策略

**托管：GitHub 公开仓库 + jsDelivr CDN**

| 用途 | 地址 | 缓存策略 |
|---|---|---|
| 检查更新（每天一次） | `https://cdn.jsdelivr.net/gh/{owner}/{repo}@main/content-dist/manifest.json` | 走 `main` 分支；**发布后调 jsDelivr purge API 立即失效** |
| 下载正文（仅真有更新时） | `https://cdn.jsdelivr.net/gh/{owner}/{repo}@{contentTag}/content-dist/concepts.jsonl` | 走 **tag**，内容不可变，可长期缓存 |

**地址的唯一来源：`package.json` 的 `recall.contentRepo`**（形如 `"owner/repo"`）

```json
"recall": { "contentRepo": "owner/RAMON-project", "contentBranch": "main" }
```

| 谁在读 | 怎么读 |
|---|---|
| App 运行时 | 主进程启动时读 `package.json`，拼出默认 `manifestUrl` 注入设置项（用户没填过就回落用它） |
| 发布脚本 | `tools/publish.sh` 用 `node -p` 读同一字段，决定 `origin` 与 purge 地址 |

放一处的理由很实际：**脚本推的分支/tag 与 App 拉地址必须严格对应**。两处配置最容易出现的故障是"发布成功但永远查不到更新"，而且完全没有报错。`src/shared/repo.ts` 里集中了所有 URL 构造与 tag 命名，并有测试钉死"URL 路径 == `content-dist/` 实际布局"。

为空（`""`）是**合法状态**：表示未配置联网更新，检查更新一律静默跳过，手动导入通道照常可用。

**为什么两个地址分开**：`main` 上的 manifest 是可变的需要即时可见，靠 purge 解决缓存；`tag` 上的正文不可变，靠长缓存换稳定与速度。这是 jsDelivr 的推荐用法。

**已知限制**：jsDelivr 单文件上限 50MB，我们两个文件加起来不到 1MB，无忧。jsDelivr 大陆可用性不是 100%，偶发抽风——但抽风的后果只是"这次没查到更新"，**静默跳过、无害**，符合 §4.1 的离线优先原则。

> ⚠️ **公开性提醒**：jsDelivr 只能代理**公开仓库**，内容文件会公开可访问。68 个金融概念属通用知识，不涉及隐私；但如果日后要加入个人笔记、持仓分析这类内容，必须先换回对象存储方案——架构不用动，改一个配置项即可。

### 4.4 发布流程

```
改 YAML
  → validate-content（门禁 0 error）
  → build-package（生成 content-dist/ 两个文件）
  → git add + commit + push main
  → git tag content-v2026.09.28 && git push --tags
  → 调 jsDelivr purge API，让 manifest 立即生效
```

`tools/publish.sh` 把后四步串成一条命令。

**发布是手动触发的，不做 CI**——内容必须人工复核事实与来源（PRD §5.4），自动发布会绕过这一步。

### 4.5 启动与更新时序

```
t0  应用启动
    └─ 打开 content.db（本地），渲染首页          ← 用户此刻已经能用了
t1  异步 content:checkUpdate（不阻塞 UI，不显示 loading）
    ├─ 请求 manifest.json（超时 5s）
    ├─ manifest.version <= 本地 version ? → 静默结束，什么都不做
    └─ 有更新 → 记录到状态，在顶栏显示一个不起眼的提示点
t2  用户点击提示（或进设置页点「检查更新」）
    └─ 展示更新详情：版本号、日期、各分区新增 / 修改数量
t3  用户确认更新
    ├─ 下载 concepts.jsonl 到 cache/pending/
    ├─ 校验 sha256 与 size（不匹配 → 丢弃，保留旧库）
    ├─ 逐行解析 JSONL → 写入 content.db.new
    ├─ 校验通过（schema、id 唯一性、links 无死链、recipe 组合不重复）
    └─ 原子替换：content.db.new → content.db（rename，失败可回滚）
t4  替换成功 → 通知渲染层刷新 → 顶栏版本号更新
```

### 4.6 失败与降级矩阵

| 情况 | 行为 |
|---|---|
| 无网络 | 静默跳过。不弹窗、不报错、不留红点 |
| 请求超时（>5s） | 同上，记录到日志 |
| manifest 格式非法 | 同上，记录到日志 |
| jsDelivr 返回了旧缓存 | 视为"无更新"，静默跳过（下次 purge 后自然恢复） |
| sha256 或 size 不匹配 | 丢弃下载文件，保留旧库，提示"内容校验失败" |
| JSONL 解析失败 | 保留旧库，提示具体行号与原因 |
| 新内容未通过门禁校验 | 拒绝替换，保留旧库，提示"内容未通过校验" |
| 替换过程中断（断电 / 崩溃） | 旧库仍在（rename 是原子的），下次启动照常用 |

**任何情况下，最坏结果都是"你还在用旧内容"，而不是"应用打不开"。**

### 4.7 手动导入通道（并存，不是备胎）

设置页提供「导入内容」，选本地 `concepts.jsonl` 一个文件即可（版本从首行 `_meta` 读取），走**完全相同的校验与替换流程**（跳过 sha256，因为文件本身即来源，但仍做逐行 schema 校验与门禁校验）。用途：无网环境、或你想自己写几个概念先本地试。

### 4.8 版本号规则

内容版本 `YYYY.MM.DD` 或 `YYYY.MM.DD-N`；对应的 git tag 为 `content-v{版本号}`。版本号**必须单调递增**，更新器只做字符串比较后的"是否不同 + 是否更新"判断，不做语义化版本推理。

### 4.9 与三大区的关系

- **经济学 / 金融学**：存量知识，更新频率低，主要是修正与补充
- **世界热点**：更新频率高，这是联网通道存在的主要理由。UI 上热点区每个概念都显示各自的 `updated_at`，让你一眼看出"这条是不是过期了"

---

## 5. 目录结构

```
acknowledge/
├─ app/                          Electron 主进程
│  ├─ main.ts                    窗口与生命周期
│  ├─ ipc/                       各通道 handler
│  ├─ db/{content,user}.ts       SQLite 访问
│  ├─ update/updater.ts          联网更新
│  └─ preload.ts                 contextBridge 暴露的受限 API
├─ src/                          React 渲染层
│  ├─ pages/                     Home / Track / Concept / Review / Search / Settings
│  ├─ blocks/                    ★ 11 种讲解方式的渲染器
│  │  ├─ registry.ts
│  │  └─ {Prose,Formula,Curve,Dataviz,Compare,Flow,Timeline,Pitfall,Case,Scale,Calc}.tsx
│  ├─ review/scheduler.ts        ★ 纯函数，可独立测试
│  └─ styles/
│     ├─ tokens.css              ★ 视觉值唯一来源：基础层 + 主题层（浅/深/未来的 v1.1 主题）
│     └─ global.css              全局重置与排版，只引用 token
├─ content/                      ★ 内容源文件（YAML），与代码完全分离
│  ├─ econ/{macro,micro,econometrics}/
│  ├─ finance/{equity,bond,fund,quant}/
│  └─ hotspot/
├─ content-dist/                 ★ 发布产物（提交进仓库，供 jsDelivr 分发）
│  ├─ manifest.json              版本头 ~1KB
│  └─ concepts.jsonl             正文 ~200KB
├─ tools/
│  ├─ validate-content.ts        内容质量门禁（本地与发布前共用）
│  ├─ build-package.ts           YAML → content-dist/ 两个裸文件
│  ├─ publish.sh                 commit + push + 打 tag + purge jsDelivr 缓存
│  └─ make-icon.sh
├─ build/icon.icns
├─ 原型/index.html               交互原型（本阶段交付）
├─ 架构设计.md / 页面交互设计.md / 开发计划.md
└─ .workbuddy/memory/
```

---

## 6. 测试策略（按架构层映射）

| 层 | 测试类型 | 具体断言 |
|---|---|---|
| `db/content` | 集成 | 建库 → 导入样例 → 查询往返一致；FTS5 能搜到中文子串 |
| `db/user` | 集成 | 进度写入 → 重启进程 → 读回一致；孤儿 concept_id 不报错 |
| `review/scheduler` | 单元（无 IO） | SM-2 状态迁移全路径；边界：首次评分 / 连续"忘了" / 跨天翻转 / 时间回拨 |
| `update/updater` | 单元 + 集成 | sha256 / size 不匹配必须拒绝替换；JSONL 解析失败必须保留旧库；断网必须静默通过；拿到旧缓存的 manifest 必须视为无更新；替换后旧进度仍正确关联 |
| `blocks/*` | 快照 + 边界 | 每种 block 一个快照；超长文本、空 series、公式语法错误、缺 source 字段 |
| `tools/validate-content` | 单元 | recipe 组合重复必须报错；links 死链必须报错；热点缺 timeline 必须报错；`key_points` 条数越界或与 `one_liner` 重复必须报错 |
| 端到端 | 手动 + 脚本 | 全概念遍历无崩溃；断网全流程可用；更新内容后进度不丢 |

**质量门禁脚本 `validate-content.ts` 是内容阶段的唯一守门人**，它必须在 P7 之前完成并纳入每次内容提交。

---

## 7. 本阶段遗留的决策点

| 项 | 状态 | 说明 |
|---|---|---|
| 首版内容规模 | ✅ 已定（见 `PRD.md` §5.1） | 每区 20–30，合计 68 个概念，9 个专题每专题 ≥5 |
| 复习机制深度 | ✅ 已定（见 `PRD.md` §4.3） | SM-2 简化版，三档评分，算法为纯函数 |
| 视觉主题 | ⏳ 待评审 | 原型中已实现「卡片流 + 浅色/深色双主题」，见《页面交互设计》§5 |
| 内容分发地址 | ⏳ 待定 | 联网更新需要知道从哪拉 manifest；P5 内容引擎开工前必须确定 |
| 速览页是否保留状态/难度筛选 | ⏳ 待评审 | 见 `PRD.md` §12 P4 |
