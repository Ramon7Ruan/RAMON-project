# 金融知识复习 App — 开发计划

> 工作代号：**Recall**
> 版本：**v2.0** ｜ 2026-09-21 ｜ 状态：**待评审**
>
> **需求源**：`PRD.md` ｜ **技术源**：`架构设计.md` ｜ **交互源**：`页面交互设计.md` ｜ **原型**：`原型/index.html`
>
> 本文只做一件事：**把 PRD 的需求拆成可执行的工作包与阶段门禁**。
> 不重复定义需求、Schema、接口、页面规格——那些各有单一出处，避免出现两套说法。

---

## 0. 文档分工（单一定义点）

| 文档 | 它**只**负责 | 不负责 |
|---|---|---|
| `PRD.md` | **要求什么**：目标、场景、模块的数据与操作、验收标准 | 怎么实现、分几步做 |
| `架构设计.md` | **怎么搭**：进程模型、双库 Schema、IPC 契约、内容更新机制 | 需求来源、排期 |
| `页面交互设计.md` | **长什么样、怎么交互**：逐页线框、视觉规范、状态清单 | 数据结构、实施顺序 |
| `原型/index.html` | **可点击的验证物** | 真实后端行为 |
| **`开发计划.md`（本文）** | **谁在什么阶段做哪个工作包、门禁是什么、怎么测** | 重复定义上面四项中的任何内容 |

**维护规则**：需求变更改 PRD，实现方案变更改架构文档，视觉变更改交互文档，只有排期与拆分变化才改本文。本文中出现的需求一律以「引用 + 章节号」形式存在。

---

## 1. 范围与基线

### 1.1 首版范围

严格按 `PRD.md` §10 的 In / Out 执行。**Out 列表里的东西一律不在本计划的工作包内**，包括：账号与云同步、移动端、应用本体自动更新、内容在线编辑器、个人笔记、学习统计、自定义复习范围、自测题、多语言、分享。

### 1.2 首版规模

| 项 | 数量 |
|---|---|
| 概念总数 | **68**（经济学 24 / 金融学 24 / 世界热点 20） |
| 专题 | 9 个，每个 ≥5 个概念 |
| 讲解方式 | 11 种（首版全部实现） |
| 页面 | 7 个 + 1 个共用概念详情页 |

### 1.3 已冻结的决策清单

| 决策 | 结论 | 出处 |
|---|---|---|
| 技术栈 | Electron + Vite + React + TypeScript + `node:sqlite` + 自绘 SVG + KaTeX | §1.4（选型修正在架构 §4.1.1） |
| 应用外壳 | 打包为 macOS `.app`，双击即开，不调用系统浏览器 | PRD R1 |
| 存储 | 双库分离：`content.db`（可替换）/ `user.db`（永不覆盖） | 架构 §2 |
| 内容形态 | YAML 源 → `content-dist/` 两个裸文件（**不用 zip**）→ 内容库 | 架构 §4.2 |
| 内容分发 | **GitHub 公开仓库 + jsDelivr**：manifest 走 `@main` + 发布后 purge，正文走 `@{tag}` 长缓存 | 架构 §4.2–4.4 |
| 手动导入 | 选本地单个 `concepts.jsonl`（版本从首行 `_meta` 读） | 架构 §4.7 |
| 更新机制 | 离线优先、联网增强；失败一律保留旧库 | PRD §4.5、§6.6 |
| 页面纵深 | 两级：要点速览页（给结论）→ 概念页（给原理） | PRD §3.4 |
| 速览页筛选 | **不做筛选** | PRD §4.2.1 |
| 复习机制 | SM-2 简化版，三档评分，算法为纯函数 | PRD §4.3 |
| 搜索 | 首版要做（FTS5） | PRD §4.4 |
| 视觉策略 | **v1.0 中性视觉，二次元风格列为 v1.1**；首版必须做到视觉 token 化 | PRD NF11 / NF12 |
| 首版规模 | 68 个概念 | PRD §5.1 |

### 1.4 技术基线

**选型：Electron + Vite + React + TypeScript + `node:sqlite`（Electron 内置）+ 自绘 SVG + KaTeX**

> 实施期修正：原定 `better-sqlite3` 与 `Chart.js` 均已替换，原因见 `架构设计.md` §4.1.1——前者为了彻底避开原生模块 ABI 重编译，后者为了让图表颜色走视觉 token（A11 要求）。

选它的理由直接对着 R5（解析方法多样化）来：

- 图表、公式、富文本的生态最成熟，`curve` / `dataviz` / `calc` 这类交互 block 用 Web 技术实现成本最低
- electron-builder 一条命令产出 `.app` + `.icns`，双击即开
- 内置 Chromium 随包发布，**不调用也不需要系统里的任何浏览器**，天然离线
- 代价：包体约 150–250MB（自用工具，可接受）

已归档的备选（不再评估）：Tauri（体积小但需 Rust 工具链，WebView 版本差异带来渲染不确定性）、SwiftUI 原生（图表与公式全要自建，与"大量内容"目标冲突）、PyWebView（运行时打包繁琐）。若日后在意体积，React 代码可平滑迁到 Tauri，只换外壳。

### 1.5 版本路线

| 版本 | 内容 | 前置条件 |
|---|---|---|
| **v1.0** | 本计划全部范围：功能完整 + **中性视觉** | — |
| **v1.1** | **二次元视觉升级**（G 组工作包）：替换主题 token、启用装饰位、字体与配色定稿 | **v1.0 的 NF11（视觉 token 化）验收通过**。这是 v1.1 能否"只换皮不返工"的唯一前提 |
| v1.2+ | 待定。候选：个人笔记、学习统计、自定义复习范围、热点内容自动抓取 | 视 v1.0 实际使用反馈 |

> **关于 v1.1 的关键判断**：视觉升级的成本几乎完全由 v1.0 的工程纪律决定。如果首版把颜色、圆角、字号散落在组件里，v1.1 就是重写；如果收敛在 `tokens.css` 一层，v1.1 就是换一个文件。所以 **A4 工作包虽然小，却是 v1.1 的成败点**。

---

## 2. 工作包分解（WBS）

量级说明：**S** 小 / **M** 中 / **L** 大 / **XL** 很大（内容生产是唯一的 XL）。

### A 工程底座

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| A1 | Electron 工程骨架 | 主进程、窗口生命周期、恢复上次尺寸与位置、开发热重载 | PRD §3.1 | L |
| A2 | 打包与图标 | electron-builder 配置、`.icns` 生成（含 Retina 各尺寸）、产出 `.app` | PRD R1、NF4、NF5 | M |
| A3 | 双库初始化与 Schema | 建 `content.db` / `user.db`、WAL 模式、目录初始化 | 架构 §2.1–2.3 | M |
| **A4** | **视觉 token 层** | `tokens.css`：基础层 + 主题层（浅 / 深）；`global.css`；规定组件只用 `var(--*)` | **PRD NF11**、交互 §5.5 | S |
| A5 | IPC 契约与 preload | `contextBridge` 暴露受限 API、13 条通道 handler 骨架、handler 永不把异常抛回渲染层 | 架构 §1.3 | M |

### B 内容引擎

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| B1 | YAML 解析 + Schema 校验 | 读 `content/**/*.yaml`，逐字段类型校验 | 架构 §3.2 | M |
| B2 | **内容质量门禁 `validate-content`** | PRD §5.3 的 11 条检查全部实现 | **PRD §5.3** | L |
| B3 | 内容导入管道 | YAML → `content.db`（含 JSON 列、FTS5 索引构建） | 架构 §2.2 | M |
| B4 | 全文检索 | FTS5 虚拟表、中文分词、搜索查询接口 | PRD §4.4 | S |
| B5 | 打包与发布工具 | `build-package`：YAML → `content-dist/` 两个裸文件（manifest.json + concepts.jsonl，含 sha256/size/counts）；`publish.sh`：commit + push + 打 tag + purge jsDelivr 缓存 | 架构 §4.2–4.4 | M |

### C 页面与渲染

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| C1 | 应用外壳 | Sidebar 208px、标题栏、面包屑、版本徽标三态 | PRD §3.1 | M |
| C2 | 要点速览页 | 专题手风琴、要点卡、分区空状态、**无筛选** | PRD §4.2.1 | M |
| C3 | 概念详情页 | recipe 徽标、返回入口、相关概念（带原因）、底部三按钮、收藏 | PRD §4.2.1 | M |
| **C4** | **11 种 Block 渲染器** | prose / formula / curve / dataviz / compare / flow / timeline / pitfall / case / scale / calc | 架构 §3.3 | L |
| C5 | 首页 | 今日复习卡、三区进度、最近读过 | PRD §4.1 | S |
| C6 | 搜索页 | 即时搜索、分组、命中高亮、最近搜索、无结果建议 | PRD §4.4 | S |
| C7 | 全站四态 | 每页的空 / 加载 / 错误 / 部分失败（单块降级） | PRD §8 | M |

### D 复习机制

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| D1 | **SM-2 纯函数** | 状态迁移、ease 边界（1.3–3.0）、跨天翻转、时间回拨 | PRD §4.3 | M |
| D2 | 复习队列与复习页 | 两段式（回忆 → 展开 → 评分）、键盘 空格 / 1 / 2 / 3、中途退出 | PRD §4.3 | M |
| D3 | 评分流水 | `review_log` 只追加写入 | PRD §4.3 | S |

### E 数据与备份

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| E1 | **联网内容更新** | 检查 / 下载 / sha256 校验 / 原子替换 / **完整失败矩阵** | PRD §4.5、§6.6 | L |
| E2 | 设置页 | 内容 / 数据 / 外观三段全部条目 | PRD §4.5 | M |
| E3 | 自动备份 | 替换内容库前备份 `user.db`，保留最近 5 份，**失败则中止更新** | PRD §6.4 | S |
| E4 | 手动导出 JSON | 含 schemaVersion / appVersion / contentVersion / 概念 title | PRD §6.4 | S |
| E5 | **差异预览 + 合并式导入** | 校验 → 预览 → 确认 → 单事务合并（按 `last_review` 取较新） | PRD §6.5 | M |
| E6 | 危险操作防护 | 重置进度二次确认、导入事务回滚、孤儿记录不报错 | PRD §6.7 | S |

### F 内容生产

| 编号 | 工作包 | 内容 | 追溯 | 量级 |
|---|---|---|---|---|
| F1 | 内容模板与写作规范 | 11 种 block 的 YAML 模板；`one_liner` / `key_points` / `links.reason` 的写法示例 | PRD §5.2 | S |
| F2 | **首批 24 个概念** | 9 个专题各 2–3 个，用于验证渲染与复习闭环 | PRD §5.4 | L |
| F3 | 补齐至 68 个 | 按专题配额生产 | PRD §5.1 | **XL** |
| F4 | 内容复核与门禁通过 | 事实核验 + `validate-content` 0 error | PRD §5.3 | L |

### G v1.1 视觉升级（首版不做，此处仅登记）

| 编号 | 工作包 | 前置 |
|---|---|---|
| G1 | 二次元主题 token（替换 `tokens.css` 主题层） | v1.0 NF11 通过 |
| G2 | 启用装饰位与插画（要点卡 / 概念页顶部的预留槽位） | G1 |
| G3 | 字体与配色定稿、动效补充 | G1 |

---

## 3. 阶段计划与门禁

**每阶段必须有交付物 + 测试，门禁不过不进入下一阶段。**

### P0 需求冻结 ✅ 已完成

| 项 | 内容 |
|---|---|
| 交付物 | `PRD.md` §2 的 R1–R8 全部确认 |
| 门禁 | 所有待确认项已拍板，无遗留 TODO |

### P1 内容架构 ✅ 已完成

| 项 | 内容 |
|---|---|
| 交付物 | `架构设计.md` §3：Concept Schema 冻结、11 种 Block 类型全表、三大区差异化约束 |
| 测试 | Schema 校验脚本跑通，0 error |
| 门禁 | Schema 冻结；后续变更需走版本号 + 迁移脚本 |

### P2 页面 / UX 设计 ✅ 已完成（v2 改版）

| 项 | 内容 |
|---|---|
| 交付物 | `页面交互设计.md` + `原型/index.html` |
| 测试 | 无头 DOM 遍历全部页面与交互：**12 项断言全过、0 运行时错误**；8 个图表 SVG 通过严格 XML 校验 |
| 门禁 | 逐页走查通过；两级纵深成立（速览页 0 个解析 block）；筛选已彻底移除 |

### P3 PRD ✅ 已完成

| 项 | 内容 |
|---|---|
| 交付物 | `PRD.md`（12 章） |
| 测试 | 需求可追溯性检查：R1–R8 全部映射到 PRD 章节 |
| 门禁 | 追溯矩阵无遗漏；§10 In / Out 无歧义 |

### P4 工程骨架 ← **下一步**

| 项 | 内容 |
|---|---|
| 目标 | 跑通「双击图标 → 打开 App → 看到空白但完整的外壳」 |
| 工作包 | **A1、A2、A3、A4、A5** |
| 交付物 | `.app`（带自定义图标）+ 双库已初始化 + IPC 通道骨架 + `tokens.css` |
| 必跑测试 | ① 冷启动 ≤1.5s ② Dock / Finder 图标正确 ③ 断网可启动 ④ 打包产物在干净目录能运行 ⑤ 关闭再打开，窗口尺寸位置被恢复 ⑥ **扫全部组件文件，硬编码颜色 / 圆角 / 字号 = 0 处** |
| 门禁 | 冒烟测试全过；**A4 的 token 层验收通过（v1.1 的前置）** |
| 对应 PRD 验收项 | A1、A11（前半） |

### P5 内容引擎 + 渲染器

| 项 | 内容 |
|---|---|
| 目标 | 内容能从 YAML 走到页面，11 种讲解方式全部渲染正确 |
| 工作包 | **B1–B5、C1–C7** |
| 交付物 | 内容管道 + 门禁脚本 + 7 个页面 + 11 种渲染器 |
| 必跑测试 | ① 每种 Block 一个快照测试 ② 边界：超长文本溢出、公式语法错、图表数据缺失、`blocks` 为空 ③ YAML → SQLite 往返一致性 ④ 门禁脚本对**故意构造的违规内容**必须报错（recipe 重复 / 死链 / 热点缺 timeline / `key_points` 超长）⑤ FTS5 能搜到中文子串 ⑥ 每页四种状态均可触达 |
| 门禁 | 全部渲染器测试通过；门禁脚本对违规样本 100% 拦截 |
| 对应 PRD 验收项 | A3、A9（前半） |

### P6 复习机制 + 数据与备份

| 项 | 内容 |
|---|---|
| 目标 | 复习闭环成立；内容可更新且进度不丢；备份恢复可走通 |
| 工作包 | **D1–D3、E1–E6** |
| 交付物 | 复习页 + 设置页 + 更新器 + 备份恢复 |
| 必跑测试 | **全项目测试最重的阶段**：<br>① SM-2 单元测试：全路径状态迁移 + 边界（首次评分 / 连续"忘了" / 跨天翻转 / 时间回拨）<br>② 持久化：杀进程重启后进度一致<br>③ **失败矩阵逐条验证**：断网 / 超时 / manifest 非法 / sha256 不匹配 / 解析失败 / 替换中断——六种情况**都必须保留旧库且应用仍可用**<br>④ 备份：更新前确实生成备份；备份失败时更新被中止<br>⑤ 导入：差异预览内容正确；事务失败时整体回滚；概念不存在时写成孤儿记录不报错<br>⑥ 重置进度：二次确认生效，收藏默认不清空 |
| 门禁 | 上述 6 组测试全过。**③ 是硬门禁：任何一条失败都不允许进入 P7** |
| 对应 PRD 验收项 | A5、A6、A7、A9（后半） |

### P7 内容填充

| 项 | 内容 |
|---|---|
| 目标 | 68 个概念全部可读、可复习 |
| 工作包 | **F1–F4**（分两批：F2 首批 24 → 验证 → F3 补齐） |
| 交付物 | `content/**/*.yaml` 共 68 个概念 + 内容包 |
| 必跑测试 | ① `validate-content` **0 error** ② 事实性数据均有来源 ③ 9 个专题各 ≥5 个概念、覆盖完整 ④ 难度三档均有分布 ⑤ 无孤立概念 ⑥ 逐概念人工通读一遍 |
| 门禁 | 门禁脚本 0 error；**三大区差异化命中检查通过**（econ 全含 curve / flow、finance 全含 formula / calc / dataviz、hotspot 全含 timeline） |
| 对应 PRD 验收项 | A2、A4 |

### P8 验收与交付

| 项 | 内容 |
|---|---|
| 目标 | 完整可用版本 |
| 工作包 | 全量回归 + 修复 |
| 交付物 | `.app` + 内容包 + 使用说明 |
| 必跑测试 | **PRD §11 的 A1–A11 逐条核对**，重点是：<br>① 端到端遍历 68 个概念无崩溃<br>② **断网状态下走通全部 7 个页面与复习流程，无任何错误提示**<br>③ 执行一次完整内容更新（含新增 / 修改 / 删除三种情况），**逐条核对进度记录，丢失数为 0**<br>④ 抽查 20 个组件文件，硬编码视觉值 = 0 |
| 门禁 | A1–A11 全部通过，无豁免 |
| 对应 PRD 验收项 | 全部 |

---

## 4. 测试计划

### 4.1 测试类型与归属

| 类型 | 覆盖对象 | 手段 | 可自动化 |
|---|---|---|---|
| 单元 | `review/scheduler`、`validate-content` | 直接调用纯函数 | ✅ |
| 集成 | 双库读写、内容导入管道、备份恢复 | 临时目录建库跑真实 SQLite | ✅ |
| 快照 | 11 种 Block 渲染器 | 固定输入比对输出 | ✅ |
| 边界 | 溢出 / 空数据 / 语法错误 / 缺字段 | 构造恶意输入 | ✅ |
| 端到端 | 全页面遍历、全概念渲染 | 无头 DOM 遍历（原型阶段已验证该手段可行） | ✅ |
| 手动 | 图标、窗口恢复、真实联网更新、断网体验 | 人工走查 | ❌ |
| 内容 | 事实准确性、表述质量 | 人工通读 | ❌ |

### 4.2 硬门禁测试（失败则不允许进入下一阶段）

| 测试 | 阶段 | 为什么是硬门禁 |
|---|---|---|
| 内容更新失败矩阵（6 种情况） | P6 | 最坏结果必须是"还在用旧内容"，绝不能是"应用打不开" |
| 内容更新后进度丢失数 = 0 | P7 / P8 | 丢进度 = 这个 App 失去存在意义 |
| 断网全流程可用 | P8 | "离线优先"是核心承诺，不能只是口号 |
| 组件硬编码视觉值 = 0 | P4 / P8 | v1.1 换皮不返工的唯一保障 |

### 4.3 PRD 验收项 → 阶段映射

| PRD 验收项 | 首次验证阶段 | 最终确认阶段 |
|---|---|---|
| A1 启动与图标 | P4 | P8 |
| A2 三大分区内容完整 | P7 | P8 |
| A3 两级结构成立 | P5 | P8 |
| A4 讲解方式多样化 | P7 | P8 |
| A5 状态与复习 | P6 | P8 |
| A6 内容更新 | P6 | P8 |
| A7 备份与恢复 | P6 | P8 |
| A8 离线完整可用 | P8 | P8 |
| A9 状态完整性（四态） | P5 / P6 | P8 |
| A10 持久化 | P8 | P8 |
| A11 视觉可替换性 | P4（前半） | P8 |

---

## 5. 里程碑

| 里程碑 | 覆盖阶段 | 验收表现 |
|---|---|---|
| **M1 骨架可跑通** | P4 | 双击图标 → 打开 App → 看到外壳；token 层已就位 |
| **M2 引擎可渲染** | P5 | 内容从 YAML 进库，11 种讲解方式全部正确渲染，门禁脚本能拦住违规内容 |
| **M3 闭环可用** | P6 | 复习可走通；断网可用；更新内容后进度不丢 |
| **M4 内容齐备** | P7 | 68 个概念全部可读，门禁 0 error |
| **M5 完整交付** | P8 | A1–A11 全通过 |
| **M6 视觉升级** | v1.1 | 替换主题 token 后界面整体变为二次元风格，功能零回归 |

---

## 6. 风险与对策

| 风险 | 影响 | 对策 | 状态 |
|---|---|---|---|
| **v1.1 视觉升级变成重写** | 首版工作白做一半 | **A4 工作包强制 token 化**；P4 门禁检查硬编码视觉值 = 0；P8 复检 | ✅ 已通过机制化解 |
| **内容生产是最大工作量** | 68 个概念写不完 | F 组拆成"首批 24 验证 → 再补齐"；B2 门禁脚本先于内容生产完成；F1 先出模板与写法示例 | ⏳ 待执行 |
| **Schema 反复变动导致返工** | 已写内容全部重做 | Schema 在 P1 已冻结；变更走版本号 + 迁移脚本；F2 首批特意做小就是为了这个 | ✅ 已冻结 |
| **Block 类型膨胀** | 维护成本失控 | 首版锁死 11 种；新类型必须证明"现有类型无法表达"才加 | ⏳ 持续 |
| **热点内容时效性** | 第三部分几周后就过时 | 内容带 `updated_at`，UI 显示"内容截至 X 日"，超 30 天转琥珀色提示；「相关基础概念」把事件沉淀为长期知识 | ✅ 已设计 |
| **内容更新引入不可信数据** | 渲染层被污染 | 渲染层无 Node 权限；内容包 sha256 校验；门禁校验通过后才允许替换 | ✅ 已设计 |
| **macOS 未签名应用提示** | 首次打开被 Gatekeeper 拦 | 本地自用：ad-hoc 签名；必要时 `xattr -d com.apple.quarantine` | ⏳ P4 处理 |
| **"复习"退化成"浏览"** | 产品失去价值 | 复习机制独立成 P6 阶段，不允许并进 P5 顺带做；SM-2 算法必须有单元测试 | ✅ 已排期 |
| **jsDelivr 偶发不可用** | 当次检查更新失败 | 已计入设计：失败即静默跳过，下次重试；不做重试风暴、不做错误弹窗。后果仅为"晚一点拿到更新" | ✅ 已接受 |
| **内容仓库公开** | 内容文件可被公开访问 | 现有 68 个概念属通用知识，可接受。若日后加入个人笔记 / 持仓类内容，需换回对象存储——只改配置项，架构不动 | ⚠️ 需留意 |

---

## 7. 进度

### 已完成

| 阶段 | 状态 | 产出 |
|---|---|---|
| P0 需求冻结 | ✅ | R1–R8 全部确认；技术栈 = Electron；更新通道 = 联网 + 手动导入 |
| P1 内容架构 | ✅ | Schema 冻结、11 种 Block、三大区差异化约束 |
| P2 页面 / UX | ✅ 已评审 | `页面交互设计.md` + `原型/index.html`（12 项断言全过，0 运行时错误） |
| P3 PRD | ✅ 已评审 | `PRD.md` 12 章：目标与场景、页面框架、7 模块数据与操作、备份恢复、验收标准 |
| P4 工程骨架 | ✅ 完成 | Electron 外壳 + 打包 + 图标 + 双库初始化 + 视觉 token 层。冷启动 331ms；包体约 250MB（踩线） |
| P5 内容引擎 + 渲染器 | ✅ 完成 | YAML 管道 + 11 条门禁 + FTS5 中文检索 + 7 个页面 + 11 种渲染器 |
| P6 复习机制 + 数据与备份 | ✅ 完成 | SM-2 纯函数 + 失败矩阵 13 项 + 两层备份 + 合并式导入（事务回滚已验证） |
| P7 内容生产 | ✅ 完成 | 68 个概念（24/24/20），9 专题全覆盖，门禁 0 error 0 warn，recipe 组合 68/68 唯一 |
| P8 验收 | ✅ 完成 | `验收报告.md`：A1–A11 逐条核对；213 个用例全过 |

### 测试现状

**11 个测试文件 / 213 个用例，全部通过**（`npm test`，约 2.7s）。其中 4 条硬门禁均已落实为自动化测试：

| 硬门禁 | 落在 |
|---|---|
| 内容更新失败矩阵 | `tests/p6-review.test.ts`（13 项，逐条验证失败后保留旧库） |
| 更新后进度丢失 = 0 | `tests/p6-review.test.ts` + `tests/p8-persistence.test.ts`（跨重开逐条核对） |
| 断网全流程可用 | `tests/p8-acceptance.test.tsx`（fetch 全程抛错，走通全部页面与复习） |
| 组件硬编码视觉值 = 0 | `tests/a11-tokens.test.ts` |

### 下一步

1. ~~建公开 GitHub 仓库、联调联网更新~~ ✅ **已完成**（`Ramon7Ruan/RAMON-project`，tag `content-v2026.09.21`，真实 jsDelivr 上逐段验证通过）
2. **人工走一遍** `release/mac-arm64/Recall.app`，重点看复习闭环（尤其隔几天后的**二次复习**是否真的出现）与设置页导出/导入
3. **改内容后跑一次 `npm run publish:content -- <新版本号>`**：这会走完发布全链路。注意版本号必须大于 `2026.09.21`，否则会被守卫拒绝（脚本会明确提示）
4. **v1.1 视觉升级**：启动前单独开一轮设计（配色 / 字体 / 角色插画），token 层已就绪

### 阻塞项

**无。** 所有阶段完成，A1–A11 全部通过（详见 `验收报告.md` §4 的未达标项与已知限制）。

唯一需要你动手的：一个**公开的 GitHub 仓库**，用来放 `content-dist/` 两个文件。仓库名定了之后告诉我，我把它写进配置。
