# Recall 验收报告（P8）

> 验收对象：PRD §11 的 A1–A11
> 验收日期：2026-09-21
> 验收方式：自动化测试为主 + 打包产物实测。**未通过项如实列出，不做粉饰。**

---

## 一、交付概览

| 项 | 结果 |
|---|---|
| 应用形态 | macOS `.app`，双击启动，不经系统浏览器 |
| 技术栈 | Electron 44 + React 19 + TypeScript + Vite + `node:sqlite`（FTS5）+ KaTeX + 自绘 SVG 图表 |
| 应用包体 | **约 250 MB**（`du -sk` 实测 251.18 MB）—— 目标 ≤250MB，**踩线且略超**，构成见 §4 |
| 冷启动 | 打包产物：首次 331ms / 再次 246ms（目标 ≤1.5s，达标） |
| 内容规模 | 68 个概念：经济学 24（宏观 10 / 微观 8 / 计量 6）、金融学 24（股票 7 / 债券 6 / 基金 5 / 量化 6）、世界热点 20（事件 12 / 数据 8） |
| 源码规模 | 4,658 行 TS/TSX（app + src + tools） |
| 内容规模 | 2,924 行 YAML |
| 自动化测试 | **11 个文件 / 206 个用例，全部通过**（约 2.3s） |
| 讲解方式 | 11 种 block 全部实现；68 个概念的 recipe 组合**全库唯一** |
| 数据目录 | `~/Library/Application Support/Recall/`（content.db / user.db / backups / cache / runtime） |

### 交付物清单

| 类别 | 文件 |
|---|---|
| 应用 | `release/mac-arm64/Recall.app`（含 `Contents/Resources/content-dist/concepts.jsonl` 内置内容） |
| 需求 | `PRD.md` |
| 设计 | `架构设计.md`、`页面交互设计.md`、`原型/index.html` |
| 计划 | `开发计划.md`（v2.0，工作包 WBS + 门禁） |
| 内容源 | `content/econ/econ.yaml`、`content/finance/finance.yaml`、`content/hotspot/hotspot.yaml` |
| 发布产物 | `content-dist/manifest.json`、`content-dist/concepts.jsonl` |
| 工具 | `tools/validate-content.ts`（门禁）、`tools/build-package.ts`、`tools/publish.sh`、`tools/make-icon.sh`、`tools/after-pack.js` |
| 测试 | `tests/*.test.ts(x)`（10 个文件） |

---

## 二、如何运行

```bash
# 开发模式（渲染层热更新）
npm run dev

# 构建 + 打包出 .app（一条命令）
npm run package
open release/mac-arm64/Recall.app

# 内容门禁（改动内容后必须跑）
npm run gate

# 内容发布（门禁 → 打包 → push → 打 tag → 刷新 jsDelivr 缓存）
npm run publish:content -- 2026.09.28

# 全部测试
npm test
```

首次打开若被 macOS 拦住，右键 →「打开」即可（本地自用，未做代码签名）。

---

## 三、验收矩阵

| 项 | 判据 | 验证方式 | 结果 |
|---|---|---|---|
| **A1** 启动与图标 | 看到首页；图标正确；冷启动 ≤1.5s | 打包产物实测 + `p8-acceptance`（外壳/导航/版本号/图标文件） | ✅ 345ms / 231ms；`icon.icns` 与 PNG 各尺寸齐备 |
| **A2** 内容完整 | 各 ≥20 个概念，9 个专题全覆盖，门禁 0 error | `p7-content`（总量/分布/id 唯一/门禁）+ `p8-acceptance`（页面完整列出） | ✅ 24/24/20，9 专题，门禁 0 error 0 warn |
| **A3** 两级结构 | 速览页无任何解析 block；概念页只出现当前概念；≤2 次点击 | `p8-acceptance`（速览页 block 数 = 0、详情跳转、返回入口） | ✅ 从启动到完整解析 = 2 次点击 |
| **A4** 讲解方式多样 | recipe 组合全库唯一；三大区各命中本区必含方式 | `p7-content` + `p5-gate`（门禁逐条拦截验证） | ✅ 68/68 唯一；econ 命中 curve/flow、finance 命中 formula/dataviz、hotspot 命中 timeline |
| **A5** 状态与复习 | 状态机全路径；队列顺序；评分符合算法表 | `p6-review`（40 项：算法全路径 + 队列语义） | ✅ |
| **A6** 内容更新 | 断网静默跳过；有更新时顶栏提示；**更新后进度丢失 = 0** | `p6-review` 失败矩阵 13 项 + `p8-acceptance` 顶栏提示 | ✅ 进度逐条核对一致；提示不弹窗、不夺焦 |
| **A7** 备份与恢复 | 更新前自动备份；导出 JSON 可读；导入有差异预览；合并式 + 事务回滚 | `p6-review` + `p4-db` + **`p8-persistence`（故障注入验证回滚）** | ✅ 保留最近 5 份；回滚后库内无任何部分写入 |
| **A8** 离线可用 | 断网走通全部 7 个页面与复习流程 | `p8-acceptance`（fetch 全程抛错）+ 单测全覆盖 | ✅ 无任何错误提示 |
| **A9** 四态完整 | 空 / 加载 / 错误 / 部分失败 均已实现 | `p8-acceptance`（四态逐一渲染） | ✅ 部分失败由 `BlockErrorBoundary` 兜底，单块降级不影响整页 |
| **A10** 持久化 | 重启 / 更新内容后逐条核对，丢失数 = 0 | **`p8-persistence`（真的关库再重开）** | ✅ 三次重启 + 一次内容更新后进度一致 |
| **A11** 视觉可替换 | 抽查 20 个组件文件硬编码视觉值 = 0 | `a11-tokens`（全量扫描渲染层） | ✅ 0 处硬编码；`tokens.css` 分基础层 + 主题层，装饰槽位已预留 |

### 自动化测试清单

| 文件 | 用例 | 覆盖 |
|---|---|---|
| `p4-db.test.ts` | 22 | 双库 Schema、WAL、FTS5 中文检索、备份恢复语义 |
| `p4-ipc.test.ts` | 13 | IPC 通道契约、参数健壮性、**文件路径校验（越界写入回归）** |
| `p5-blocks.test.tsx` | 31 | 11 种讲解方式渲染 + 边界（字段残缺降级） |
| `p5-gate.test.ts` | 25 | 11 条门禁规则逐条拦截 + 管道往返一致性 |
| `p6-review.test.ts` | 40 | SM-2 全路径 + **内容更新失败矩阵 13 项** + 备份恢复 + 队列 |
| `p7-content.test.ts` | 17 | 内容规模分布 + 单概念质量 + 整库门禁与入库 |
| `p8-seed.test.ts` | 7 | 首启内置内容（不覆盖已有内容 / 坏内容不拦启动） |
| `p8-persistence.test.ts` | 8 | 跨重开持久化 + **导入事务回滚（故障注入）** |
| `p8-acceptance.test.tsx` | 25 | 真实 App 挂 DOM + 真实 IPC + 真实 68 概念，端到端验收 |
| `p8-repo-config.test.ts` | 13 | 内容仓库地址构造、tag 命名、**URL 路径 == `content-dist/` 实际布局**、内置地址回落 |
| `a11-tokens.test.ts` | 5 | 硬编码视觉值扫描 + token 分层 + 涨红跌绿 |

---

## 四、未达标项与已知限制

| 项 | 状态 | 说明 |
|---|---|---|
| 应用包体 | ⚠️ **约 250MB，踩线且略超目标** | 过程中一度为 302MB。原因是 Electron 自带 200+ 语言包（49MB），而 electron-builder 的 `electronLanguages` 在 Electron 44 上**静默失效**（它只清 `Contents/Resources`，语言包实际在 framework 内部）。已用 `tools/after-pack.js` 钩子自行裁剪掉 218 个语言包。**剩下的 250MB 里，201MB 是 Electron Framework 二进制本身、49MB 是其余框架——与本项目代码无关（自己的代码只有 1.8MB）。** 要显著减小只能换应用外壳（如 Tauri），不在首版范围。另注：`du` 的计数口径会让同一产物在 249.6–251.2MB 之间浮动。 |
| 代码签名 / 公证 | ❌ 未做 | 本地自用，`identity: null`。首次打开需右键「打开」绕过 Gatekeeper。若要分发给他人必须补签名。 |
| 联网更新通道 | ⚠️ 未联调 | 代码与失败矩阵已完成，地址机制也就位了（`package.json` 的 `recall.contentRepo` 为唯一来源，含 13 项测试）。**但仓库 owner 还没填进去，真实联网路径一次都没跑过。** 手动导入通道不受影响。 |
| `notes`（个人笔记） | ⏸ 首版不做 | PRD §6.3 明确划到 Out，表未建。 |
| v1.1 二次元视觉 | ⏸ 后续版本 | 首版为中性视觉；token 层已就绪（A11 已验证），换皮不需要改组件。 |
| 复习「今日小结」 | ⏸ 未做 | PRD §12 P3 遗留项，可在后续版本决定。 |

---

## 五、本阶段发现并修复的真实缺陷

测试确实抓到了问题——这些都不是"写测试走过场"，其中若干是会导致**数据损失或应用无法启动**的：

| # | 缺陷 | 严重度 | 发现方式 | 修复 |
|---|---|---|---|---|
| 1 | **全新数据目录首次启动必失败**（`disk I/O error`）。Chromium 的缓存文件与 SQLite 挤在同一目录，SQLite 切 WAL 时失败 | 阻断（装完打不开） | 打包产物冒烟自检 | Electron `userData` 指向 `runtime/` 子目录；WAL 改为**尽力而为 + 可降级**，失败退回默认日志模式，应用照常可用 |
| 2 | **间隔重复不会二次触发**。`isDue` 依赖 `status==='review'`，而评分「记得」会写成 `known` → 到日子也不会再出现，间隔重复退化成"一次过" | 高（产品核心失效） | 队列语义测试 | `isDue` 改为只由 `due_at` 决定；`setStatus` 明确各状态对 `due_at` 的影响（「我掌握了」才清空） |
| 3 | `dueQueue` 里"不懂优先"是**死代码** | 中 | 队列测试 | 重写筛选与排序逻辑 |
| 4 | `parseManifest` 遇到 `null` / 数组 / 字符串直接取属性 → 崩溃 | 中 | 失败矩阵测试 | 加类型守卫，一律当作非法清单 |
| 5 | **打包产物不带内容**：全新安装是空库，离线打开等于空壳 | 阻断（A2/A8 不成立） | 交付前检查 | 新增首启内置内容导入（走与手动导入**完全相同**的校验管道），并写入 `extraResources` |
| 6 | YAML 里写字面 `\n` 会渲染成两个字符，而门禁**不拦** | 中（内容全库风险） | 人工发现 + 补规则 | 新增门禁规则（跳过 LaTeX / 符号字段避免误伤）+ 对应测试 |
| 7 | `previewImport` / `applyImport` 遇空载荷崩溃；孤儿计数语义错误 | 中 | 数据层测试 | null 安全化 + 修正计数口径 |
| 8 | `app:version` 通道漏注册；`app:pickPath` 未纳入 handlers | 低 | IPC 契约测试 | 补齐通道并让文件选择可注入（可测） |
| 9 | recipe 与 blocks 脱节（批量替换误伤） | 中 | 门禁 | 改为**从 blocks 直接重算 recipe**，消除这一类错误 |
| 10 | 16 个概念是孤立节点（无人指向） | 低 | 门禁提醒 | 逐条补入边并写明关联理由 |
| 11 | 门禁漏掉 `\n` 规则；A11 命名检查把注释里的示例也算违规 | 低 | 测试自检 | 规则细化（剥注释、按语义判断） |
| 12 | 打包语言包裁剪配置静默失效，包体超标 52MB | 中 | 交付前实测 | 自写 `afterPack` 钩子 |
| 13 | **IPC 把渲染层传来的路径 `String()` 强转 → 越界写入**。`{ path: 42 }` 变成相对路径 `"42"`，文件被写进**进程当前目录**；且违反 PRD §6.1「只写数据目录」 | 中高（越界写 + 路径来自不可信输入） | 发现项目根目录莫名多出一个 `42` 文件，反查来源 | 路径必须为**非空绝对路径**（真实流程里一律来自原生文件对话框，不会误伤）；非法输入返回明确原因而非抛异常。新增回归测试断言"非法路径一律拒绝且**当前目录不新增任何文件**" |

### 一个值得记下的教训

`electronLanguages` 这类**"配置看起来生效、实际没生效"**的问题最难发现——顶层 `Contents/Resources` 里确实只剩两种语言，看起来完全正常，只有真的去量 framework 内部才发现 49MB 还在。**打包产物必须实测关键指标，不能只看构建是否成功。**

第二条教训来自缺陷 #13：**项目根目录在多轮测试后多出一个 `42` 文件。**
它在测试里被"垃圾载荷不抛异常"这一条断言合法地制造出来——测试通过了，但副作用是写了一个文件。
这类问题只有靠**观察工作目录的实际变化**才会浮出来。
所以：测试不只断言返回值，也要在涉及文件系统时断言**"不该产生的东西没有产生"**。

---

## 六、下一步建议

1. **建一个公开 GitHub 仓库**放 `content-dist/` 两个文件，把 `manifestUrl` 填进设置项 → 联网更新就能真实联调（目前唯一没跑过真人验证的链路）。
2. **人工走一遍**：打开 `release/mac-arm64/Recall.app`，重点试复习闭环（尤其是隔几天后的二次复习是否真的出现）与设置页的导出/导入。
3. v1.1 视觉升级启动前，单独开一轮设计（配色 / 字体 / 是否加角色插画）——首版的 token 层已经为此准备好。
