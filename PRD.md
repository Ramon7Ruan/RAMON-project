# 金融知识复习 App — 产品需求文档（PRD）

> 项目代号：**Recall**
> 版本：**v1.0** ｜ 2026-09-21 ｜ 状态：**P3 阶段交付，待评审**
> 适用范围：**首版（v1.0）**
>
> 配套文档：
> - `架构设计.md` — Schema、进程模型、联网更新机制（本文引用其定义，不重复展开）
> - `页面交互设计.md` — 逐页布局规格与视觉规范
> - `原型/index.html` — 可点击验证的交互原型
> - `开发计划.md` — 阶段门禁与里程碑

---

## 0. 文档说明

### 0.1 本文写什么

本文把过去几轮对齐中的**全部决策固化为可验收的需求条目**。写作原则三条：

1. **每条需求可追溯到来源**（用户原话 / 后续确认），见 §9 追溯矩阵
2. **每个模块必须给出"首版必要的数据和操作"** 与"首版明确不做的"，不留模糊地带，见 §4
3. **不重复其他文档已有的内容**，直接引用。本文只做"要求什么"，不做"怎么实现"

### 0.2 决策记录（本文的输入）

| 轮次 | 用户提出 | 结论 |
|---|---|---|
| 1 | 5 条硬约束（本地运行 / 大量知识 / 分三部分 / 单页单概念 / 解析多样化） | 全部作为硬约束，见 §2.1 |
| 2 | 「无需依赖浏览器」= 不需要在浏览器中打开；打开时可联网更新数据库 | 技术栈定 Electron；更新机制定"离线优先、联网增强" |
| 3 | 点进分区应首先出现一整页学习内容，现有内容收进「详情」 | 分区首屏改为**要点速览页**，完整解析由「详情」进入；新增 `key_points` 字段 |
| 4 | 首版内容规模 20–30 | 按每区 20–30 计，三区合计 60–90，见 §5.1 |
| 5 | 去掉速览页筛选；视觉希望更二次元，但首版不投入 | **速览页不做筛选**；首版走**中性视觉**，二次元风格列为 **v1.1**，首版只需保证**视觉 token 化**（见 `页面交互设计.md` §5.5） |
| 6 | 内容分发地址走哪个方案 | **GitHub 公开仓库 + jsDelivr**；顺带把分发格式从 zip 改成两个裸文件 |

### 0.3 术语表

| 术语 | 含义 |
|---|---|
| **分区 Domain** | 三大知识区之一：经济学 `econ` / 金融学 `finance` / 世界热点 `hotspot` |
| **专题 Track** | 分区下的分类，共 9 个（宏观、微观、计量、股票、债券、基金、量化、事件、数据） |
| **概念 Concept** | 最小内容单元 = 数据库一条记录 = 一个详情页 |
| **要点卡** | 分区首屏的一张卡，展示 标题 + 一句话 + 关键结论 |
| **讲解方式 Block** | 概念的组成单元类型，共 11 种（定义 / 公式 / 曲线 / 数据图 / 对比表 / 传导图 / 时间线 / 误区 / 微案例 / 数量级 / 计算器） |
| **解释配方 recipe** | 一个概念用了哪几种讲解方式的组合，3–5 种 |
| **内容库 content.db** | 可整包替换的知识库；**用户数据 user.db**：进度、收藏，永不覆盖 |

---

## 1. 产品目标与使用场景

### 1.1 一句话定位

一个**纯本地的金融知识复习工具**：点开就有内容，读完能记住，进度不丢。

### 1.2 产品目标

| # | 目标 | 可验证的判据 |
|---|---|---|
| G1 | **降低"进入内容"的成本** | 从启动到看见第一个概念的完整解析，**不超过 2 次点击** |
| G2 | **让知识能以多种方式被理解** | 全库任意两个概念的讲解方式组合不重复；三大区各命中本区专属讲解方式 |
| G3 | **让"复习"区别于"阅读"** | 每个概念有明确掌握状态；有到期复习队列；有间隔重复调度 |
| G4 | **内容可更新，进度不丢失** | 内容库替换前后，用户进度 100% 保持关联；断网时全部功能可用 |
| G5 | **完全自足** | 双击 App 图标即用，只依赖本机文件；除内容更新外不产生任何网络请求 |

### 1.3 非目标（首版明确不做）

- 不做账号、登录、云同步、多设备
- 不做移动端、Web 版
- 不做内容在线编辑（内容生产走 YAML → 内容包）
- 不做社交、排行榜、分享
- 不做 AI 答疑、自动出题

### 1.4 使用场景

**场景 A — 通勤前 5 分钟的快速复习**
打开 App → 首页「今天要复习 8 个概念」→ 点进去 → 卡片式回忆，只看标题和一句话，想不起来按空格展开 → 三档评分 → 5 分钟做完 8 张 → 首页显示"明天还有 3 个到期"。

**场景 B — 想搞懂一个概念**
侧栏点「金融学」→ 一屏要点卡，扫一眼发现「久期」的结论没看懂 → 点「详情」→ 看到公式、可拖动的迷你计算器、数量级锚定 → 拖动滑块自己算一遍 → 点「我掌握了」→ 返回速览页继续。

**场景 C — 追一个当下热点**
点「世界热点」→ 看到「美联储 9 月降息与曲线重定价」，卡上有「内容截至 09-20」→ 点详情 → 时间线 + 会议前后曲线对比 + 两种市场解读 → 页面底部「相关基础概念」跳到「收益率曲线」把机制补上。

**场景 D — 找一条记不清的结论**
搜索框输入「久期」→ 即时高亮结果 → 点进去 → 返回继续。

**场景 E — 内容过期了**
打开 App，顶栏出现「内容有更新 ●」→ 点开看到「世界热点 新增 4 条」→ 点「立即更新」→ 细进度条走完 → 内容刷新，**复习进度一条没变**。

### 1.5 成功判据（首版验收时看）

- 冷启动到首页可交互 ≤ 1.5 秒
- 三个分区各 ≥ 20 个概念，合计 ≥ 60 个，全部通过内容质量门禁
- 断网状态下全部页面与复习流程可完整走通
- 执行一次内容更新后，逐条核对进度记录，丢失数为 0

---

## 2. 需求来源与硬约束

### 2.1 用户原始硬约束（不可放宽）

| # | 原话 | 落地要求 |
|---|---|---|
| R1 | 「它在本地运行，拥有 app 图标，打开后在本地出现交互页面，无需依赖浏览器」 | 打包为 macOS `.app`；双击图标即开；Dock / Finder 显示自定义图标；不调用系统浏览器 |
| R2 | 「它应包含大量经济、金融知识」 | 首版每区 20–30 个概念；内容与代码解耦，可独立扩充 |
| R3 | 「应该分成多个部分：经济学 / 金融学 / 世界热点」 | 三个分区 + 9 个专题，见 §3.2 |
| R4 | 「每个页面只出现一个概念及其解析，且解析无需太复杂」 | 完整解析只在概念页出现，且只出现当前一个；要点速览页不渲染任何解析块 |
| R5 | 「解析的方法应该多样化，文字、图表等等」 | 11 种讲解方式；`recipe` 组合全库唯一，由门禁强制 |

### 2.2 后续确认的约束

| # | 补充要求 | 落地要求 |
|---|---|---|
| R6 | 「不需要在浏览器中打开」 | 技术栈 = Electron（内置 Chromium 随包发布） |
| R7 | 「打开的时候可联网进行数据库的更新」 | 离线优先、联网增强；启动路径不含任何等待网络的步骤；见 §4.7 |
| R8 | 「一点击到比如经济学的部分，首先出现的是一整页的概念相关的学习内容，现有的这些可以放到一个详情按键后点开查看」 | 分区首屏 = 要点速览页；完整解析收进「详情」；新增 `key_points` 字段；见 §3.4 |

---

## 3. 整体页面框架与导航结构

### 3.1 应用外壳

| 元素 | 规格 |
|---|---|
| 窗口默认尺寸 | 1180 × 780；最小 960 × 640；**恢复上次关闭时的尺寸与位置** |
| 左侧 Sidebar | 208px 固定，不折叠。导航项 + 底部进度环 |
| 顶部标题栏 | 52px。左：面包屑；右：内容版本徽标 |
| 内容区 | 阅读类页面最大宽度 760px 居中 |
| 主题 | 浅色 / 深色 / 跟随系统；作用于整个窗口 |

**版本徽标三态**：`内容 v2026.09.21`（常态，可点进设置）/ `内容有更新 ●`（可点展开详情）/ `更新失败 ⚠`（可点看原因，**不做红色警告**，因为不影响使用）。

### 3.2 页面清单与路由

| 模块 | 路由 | 页面 |
|---|---|---|
| 应用外壳 | — | Sidebar + 标题栏 + 版本徽标 |
| M1 首页 | `/` | 今日复习 + 三区总览 + 最近读过 |
| M2 经济学 | `/d/econ` `/d/econ/:track` | 要点速览页（3 个专题） |
| M3 金融学 | `/d/finance` `/d/finance/:track` | 要点速览页（4 个专题） |
| M4 世界热点 | `/d/hotspot` `/d/hotspot/:track` | 要点速览页（2 个专题） |
| （共用）概念详情页 | `/c/:id` | 完整解析页，由 M2/M3/M4 的「详情」进入 |
| M5 复习 | `/review` | 卡片式抽取 |
| M6 搜索 | `/search` | 全库检索 |
| M7 设置 | `/settings` | 内容更新 / 数据 / 外观 |

**三大分区的专题构成**

| 分区 | 专题 |
|---|---|
| 经济学 `econ` | 宏观经济 `macro`、微观经济 `micro`、计量经济 `econometrics` |
| 金融学 `finance` | 股票 `equity`、债券 `bond`、基金 `fund`、量化 `quant` |
| 世界热点 `hotspot` | 事件解剖 `event`、数据观察 `data` |

### 3.3 导航结构

```
Sidebar
├─ 首页
├─ ── 分隔 ──
├─ 经济学      3          ← 数字为概念总数
├─ 金融学      4
├─ 世界热点    2
├─ ── 分隔 ──
├─ 复习        4          ← 数字为今日到期数（无到期则不显示）
├─ 搜索
├─ 设置
└─ ── 底部 ──
   进度环 + 「已掌握 1 / 13 · 8%」
```

**导航硬性要求**

| 要求 | 说明 |
|---|---|
| N1 | 任意页面深度不超过 2 层（`分区 → 概念`），无三级页面 |
| N2 | 概念页顶部必须有**固定的**「← 返回×要点速览」入口，不能只靠面包屑里一个彩色词 |
| N3 | 返回速览页时**保留**原筛选条件与折叠状态（不重置） |
| N4 | 侧栏导航项高亮当前分区；概念页属于哪个分区就高亮哪个 |
| N5 | 复习 / 搜索为**模态式页面**，底部返回回到进入前的页面 |

### 3.4 两级页面纵深（核心结构，R4 + R8 的落地）

| 层 | 页面 | 数据来源 | 回答的问题 | 硬约束 |
|---|---|---|---|---|
| **第一层** | 要点速览页 `/d/:domain` | `title` + `one_liner` + `key_points` + `recipe` + 难度 + 我的状态 | 「这块我要记的几个结论是什么」 | **不渲染任何解析 block**（无图表、公式、计算器） |
| **第二层** | 概念页 `/c/:id` | 完整 `blocks` | 「为什么是这样，怎么算」 | **只出现当前一个概念**的内容；其他概念只能以「相关概念」链接形式出现，且必须显式点击 |

**从启动到看见完整解析 = 2 次点击**（侧栏分区 → 要点卡详情），满足 G1。

### 3.5 全站状态图例与概念状态机

**状态点图例**（速览页要点卡、搜索结果、首页列表通用）

| 符号 | 状态 `status` | 颜色 |
|---|---|---|
| ○ | `unread` 未读 | 次要文字色 |
| ◐ | `reading` 在读 | 品牌蓝 |
| ◉ | `review` 待复习（今天到期） | 琥珀色 |
| ● | `known` 已掌握 | 绿色 |
| ◆ | `fuzzy` 不懂 | 红色 |

**状态机**

```
            ┌──────────── 打开概念页 ────────────┐
            ▼                                    │
unread ──────────► reading ──────────► known ────┤
                      │  ▲               │       │
        「标记不懂」   │  │ 「我掌握了」   │       │
                      ▼  │               │       │
                    fuzzy┘               │       │
                      │                  │       │
        「加入待复习」  ▼                  │       │
                   review ◄──────────────┘       │
                      │                          │
        复习评分「记得」└──────► known ───────────┘
        复习评分「忘了」└──────► review（间隔重置为 1 天）
```

**约束**：状态转移只在用户显式操作时发生，系统不自动改状态（唯一例外：首次打开概念页时 `unread → reading`）。

---

## 4. 模块设计

每个模块统一按 **用途 / 首版必要数据 / 首版必要操作 / 首版明确不做 / 状态处理** 五段描述。

---

### 4.1 M1 首页

#### 用途
回答一个问题：**「我现在该干什么？」** 同时提供一个不需要记忆路径的入口。

#### 首版必要数据

| 数据 | 来源 | 必填 | 说明 |
|---|---|---|---|
| 今日到期概念数 | `user.concept_state` 中 `due_at <= 今天` 且 `status = review` 的条数 | 是 | 驱动主行动卡 |
| 三分区概念总数 | `content.concepts` 按 domain 聚合 | 是 | — |
| 三分区已掌握数 | 与 `user.concept_state` 联合 | 是 | 算进度百分比 |
| 最近读过的概念（≤5 条） | `user.concept_state` 按 `last_review` 或首次阅读时间倒序 | 否 | 空则不显示该区块 |
| 今天日期 | 系统时间 | 是 | 显示「9 月 21 日 周一」 |

#### 首版必要操作

| 编号 | 操作 | 触发 | 结果 | 边界 |
|---|---|---|---|---|
| M1-O1 | 开始复习 | 点主行动卡 | 构建队列（今日到期）→ 进入 `/review` | 无到期时按钮文案变为「随便看一个 →」，随机抽一个 `unread` 概念进详情页 |
| M1-O2 | 进入分区 | 点三区卡片 | 进入 `/d/:domain` | — |
| M1-O3 | 进入概念 | 点最近读过任意行 | 进入 `/c/:id` | — |

#### 首版明确不做
- 自定义首页布局、问候语、天气、日历
- 学习统计图表（周报 / 连续天数 / 热力图）
- 推荐算法（"你可能想看"）

#### 状态处理

| 状态 | 表现 |
|---|---|
| 内容库为空（首次启动且未联网） | 主行动卡位置显示「内容库是空的，去设置页检查更新」+ 直达按钮；三区卡片置灰 |
| 全部已掌握 | 主行动卡显示「今天没有到期的复习」+ 「随便看一个 →」 |
| 无最近读过 | 该区块整体不显示（不显示空占位） |

---

### 4.2 M2 经济学 / M3 金融学 / M4 世界热点

三个分区**共用同一套页面结构**（要点速览页 + 概念详情页），但**数据要求与操作要求按分区差异化**。这是"反同质化"的落地方式。

#### 4.2.1 共用结构

**用途**：分区的第一部分职责是让用户**一眼看到本区所有概念的结论**；第二部分是用最合适的讲解方式把每个概念讲透。

**共用数据（速览页）**

| 数据 | 来源字段 | 必填 |
|---|---|---|
| 概念标题 | `concepts.title` | 是 |
| 一句话 | `concepts.one_liner` | 是 |
| 关键结论 | `concepts.key_points` | 是（1–3 条） |
| 难度 | `concepts.difficulty` | 是（1/2/3） |
| 讲解方式徽标 | `concepts.recipe` | 是 |
| 我的状态 | `user.concept_state.status` | 否（默认 `unread`） |
| 专题名 / 专题下概念数 / 专题下待复习数 | `tracks` + 聚合 | 是 |

**共用数据（概念详情页）**

| 数据 | 来源字段 | 必填 |
|---|---|---|
| 完整解析块（3–5 个） | `concepts.blocks` | 是 |
| 事实来源 | `concepts.source` | 数据类 / 公式类 / 热点类**必填** |
| 相关概念 + 关联原因 | `concepts.links` | 是（≥1 条，且必须带 reason 文案） |
| 我的状态 / 是否收藏 | `user.concept_state` / `user.favorites` | 否 |

**共用操作**

| 编号 | 操作 | 触发 | 结果 | 边界 |
|---|---|---|---|---|
| M2-O1 | 展开 / 折叠专题 | 点专题头 | 切换展开态，状态在本次会话内保留 | 默认展开**有待复习**的专题；都没有则展开第一个 |
| M2-O2 | 进入详情 | 点要点卡任意位置 / 点「详情 →」 | 路由到 `/c/:id`；若原状态为 `unread` 则写入 `reading` | — |
| M2-O3 | 返回速览 | 点「← 返回×要点速览」 | 回 `/d/:domain`，**保留折叠状态** | — |
| M2-O4 | 标记状态 | 概念页底部三按钮 | 写入 `concept_state`，图标实时变化 | 「我掌握了」不弹确认 |
| M2-O5 | 收藏 | 概念页星标 | 写 / 删 `user.favorites` | — |
| M2-O6 | 跳转相关概念 | 点相关概念项 | 路由到目标概念页 | 目标不存在时提示"该概念已不在内容库中"并提供返回 |

**共用明确不做**
- **速览页的筛选条（状态 / 难度）——已确认移除**。要点卡本身就是"一屏扫完"的形态，再加筛选是给用户增加操作而不是减少负担；要缩小范围用搜索页
- 分区内多概念同屏渲染完整解析（违反 R4）
- 自动下一篇 / 无限滚动推荐流
- 学习时长统计、打卡

#### 4.2.2 M2 经济学 — 差异化要求

| 项 | 要求 |
|---|---|
| 讲解重心 | **机制与均衡直觉**——"为什么会这样" |
| 必含讲解方式 | 每个概念必须包含 `curve`（曲线）或 `flow`（传导图）**至少一种** |
| 数据要求 | `curve` 类概念需给出坐标轴含义与曲线移动方向说明；理论类概念须标注适用前提 |
| 特有操作 | 无（首版不额外增操作） |
| 专题划分 | 宏观经济 / 微观经济 / 计量经济 |

#### 4.2.3 M3 金融学 — 差异化要求

| 项 | 要求 |
|---|---|
| 讲解重心 | **可计算、可比、可查**——"怎么算、怎么比、怎么用" |
| 必含讲解方式 | 每个概念必须包含 `formula`（公式）/ `calc`（计算器）/ `dataviz`（数据图）**至少一种** |
| 数据要求 | `dataviz` 必须带 `source`；`formula` 必须带符号注释表（每个符号一句人话） |
| 特有操作 | `calc` 类概念的输入项**必须可交互**（拖动或输入即算，不设"计算"按钮） |
| 专题划分 | 股票 / 债券 / 基金 / 量化 |

#### 4.2.4 M4 世界热点 — 差异化要求

| 项 | 要求 |
|---|---|
| 讲解重心 | **时间、因果、分歧**——"发生了什么、各方怎么看" |
| 必含讲解方式 | 每个概念**必须包含 `timeline`**（时间线），且按时间线优先排序 |
| 数据要求 | `source` **必填**（含"截至 X 日"）；每个概念显示 `updated_at` |
| 时效机制 | 标题下方强制显示「内容截至 YYYY-MM-DD」；**超过 30 天转为琥珀色过时提示** |
| 特有操作 | 底部必须有**「相关基础概念」**区块，把事件链接到经济学/金融学的机制概念（`links` 至少要有一条指向 `econ` 或 `finance`） |
| 专题划分 | 事件解剖 / 数据观察 |

**为什么热点区要有「相关基础概念」**：事件会过时，但事件背后的机制不会。这一步是把**时效性内容沉淀为长期知识**的机制，否则热点区每两个月就变成需要清理的垃圾。这是本模块存在的正当性。

---

### 4.3 M5 复习

#### 用途
把"读过"变成"记住"。核心是**先回忆、再看答案**，以及按遗忘曲线安排下次出现的时间。

#### 首版必要数据

| 数据 | 来源字段 | 必填 | 说明 |
|---|---|---|---|
| 到期队列 | `user.concept_state` 中 `due_at <= now` 的 `concept_id` 列表 | 是 | 按 `due_at` 升序；`fuzzy` 状态排最前 |
| 难度因子 | `ease` | 是 | 默认 2.5，范围 1.3–3.0 |
| 当前间隔 | `interval_days` | 是 | 默认 0 |
| 下次到期 | `due_at` | 是 | — |
| 复习次数 / 遗忘次数 | `reps` / `lapses` | 是 | 用于展示与统计 |
| 上次复习时间 | `last_review` | 是 | — |
| 评分流水 | `user.review_log` | 是 | 每次评分追加一条，用于回溯 |
| 队内进度 | 派生（当前索引 / 队列长度） | 是 | 驱动进度条 |

#### 首版必要操作

| 编号 | 操作 | 触发 | 结果 | 边界 |
|---|---|---|---|---|
| M5-O1 | 开始一轮复习 | 首页主行动卡 / 侧栏「复习」 | 快照当前到期队列，进入第一张 | 队列为空 → 显示空状态 + 引导去各分区标记 |
| M5-O2 | 展开解析 | 点按钮 / 按 `空格` | 显示该概念完整解析，出现评分按钮 | 展开前不显示任何解析内容 |
| M5-O3 | 评分 | 点三档按钮 / 按 `1` `2` `3` | 计算新状态 → 写入 `concept_state` 与 `review_log` → 自动进入下一张 | 见下方算法表 |
| M5-O4 | 中途退出 | 点返回 | **已评分的保留，未评分的状态不变**（不改动） | 再次进入重新构建队列 |
| M5-O5 | 完成 | 队列走空 | 显示「今天复习完了（N 个）」+ 明日到期数 + 返回首页 | — |

**间隔重复算法（SM-2 简化版，纯函数）**

| 评分 | 记忆锚点 | `ease` 变化 | 新间隔 |
|---|---|---|---|
| 忘了 (0) | `lapses + 1` | −0.2（下限 1.3） | 重置为 **1 天** |
| 模糊 (1) | — | −0.15 | 上次间隔 × 1.2；首次为 1 天 |
| 记得 (2) | `reps + 1` | +0.1（上限 3.0） | 上次间隔 × `ease`；首次为 2 天 |

`due_at = 本次复习时间 + 新间隔`。算法必须是**纯函数**（输入：旧状态 + 评分 + 当前时间；输出：新状态），不碰数据库，以便独立单元测试。

#### 首版明确不做
- 自定义复习顺序、只复习某专题
- 复习统计报表、正确率曲线、连续天数
- 自测题 / 填空题（首版只有"回忆—评分"这一种形式）

#### 状态处理

| 状态 | 表现 |
|---|---|
| 队列为空 | 「复习队列是空的」+ 说明如何标记 + 返回首页按钮 |
| 内容更新后概念被删 | 该条从队列静默剔除，不报错 |
| 复习中的概念已被移除 | 显示「这个概念已不在内容库中」，直接跳到下一张 |

---

### 4.4 M6 搜索

#### 用途
当用户记得"有这么个概念"但忘了在哪时，用最短路径找到它。

#### 首版必要数据

| 数据 | 来源 | 必填 |
|---|---|---|
| 全文检索索引 | 虚拟表，索引 `title` / `one_liner` / `tags` / 正文纯文本 | 是 |
| 最近搜索词（≤5 条） | 本地设置 | 否 |
| 结果分组信息 | `concepts.domain` + `concepts.track` | 是 |

#### 首版必要操作

| 编号 | 操作 | 触发 | 结果 | 边界 |
|---|---|---|---|---|
| M6-O1 | 即时搜索 | 输入即搜（无提交按钮） | 结果实时刷新，命中词高亮 | 输入 < 1 字符不搜 |
| M6-O2 | 打开结果 | 点结果行 | 进入该概念页 | — |
| M6-O3 | 空输入状态 | 清空输入 | 显示最近搜索词，点击可直接填入 | 无历史则不显示该区块 |
| M6-O4 | 无结果 | 自动 | 「没有找到"××"」+ 两条建议（换关键词 / 去设置页检查内容更新） | — |

#### 首版明确不做
- 高级筛选（按难度、按状态过滤搜索结果）
- 搜索历史持久化到数据库（首版只存最近 5 条，存本地设置）
- 拼音搜索、模糊纠错

---

### 4.5 M7 设置

#### 用途
三类事：**更新内容**、**管我的数据**、**调外观**。

#### 首版必要数据

| 数据 | 来源 | 必填 |
|---|---|---|
| 当前内容版本 / 内容和日期 / 分区概念数 | `content.meta` + 聚合 | 是 |
| 上次检查更新时间 | 本地设置 | 是 |
| 自动检查开关 | 本地设置（默认开） | 是 |
| 上次导出时间 | 本地设置 | 否 |
| 主题偏好 | 本地设置（默认跟随系统） | 是 |
| 最近备份文件列表（≤5） | 文件系统扫描 | 是 |

#### 首版必要操作

| 编号 | 操作 | 触发 | 结果 | 边界 |
|---|---|---|---|---|
| M7-O1 | 检查更新 | 点「立即检查」 | 就地变 inline loading → 就地显示结果（**不弹窗**） | 无网络 → 显示"检查失败，不影响使用"；有更新 → 就地展开更新详情 |
| M7-O2 | 应用更新 | 更新详情里点「立即更新」 | 下载 → 校验 → 原子替换 → 顶栏版本号更新 → 就地显示"已更新到 vX" | 任一环节失败 → **保留旧库**，就地显示原因 + 重试 |
| M7-O3 | 自动检查开关 | 切换 | 写入本地设置 | 关闭后启动不再检查（顶栏不出现更新提示） |
| M7-O4 | 导入内容 | 选一个本地 `concepts.jsonl`（版本从首行 `_meta` 读取） | 走与联网更新**完全相同**的校验与替换流程（跳过 sha256，文件即来源） | 校验不过 → 拒绝替换，保留旧库 |
| M7-O5 | 导出我的数据 | 点「导出」 | 选择保存位置 → 生成单个 JSON 文件 | 见 §6.4 |
| M7-O6 | 导入我的数据 | 点「导入」+ 选文件 | 校验 → **展示差异预览** → 确认 → 事务内合并 | 见 §6.5 |
| M7-O7 | 重置复习进度 | 点「重置」 | **二次确认**（须输入确认文案）→ 清空进度 | 明确告知不可撤销；默认不勾选"同时清空收藏" |
| M7-O8 | 切换主题 | 三选一 | 立即生效并持久化 | — |

#### 首版明确不做
- 应用本体自动更新（只更新内容，不更新程序）
- 快捷键自定义、字号调节
- 多语言

---

## 5. 内容规格

### 5.1 首版规模（已确认：每区 20–30）

| 分区 | 概念数 | 专题分布 |
|---|---|---|
| 经济学 | **24** | 宏观经济 10 · 微观经济 8 · 计量经济 6 |
| 金融学 | **24** | 股票 7 · 债券 6 · 基金 5 · 量化 6 |
| 世界热点 | **20** | 事件解剖 12 · 数据观察 8 |
| **合计** | **68** | 覆盖全部 9 个专题，**每个专题不少于 5 个概念** |

**为什么是 68 而不是 90**：目标是让 9 个专题都有足够样本验证"每个专题的讲解方式确实不同"。多出来的 22 个不会带来新的验证价值，只会放大 Schema 返工的风险。先跑通闭环再扩。

**热点区为什么只放 20**：热点区的内容会持续被更新覆盖，首版目的在于验证"更新通道 + 时效标记 + 相关基础概念"这条链路是否成立，不在数量。其中「数据观察」类（如"怎么看 PMI""怎么读社融"）机制稳定、不易过时，是热点区的长期骨架。

### 5.2 概念的数据要求

完整字段定义见 `架构设计.md` §3。此处只列**内容写作必须遵守的硬性规格**：

| 字段 | 规格 |
|---|---|
| `id` | `domain.track.slug`，全库唯一，小写连字符 |
| `title` | ≤ 20 字，是一个概念名，不是一句话 |
| `one_liner` | ≤ 40 字，**负责"勾人"**——是一个问题或反直觉的断言 |
| `key_points` | **1–3 条，每条 ≤ 30 字**，**负责"给答案"**——必须是结论，不是话题 |
| `difficulty` | 1 入门 / 2 进阶 / 3 硬核，三个档位都要有分布 |
| `blocks` | 3–5 个 |
| `recipe` | 与 `blocks` 实际类型一致，3–5 种 |
| `source` | 数据类 / 公式类 / 热点类必填 |
| `links` | ≥ 1 条，且每条必须带 `reason`（"为什么相关"），**不允许裸链接** |

**`one_liner` 与 `key_points` 的分工**——这是要点速览页成立的前提：

```
one_liner  央行降息，为什么企业不一定马上借钱？   ← 制造疑问
key_points ·
           · 三道关：银行负债端 → 信贷定价 → 企业预期   ← 给出答案
           · 漏损多发生在最后一关，取决于预期而非利率
```

### 5.3 内容质量门禁（`tools/validate-content`，每次内容提交必跑）

| # | 检查项 | 不通过的后果 |
|---|---|---|
| 1 | `id` 唯一且符合命名规范 | 报错 |
| 2 | `title` ≤20 字、`one_liner` ≤40 字 | 报错 |
| 3 | `key_points` 条数 1–3、单条 ≤30 字、不与 `one_liner` 重复 | 报错 |
| 4 | `blocks` 数量 3–5，类型在白名单内 | 报错 |
| 5 | `recipe` 与 `blocks` 实际类型一致 | 报错 |
| 6 | **`recipe` 组合全库唯一** | 报错 |
| 7 | `links` ≥1 条且**每条带 `reason`**；目标 `id` 必须存在 | 报错 |
| 8 | 三大区差异化：`econ` 含 `curve`/`flow`；`finance` 含 `formula`/`calc`/`dataviz`；`hotspot` 含 `timeline` 且 `source` 非空 | 报错 |
| 9 | `dataviz` 块必须带 `source` | 报错 |
| 10 | 无孤立概念（每个概念至少被一条 `links` 指向，或属首批引入） | 警告 |
| 11 | 难度 1/2/3 三档均有分布 | 警告 |

> 门禁 6 在原型自测中**真的抓到过一处违规**：`收益率曲线` 与 `跟踪误差` 的讲解方式组合原本完全相同。这条规则不是装饰。

### 5.4 内容生产流程

```
起草 YAML  →  跑 validate-content  →  人工复核事实与来源  →  打包 build-package  →  本地导入验证
   ↑                                                                                    │
   └────────────────────────── 不通过则回炉 ────────────────────────────────────────────┘
```

**分批策略**：先做 9 个专题各 2–3 个概念（约 24 个）验证渲染与复习闭环 → 确认无误后再补齐到 68 个。

---

## 6. 本地数据文件、备份与恢复

### 6.1 文件清单与位置

**统一放在**：`~/Library/Application Support/Recall/`

```
Recall/
├─ content.db            内容库（SQLite）—— 可被整包替换
├─ user.db               我的数据（SQLite）—— 任何更新流程都不覆盖
├─ cache/
│  ├─ pending/           下载中的内容包（成功后自动清空）
│  └─ last-check         上次检查更新的时间戳
├─ backups/
│  └─ user-YYYYMMDD-HHmm.db   内容更新前的自动备份，保留最近 5 份
└─ logs/                 运行日志（仅保留最近 7 天，仅记录更新与错误）
```

**硬性要求**

| # | 要求 |
|---|---|
| F1 | 所有数据文件**只写入上表目录**，不写入用户的桌面、文档、下载等目录 |
| F2 | 除内容更新外**不产生任何网络请求**；不上传任何用户数据 |
| F3 | 卸载 / 删除 App **不自动删除**该目录；用户数据需手动清理 |
| F4 | 两个数据库均启用 WAL 模式，保证异常退出时的一致性 |
| F5 | 任何内容更新操作**不得修改 `user.db`**（唯一例外：写入 `backups/` 里的一份副本） |

### 6.2 content.db 规格

| 表 | 用途 | 更新时 |
|---|---|---|
| `meta` | 版本号、内容日期、Schema 版本 | 整表覆盖 |
| `concepts` | 概念主表（含 `blocks` / `links` / `key_points` 的 JSON 列） | 整表覆盖 |
| `tracks` | 专题定义与排序 | 整表覆盖 |
| `concepts_fts` | 全文检索虚拟表 | 跟随重建 |

字段定义见 `架构设计.md` §2.2。

**更新方式**：写入 `content.db.new` → 全量校验 → **原子 rename 替换**。中断（断电 / 崩溃）时旧库仍完整可用。

### 6.3 user.db 规格

| 表 | 用途 | 首版是否启用 |
|---|---|---|
| `concept_state` | 每个概念的掌握状态、间隔重复参数 | ✅ 启用 |
| `review_log` | 评分流水（只追加） | ✅ 启用 |
| `favorites` | 收藏 | ✅ 启用 |
| `settings` | 主题、自动检查开关、上次检查 / 导出时间 | ✅ 启用 |
| `notes` | 个人笔记 | ❌ **首版不启用**（表预留，UI 不做） |

**孤儿数据策略（重要）**：内容更新后若某个 `concept_id` 消失，`user.db` 中它的记录**不删除**。理由：内容可能被误删后又加回来，进度不该因此蒸发。查询时忽略这些记录即可；只有用户显式「重置复习进度」时才真正清理。

### 6.4 备份要求

**两层备份机制，缺一不可。**

#### 第一层：自动备份（防内容更新出错）

| 项 | 要求 |
|---|---|
| 触发时机 | **每次内容更新替换 `content.db` 之前** |
| 备份对象 | `user.db`（内容库无需备份，可重新下载） |
| 存储位置 | `backups/user-YYYYMMDD-HHmm.db` |
| 保留策略 | 最近 **5** 份，超出自动删除最旧的 |
| 失败处理 | 备份失败则**中止本次内容更新**，不替换内容库 |

#### 第二层：手动导出（防机器故障 / 换机）

| 项 | 要求 |
|---|---|
| 入口 | 设置页「导出我的数据」 |
| 输出 | **单个 JSON 文件**，默认文件名 `recall-user-YYYYMMDD.json`，由用户选择保存位置 |
| 内容 | Schema 版本号 + 导出时间 + 应用版本 + 全部 `concept_state` + `favorites` + `settings` + （预留）`notes` |
| 为何用 JSON 而非直接复制 db | 可读、可手工查看与修改、可跨 Schema 版本迁移、体积小 |
| 附带信息 | 导出时同时记录每个概念当时的 `title`（便于脱离内容库识别"这条进度对应哪个概念"） |
| 完成后 | 就地提示保存路径 + 条数统计，**不弹窗** |

**导出文件格式示例**

```json
{
  "schemaVersion": 1,
  "appVersion": "1.0.0",
  "exportedAt": "2026-09-21T14:58:00+08:00",
  "contentVersion": "2026.09.21",
  "conceptState": [
    { "conceptId": "finance.bond.duration", "title": "久期",
      "status": "known", "ease": 2.6, "intervalDays": 12,
      "dueAt": "2026-10-03", "reps": 4, "lapses": 1,
      "lastReview": "2026-09-21T13:20:00+08:00" }
  ],
  "favorites": ["finance.bond.duration"],
  "settings": { "theme": "light", "autoCheckUpdate": true },
  "notes": []
}
```

### 6.5 恢复要求

| 项 | 要求 |
|---|---|
| 入口 | 设置页「导入我的数据」 |
| 前置校验 | ① Schema 版本可识别 ② 文件结构合法 ③ 统计可识别 / 不可识别的概念数 |
| **差异预览** | **必须先展示预览再执行**：本次将更新 N 条进度、其中 M 条比现有记录更新、K 条对应概念已不在内容库中。用户确认后才写入 |
| 合并策略 | **合并式，不是覆盖式**。同一条进度按 `last_review` 取较新者 |
| 概念已不存在 | 该条**仍写入** `concept_state`（成为孤儿记录），不报错；差异预览中告知数量 |
| 事务性 | 整个导入在**单个事务**内完成，任一步失败则整体回滚，不留半成品 |
| 完成后 | 就地提示"已恢复 N 条进度"，并提示需重启或刷新以生效 |

### 6.6 内容更新与用户数据的关系（对应 R7）

| 环节 | 对 `user.db` 的影响 |
|---|---|
| 检查更新 | 无（只读 `meta` 与网络） |
| 下载内容包 | 无（写 `cache/pending/`） |
| 校验失败 | 无 |
| 替换内容库前 | **写入一份副本到 `backups/`** |
| 替换内容库 | 无（只动 `content.db`） |
| 替换后 | 概念新增 → 进度视为 `unread`；概念修改 → 进度保留；概念删除 → 进度保留为孤儿 |

**验收要求**：执行一次完整的内容更新（含新增 / 修改 / 删除三种情况），逐条核对 `concept_state`，**丢失记录数必须为 0**。

### 6.7 危险操作与防护

| 操作 | 防护 |
|---|---|
| 重置复习进度 | 二次确认（须输入确认文案）+ 明确告知不可撤销 + 默认**不**同时清空收藏（需单独勾选） |
| 导入我的数据 | 强制差异预览 + 事务回滚 |
| 替换内容库 | 自动备份 `user.db`；备份失败则中止；sha256 校验；原子替换 |
| 删除 App | 数据目录保留，不自动清理 |

---

## 7. 非功能需求

| # | 项 | 要求 |
|---|---|---|
| NF1 | 冷启动 | 双击图标到首页可交互 **≤ 1.5 秒** |
| NF2 | 离线可用 | 除内容更新外，全部功能断网可用；无网络时不出现任何错误提示 |
| NF3 | 离线优先 | 启动路径中**没有任何一步等待网络**；网络检查在后台进行，超时 5 秒 |
| NF4 | 包体 | `.app` 体积 ≤ 250MB |
| NF5 | 图标 | 自定义 `.icns`，在 Finder / Dock / 切换器中均正确显示；含 Retina 各尺寸 |
| NF6 | 数据持久化 | 刷新、重启、关机、内容更新后，进度 100% 保持 |
| NF7 | 安全性 | 渲染层无 Node 权限（`contextIsolation: true`）；内容包必须 sha256 校验；因内容来自网络，一律视为不可信输入 |
| NF8 | 键盘支持 | 复习页 `空格` 展开、`1/2/3` 评分；搜索页打开即聚焦 |
| NF9 | 崩溃恢复 | 原子替换 + WAL 模式，异常退出最坏结果是"仍在用旧内容"，绝不出现"应用打不开" |
| NF10 | 主题 | 浅色 / 深色 / 跟随系统，深浅两套均需可读 |
| NF11 | **视觉 token 化** | 所有颜色 / 圆角 / 间距 / 字号 / 字体栈 / 动效时长必须收敛到 `src/styles/tokens.css` 单一来源；组件内不允许出现视觉字面量。这是 v1.1 视觉升级不返工的前提，详见 `页面交互设计.md` §5.5 |
| NF12 | 版本策略 | **v1.0 走中性视觉**（清晰、不抢内容，不投入风格化成本）；**二次元风格属于 v1.1**，不在首版范围内 |

---

## 8. 页面状态完整性要求

**每一页都必须实现四种状态**，不允许只做"有数据"那一种：

| 状态 | 要求 |
|---|---|
| 空 | 说明"为什么空"+ 给出一个明确的下一步动作 |
| 加载 | 骨架屏（不用 spinner），内容区高度不塌陷 |
| 错误 | **就地**显示原因 + 重试按钮，不弹 modal |
| 部分失败 | 单个解析块降级为"此部分暂时无法显示"，其余照常渲染 |

---

## 9. 需求可追溯矩阵

| 需求 | 来源 | PRD 章节 |
|---|---|---|
| R1 本地运行 / 图标 / 不经浏览器 | 用户原话 | §3.1、§6.1、§7 NF1/NF4/NF5 |
| R2 大量经济金融知识 | 用户原话 | §5.1、§5.2 |
| R3 分成多个部分 | 用户原话 | §3.2、§4.2 |
| R4 每页单概念、解析不复杂 | 用户原话 | §3.4、§4.2.1 |
| R5 解析方法多样化 | 用户原话 | §5.2、§5.3（门禁 4–6、8） |
| R6 不需要在浏览器中打开 | 第 2 轮确认 | §3.1 应用外壳 |
| R7 打开时可联网更新数据库 | 第 2 轮确认 | §4.5（M7）、§6.6 |
| R8 点分区先见内容，详情展开 | 第 3 轮确认 | §3.4、§4.2.1、§5.2（`key_points`） |
| 首版规模 20–30 | 第 4 轮确认 | §5.1 |

---

## 10. 首版范围界定

### In（首版必须做）

三大分区共 68 个概念 · 11 种讲解方式 · 要点速览页 + 概念详情页两级结构 · 状态机与筛选 · SM-2 复习 · 全文搜索 · 内容联网更新 + 手动导入 · 用户数据导出 / 导入 / 自动备份 · 深浅主题 · 键盘操作

### Out（首版明确不做）

账号与云同步 · 移动端 / Web 版 · 应用本体自动更新 · 内容在线编辑器 · 个人笔记 · 学习统计报表 · 自定义复习范围 · 自测题 / 填空 · 多语言 · 分享

---

## 11. 验收标准（首版交付时逐条核对）

| # | 验收项 | 通过标准 |
|---|---|---|
| A1 | 双击图标启动 | 看到首页；Dock 与 Finder 图标正确；冷启动 ≤1.5s |
| A2 | 三大分区内容完整 | 各 ≥20 个概念，9 个专题全覆盖，门禁 0 error |
| A3 | 两级结构成立 | 速览页无任何解析 block；概念页只出现当前概念；从启动到完整解析 ≤2 次点击 |
| A4 | 讲解方式多样化 | 全库任意两概念 recipe 组合不重复；三大区各命中本区必含讲解方式 |
| A5 | 状态与复习 | 状态机全部路径可走通；复习队列顺序正确；评分后状态与间隔符合算法表 |
| A6 | 内容更新 | 断网时静默跳过；有更新时顶栏提示；更新后进度丢失数为 0 |
| A7 | 备份与恢复 | 更新前自动备份生成；手动导出 JSON 可读；导入有差异预览；导入为合并式且事务回滚 |
| A8 | 离线完整可用 | 断网状态下走通全部 7 个页面与复习流程，无任何错误提示 |
| A9 | 状态完整性 | 每个页面四种状态（空 / 加载 / 错误 / 部分失败）均已实现 |
| A10 | 持久化 | 重启 / 关机 / 更新内容后逐条核对，进度丢失数为 0 |
| A11 | 视觉可替换性 | 抽查 20 个组件文件，**0 处硬编码颜色 / 圆角 / 字号**；把 `tokens.css` 的主题层整体替换后，界面无残留旧色 |

---

## 12. 遗留问题

| # | 问题 | 影响范围 | 需何时确定 |
|---|---|---|---|
| P1 | 内容分发地址 | ✅ 已定 2026-09-21 | **GitHub 公开仓库 + jsDelivr CDN**：检查更新走 `@main/content-dist/manifest.json`（发布后 purge），下载正文走 `@{tag}/content-dist/concepts.jsonl`（长缓存）。**不用 zip**——Release 附件大陆走不通。详见 `架构设计.md` §4.2–4.4 |
| P2 | 视觉风格 | ✅ 已定 2026-09-21 | 首版中性卡片流；二次元风格列为 v1.1；首版只需保证 token 化（NF11） |
| P3 | 复习完成后是否要一个极简的"今日小结" | M5 | 可在 P6 决定 |
| P4 | 速览页的状态 / 难度筛选 | ✅ 已定 2026-09-21 | **移除**，速览页不做筛选 |
| P5 | v1.1 二次元视觉的具体方向（配色 / 字体 / 是否加角色插画） | 全站 | v1.1 启动前单独开一轮设计 |
