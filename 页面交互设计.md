# 页面交互设计

> 项目：金融知识复习 App（代号 Recall）
> 版本：v1.0 ｜ 2026-09-21
> 状态：**P2 阶段交付**（信息架构 + 页面规格 + 交互状态 + 视觉规范）
> 配套产出：`原型/index.html`（可点击的交互原型，本文件所有页面均已实现）

---

## 0. 设计原则

| 原则 | 具体表现 |
|---|---|
| **内容优先** | 点进任一分区，**首屏就是内容，不是导航**。要点速览页不出现"点击开始学习"这类空转 |
| **两层纵深** | 分区页给**结论**（要点卡），概念页给**完整解析**（图表/公式/计算器）。第一层负责"知道"，第二层负责"弄懂" |
| **单页单概念** | 完整解析只在概念页出现，且只出现当前一个概念；速览页不渲染任何解析 block |
| **状态永远可见** | 每个要点卡、每个概念页顶部都能看到"我读到哪了"——这是"复习"而非"阅读"的前提 |
| **讲解方式可见** | 要点卡与概念页都显示本页由哪几种讲解方式组成（recipe 徽标），让内容结构本身成为信息 |
| **联网不打扰** | 内容更新的唯一入口是顶栏一个低调的版本徽标，不弹窗、不夺焦 |

---

## 1. 信息架构

```
Recall
├─ 首页 Home ──────────────────── 今天该复习什么 / 三区总览
├─ 经济学 Economics ───────────── 要点速览页
│  ├─ 宏观经济   ──→ 要点卡 × N ──[详情]──→ 概念页
│  ├─ 微观经济   ──→ 要点卡 × N ──[详情]──→ 概念页
│  └─ 计量经济   ──→ 要点卡 × N ──[详情]──→ 概念页
├─ 金融学 Finance ─────────────── 要点速览页
│  └─ 股票 / 债券 / 基金 / 量化 ──[详情]──→ 概念页
├─ 世界热点 Hotspot ───────────── 要点速览页
│  └─ 事件解剖 / 数据观察 ────────[详情]──→ 概念页
├─ 复习 Review ─────────────────── 卡片式抽取
├─ 搜索 Search ─────────────────── 全库检索
└─ 设置 Settings ───────────────── 内容更新 / 数据 / 外观
```

**路由**：`/` `/d/:domain` `/d/:domain/:track` `/c/:id` `/review` `/search` `/settings`

**两级页面的职责分工**：

| 层 | 页面 | 数据来源 | 回答的问题 |
|---|---|---|---|
| 第一层 | 要点速览页 `/d/:domain` | `title` + `one_liner` + `key_points` + `recipe` | 「这块我要记的几个结论是什么」 |
| 第二层 | 概念页 `/c/:id` | 完整 `blocks`（图表 / 公式 / 计算器 / 时间线…） | 「为什么是这样，怎么算」 |

---

## 2. 应用外壳

```
┌────────────┬──────────────────────────────────────────────────┐
│            │  扁平标题栏 52px                                  │
│  Sidebar   │  面包屑                      内容版本徽标 ●        │
│   208px    ├──────────────────────────────────────────────────┤
│            │                                                   │
│  首页       │                                                   │
│  ──────    │              内容区                               │
│  经济学 12  │          （最大宽度 760px 居中）                   │
│  金融学 18  │                                                   │
│  世界热点 8 │                                                   │
│  ──────    │                                                   │
│  复习       │                                                   │
│  搜索       │                                                   │
│  ──────    │                                                   │
│  设置       │                                                   │
│            │                                                   │
│  ◐ 进度环   │                                                   │
└────────────┴──────────────────────────────────────────────────┘
```

| 元素 | 规格 |
|---|---|
| 窗口默认尺寸 | 1180 × 780 |
| 最小尺寸 | 960 × 640 |
| Sidebar 宽度 | 208px 固定，不折叠 |
| 内容区最大宽度 | 760px 居中（**阅读类页面强制约束**，避免宽屏下行长过长） |
| 标题栏高度 | 52px |

**版本徽标**：三种状态——`内容 v2026.09.21`（当前）、`内容有更新 ●`（可点，展开更新详情）、`更新失败 ⚠`（可点看原因，不做红色警告，因为不影响使用）。

---

## 3. 页面规格

### 3.1 首页 Home

```
┌──────────────────────────────────────────────┐
│  9 月 21 日 周一                              │
│                                               │
│  ┌────────────────────────────────────────┐  │
│  │  今天要复习        8 个概念到期    →    │  │ ← 主行动卡
│  └────────────────────────────────────────┘  │
│                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ 经济学    │ │ 金融学    │ │ 世界热点  │     │ ← 三区入口
│  │ 12 概念   │ │ 18 概念   │ │ 8 概念    │     │
│  │ ▓▓▓▓░░ 33%│ │ ▓▓░░░░ 22%│ │ ▓▓▓▓▓░ 62%│    │
│  └──────────┘ └──────────┘ └──────────┘     │
│                                               │
│  最近读过                         全部 →      │
│  · 货币政策传导机制      econ.macro      2 天前│
│  · 久期                  finance.bond    3 天前│
└──────────────────────────────────────────────┘
```

**交互**：
- 主行动卡永远排第一。**没有到期复习时**，卡片文案变为"今天没有到期的复习"+"随便看一个 →"（随机抽一个未读概念）
- 三区卡片显示进度条 = 已掌握 / 总数
- 最近读过列表最多 5 条

**边界状态**：
- 内容库为空（首次启动且未联网）→ 主行动卡位置显示"内容库是空的，去设置页检查更新"，三区卡片置灰

---

### 3.2 分区页 = 要点速览页（**已改版**）

点进「经济学」后，**首屏直接是内容，不是导航**。每个概念先给你一张要点卡：标题 + 一句话 + 关键结论，完整解析收进「详情」。

```
┌──────────────────────────────────────────────────────┐
│  经济学                                              │
│  13 个概念的要点速览 · 已掌握 1 · 待复习 4            │
│                                                       │
│                                                       │
│  宏观经济 3 个概念 · 1 个待复习                    ▼  │
│  ┌────────────────────────────────────────────────┐  │
│  │ ◉ 货币政策传导机制      难度 2      [ 详情 → ]  │  │
│  │   央行降息，为什么企业不一定马上借钱？            │  │
│  │   · 三道关：银行负债端 → 信贷定价 → 企业预期      │  │
│  │   · 漏损多发生在最后一关，取决于预期而非利率      │  │
│  │   [定义][传导图][微案例][误区]                    │  │
│  └────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────┐  │
│  │ ○ 流动性陷阱            难度 3      [ 详情 → ]  │  │
│  │   利率降到接近零之后，货币政策还剩下什么？        │  │
│  │   · 利率接近零时，货币需求曲线趋于垂直            │  │
│  │   · 投放再多流动性也压不动利率                    │  │
│  │   · 钱没进信贷循环，就不会变成通胀                │  │
│  │   [定义][曲线][微案例][误区]                      │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
│  微观经济 2 个概念                                 ▶  │
│  计量经济 1 个概念                                 ▶  │
└──────────────────────────────────────────────────────┘
```

**要点卡规格**

| 元素 | 说明 |
|---|---|
| 状态点 | 沿用全站图例（见下表） |
| 标题 | 14.5px / 500 |
| 难度 | 右上角小字，只作阅读参考，**不提供按难度筛选** |
| 「详情 →」 | 右侧胶囊按钮，进入完整概念页。**整张卡都可点，效果相同** |
| 一句话 | 来自 `one_liner`，12.5px 次要色 |
| 关键结论 | 来自 `key_points`，1–3 条，每条带前导点号 |
| recipe 徽标 | 本页完整解析里用了哪几种讲解方式 |
| 内容截至 | **仅热点区**显示，琥珀色 |

**状态点图例**（贯穿全站）：

| 符号 | 状态 | 颜色 |
|---|---|---|
| ○ 空心 | 未读 | 次要文字色 |
| ◐ 半实心 | 读过 / 待复习 | 品牌蓝 |
| ● 实心 | 已掌握 | 绿色 |
| ◉ 带环 | 今天到期复习 | 琥珀色 |

**交互**

- Track 手风琴折叠。默认展开**有待复习概念**的 track；若都没有，展开第一个
- **不提供筛选条**：要点卡本身就是"一屏扫完"的形态，再加一层筛选是给用户增加操作而不是减少负担。要缩小范围请用搜索页
- 点卡片任意位置（或「详情」）→ 进入完整概念页
- 分区无内容 → "这个分区还没有内容" + 直达设置页检查更新

**为什么是「要点速览」而不是「完整解析平铺」**

- 首屏就能读到**结论**，不需要往下滚好几次才遇到有用信息
- 保留了「单页单概念」这条原则——速览页不出现任何图表、公式、计算器，完整解析只在概念页里
- 复习时先看要点卡做自测（"这条我能背出来吗"），背不出来再点详情，比直接看全文更有效

---

### 3.3 概念页 Concept Page（核心页面 · 「详情」的落点）

```
┌──────────────────────────────────────────────┐
│  ← 返回经济学要点速览                        │ ← 返回入口，必备
│                                               │
│  经济学 / 宏观经济                   ○ 收藏   │
│                                               │
│  货币政策传导机制               难度 2        │
│  央行降息，为什么企业不一定马上借钱？          │
│                                               │
│  本页讲解方式  [定义] [传导图] [微案例] [误区] │ ← recipe 徽标
│  ─────────────────────────────────────────    │
│                                               │
│  央行降息，只是把资金的「批发价」压低了……      │
│                                               │
│  ┌── 传导链路 ──────────────────────────┐    │
│  │ 政策利率 → 银行成本 → 贷款利率 → 投资 │    │
│  └──────────────────────────────────────┘    │
│                                               │
│  ┌── 2024 年 LPR 下调之后 ──────────────┐    │
│  │  1 年期 LPR 累计下调   20bp           │    │
│  └──────────────────────────────────────┘    │
│                                               │
│  ✗ 常见误区：降息一定会刺激企业投资            │
│  ✓ 实际上：只有当预期回报率高于贷款利率时才借  │
│                                               │
│  ─────────────────────────────────────────    │
│  相关概念                                     │
│  → 流动性陷阱    对比：传到不动时的极端情形     │
│  → 收益率曲线    延伸：利率如何影响债券定价     │
│                                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐     │
│  │ 标记不懂  │ │ 加入待复习│ │ 我掌握了  │     │
│  └──────────┘ └──────────┘ └──────────┘     │
└──────────────────────────────────────────────┘
```

**结构规格**：

| 区块 | 规格 |
|---|---|
| 面包屑 + 收藏 | 固定顶部，滚动时保持可见 |
| 标题区 | 标题 22px / 500，one_liner 15px 次要色 |
| recipe 徽标 | 每个讲解方式一个小胶囊，**只显示名称不显示类型 id** |
| Block 流 | 块间距 32px；每种 block 有自己的视觉形态（见 §4） |
| 相关概念 | 必须带"为什么相关"的原因文案，不允许裸链接 |
| 底部操作条 | 滚动到页尾才出现；三个按钮等宽 |

**交互**：
- 顶部固定「← 返回经济学要点速览」返回入口。改版后概念页是「详情」的落点，返回路径必须一眼可见，不能只靠面包屑里一个彩色词
- 打开即写入 `status: reading`（如果原先是 unread）
- 「我掌握了」→ `status: known` + 从复习队列移除
- 「加入待复习」→ `status: review` + `due_at = 今天`
- 「标记不懂」→ `status: fuzzy`，**下次打开该概念时自动把它排到最前**
- 相关概念是唯一的"跳出去"入口，且必须用户显式点击——不做自动下一篇

**边界状态**：
- 概念不存在（链接失效）→ "这个概念已不在内容库中"+ 返回按钮
- Block 渲染失败（如公式语法错）→ 该块降级为"此部分内容暂时无法显示"，**不影响其他块**

---

### 3.4 复习页 Review

两段式：**回忆 → 展开 → 评分**。

```
Stage 1（只给线索）
┌──────────────────────────────────────────────┐
│  ▓▓▓▓▓▓░░░░░░░░░░░░░░░░  3 / 8               │
│                                               │
│                                               │
│        货币政策传导机制                        │
│     央行降息，为什么企业不一定马上借钱？        │
│                                               │
│                                               │
│           [ 展开解析（空格） ]                 │
└──────────────────────────────────────────────┘

Stage 2（展开后评分）
┌──────────────────────────────────────────────┐
│  ▓▓▓▓▓▓░░░░░░░░░░░░░░░░  3 / 8               │
│  ┌────────────────────────────────────────┐  │
│  │ （完整概念页内容，可滚动）              │  │
│  └────────────────────────────────────────┘  │
│  ┌──────┐ ┌──────┐ ┌──────┐                 │
│  │ 忘了 │ │ 模糊 │ │ 记得 │                 │
│  └──────┘ └──────┘ └──────┘                 │
└──────────────────────────────────────────────┘
```

**交互**：
- 键盘：`空格` 展开，`1/2/3` 评分，`←` 返回上一张
- 评分后立即出下一张，不做"完成"提示
- 队列清空 → 显示"今天复习完了（8 个）"+ 明日到期数与返回首页
- 队列中途想退出 → 已评分的保留，未评分的保持不动

**算法**（SM-2 简化版，纯函数）：

| 评分 | 效果 |
|---|---|
| 忘了(0) | `lapses+1`，`ease -= 0.2`（下限 1.3），间隔重置为 1 天 |
| 模糊(1) | `ease -= 0.15`，间隔 = 上一间隔 × 1.2（首见为 1 天） |
| 记得(2) | `ease += 0.1`（上限 3.0），间隔 = 上一间隔 × ease（首见为 2 天） |

---

### 3.5 世界热点热点页

复用概念页，但**三个额外元素**：

1. 标题下方强制显示 `内容截至 2026-09-20`（来自 `updated_at`），超过 30 天显示为琥珀色提示"这条内容可能已过时"
2. 页面顶部按 `timeline` block 优先排序
3. 底部多一块**「相关基础概念」**，把热点事件链接到经济学的机制概念（如"美联储降息" → "货币政策传导机制"）——**这是把时效性事件沉淀为长期知识的关键设计**

---

### 3.6 搜索页 Search

```
┌──────────────────────────────────────────────┐
│  🔍 输入关键词…                               │
│                                               │
│  经济学 · 宏观经济                             │
│  · 货币政策传导机制 …降息，只是把资金的「批发… │
│  金融学 · 债券                                │
│  · 久期 …利率变动 1%，债券价格变动多少…       │
└──────────────────────────────────────────────┘
```

- 即时搜索（输入即搜，无提交按钮）
- 结果按 domain → track 分组，命中词高亮
- 空输入 → 显示最近搜索过的 5 个词
- 无结果 → "没有找到" + 建议（去掉筛选、检查内容更新）

---

### 3.7 设置页 Settings

```
内容
  当前版本        v2026.09.21（经济学 12 · 金融学 18 · 热点 8）
  上次检查        今天 13:52
  检查更新        [ 立即检查 ]
  自动检查        [ 开 ]   启动时后台检查，有更新才提示
  导入内容包      [ 选择文件… ]

数据
  导出我的数据    [ 导出 ]     含进度、收藏、笔记
  导入我的数据    [ 导入 ]
  重置复习进度    [ 重置 ]     ⚠ 二次确认

外观
  主题            [ 浅色 ] [ 深色 ] [ 跟随系统 ]

关于
  Recall v1.0 · 纯本地运行 · 内容可离线使用
```

**交互**：
- 「检查更新」点击后按钮变 inline loading，结果就地显示，**不弹窗**
- 有更新时在按钮下方展开更新详情：版本号、日期、三分区新增/修改数量、[ 立即更新 ]
- 更新进度显示为一条细进度条，完成后就地显示"已更新到 v2026.09.21"
- 更新失败就地显示原因 + [ 重试 ]，**不阻塞任何其他功能**

---

## 4. 讲解方式的视觉形态

11 种 block 各自长什么样。**这是"解析方法多样化"在视觉上的落地。**

| type | 视觉形态 | 关键视觉特征 |
|---|---|---|
| `prose` | 普通段落 | 行高 1.7，重点词加粗；**无边框、无底色**（作为基准层） |
| `formula` | 居中公式 + 符号注释表 | 公式区浅灰底；符号注释为两列小表 |
| `curve` | 坐标图 + 参数滑块 | 滑块在右下角；拖动时曲线实时重绘 + 显示当前值 |
| `dataviz` | 折线/柱状图 | 右下角固定显示数据来源小字 |
| `compare` | 对比表 | 表头浅底，首列加粗；奇偶行无底色差（避免噪） |
| `flow` | 横向箭头链 | 每步一个圆角块 + 下方小字注释 |
| `timeline` | 纵向时间轴 | 左侧竖线 + 圆点；热点事件用琥珀色圆点 |
| `pitfall` | 左红右绿对照 | ✗ 误区 / ✓ 实际；下方一行"为什么" |
| `case` | 左侧竖线引用卡 | 顶部标题 + 可选数据行 + 正文 |
| `scale` | 水平刻度尺 | 把抽象数字锚到生活参照物 |
| `calc` | 输入表单 + 实时结果 | 输入即算，不设"计算"按钮 |

**一致性约束**：所有 block 共用同一套间距（上下 32px）、圆角（12px）、边框色。多样性来自**结构和交互**，不是来自配色和装饰。

---

## 5. 视觉规范

### 5.1 主题

提供 **浅色 / 深色 / 跟随系统** 三选一。深浅两套用同一组语义变量，**不出现"深色下某个块看不清"的情况**。

| 语义 | 浅色 | 深色 |
|---|---|---|
| 页面底 | `#FAFAF8` | `#1A1A18` |
| 卡片/面 | `#FFFFFF` | `#242422` |
| 主文字 | `#1F1E1C` | `#EDEBE6` |
| 次要文字 | `#6B6A65` | `#A5A39D` |
| 边框 | `rgba(0,0,0,0.10)` | `rgba(255,255,255,0.12)` |
| 品牌主色 | `#2F6FB0` | `#6FA8DC` |
| 已掌握 | `#4C8A3F` | `#8FBF73` |
| 待复习 | `#C68A1E` | `#E0B050` |
| 误区红 | `#B4453F` | `#E0857F` |
| 正确绿 | `#3F7A3A` | `#86C07A` |

### 5.2 金融数据配色（遵守 A 股约定）

**涨 = 红，跌 = 绿。** 这一条在 `dataviz` 和 `curve` 里强制生效，与西方惯例相反。所有涉及价格的图表必须有图例说明，避免误读。

### 5.3 排版

| 用途 | 字号 | 字重 | 行高 |
|---|---|---|---|
| 页面主标题 | 22px | 500 | 1.3 |
| 概念一句话 | 15px | 400 | 1.5 |
| 正文 | 14px | 400 | 1.7 |
| Block 标题 | 13px | 500 | 1.4 |
| 元信息 / 来源 | 12px | 400 | 1.5 |

字体：中文用系统字体栈（`-apple-system, "PingFang SC"`），公式与数字用等宽或衬线数字，保证对齐。

### 5.4 间距与圆角

- 间距梯度：4 / 8 / 12 / 16 / 24 / 32
- 卡片圆角 12px，胶囊圆角 999px，按钮圆角 8px
- 内容区左右内边距 32px

---

### 5.5 视觉风格的版本策略（首版中性 → v1.1 二次元）

**已定**：首版的视觉目标是**中性、清晰、不抢内容**，不追求风格化。你希望后续把风格调得更二次元一些，但那属于 **v1.1 的独立工作**，首版不在视觉上投入额外成本。

**为了让 v1.1 是"换皮"而不是"重写"，首版必须做到一件事：把所有视觉值收敛到一层 token。**

| 要求 | 说明 |
|---|---|
| T1 单一来源 | 所有颜色、圆角、间距、字号、字体栈、动效时长，只允许定义在 `src/styles/tokens.css` 一处 |
| T2 组件禁止硬编码 | 组件里只能出现 `var(--xxx)`；不允许出现 `#2F6FB0`、`12px`、`border-radius:12px` 这类字面量 |
| T3 语义命名 | token 按**用途**命名（`--text-secondary`、`--brand`、`--surface`），不按外观命名。禁止 `--blue-500`、`--radius-12` 这类换皮后会名不副实的名字 |
| T4 主题分两层 | token 分**基础层**（不随主题变，如间距梯度、字号阶梯）+ **主题层**（浅色 / 深色 / 未来的二次元主题各一套值） |
| T5 预留装饰位 | 要点卡与概念页顶部预留可选的**插图槽位**（首版不渲染内容、不占高度）。v1.1 加插画或角色时不需要改布局 |

**首版刻意不用的东西**——避免 v1.1 时先删再加、浪费一倍工作量：渐变、投影、发光、装饰性字体、大面积插画、非必要动效（只保留 hover 与展开的过渡）。

> 这条约束不是"以后再补"，而是**首版必须遵守**。视觉 token 化属于工程骨架的一部分（见 `开发计划.md` 工作包 A4），一旦组件里散落了硬编码颜色，v1.1 就变成重写。

---

## 6. 全站交互状态清单

每页都必须实现这四种状态，不允许只做"有数据"那一种。

| 状态 | 表现 |
|---|---|
| **空** | 说明"为什么空"+ 一个明确的下一步动作 |
| **加载** | 骨架屏（不用 spinner），内容区高度不塌陷 |
| **错误** | 就地显示原因 + 重试按钮，**不弹 modal** |
| **部分失败** | 单块降级，其余照常渲染 |

---

## 7. 与原型文件的对应关系

`原型/index.html` 实现了本文件的全部 7 个页面、11 种 block、深浅双主题、以及上述交互状态中的"空"与"错误"两种。可直接双击打开，或在本会话的预览面板中查看。

**原型中已可实际操作的功能**：

| 功能 | 位置 |
|---|---|
| 侧栏导航切换全部 7 个页面 | 左侧 |
| **要点速览页：13 张要点卡（含要点、难度、recipe、「详情 →」）** | 三大分区首屏 |
| **「详情」→ 完整解析 → 「← 返回要点速览」往返** | 任意要点卡 |
| 概念页 11 种 block 的实际渲染 | 任意概念 |
| 复习卡片两段式（展开 → 评分 → 下一张） | 复习页 |
| 即时搜索 + 命中高亮 | 搜索页 |
| 深浅主题切换（验证 token 层是否真的可替换） | 设置页 |
| 内容更新检查的三种状态演示 | 设置页 |
| 久期迷你计算器（拖动即算） | 「久期」概念页 |
| 需求价格弹性可拖动曲线 | 「需求价格弹性」概念页 |

### 7.1 原型自测结果

用无头 DOM 遍历全部页面与交互，**12 项断言全过、0 运行时错误**；8 个图表的 SVG 通过严格 XML 校验，无 NaN 属性。覆盖的关键断言：

- 每个概念都有 1–3 条 `key_points`，且单条 ≤30 字
- **全库 recipe 组合唯一**（这条在自测中真的抓到了一处重复：`收益率曲线` 与 `跟踪误差` 原本都是 prose+dataviz+compare+case，已修正）
- 速览页渲染的要点卡数量 = 该分区概念数，且**不出现任何解析 block**（证明"单页单概念"未被破坏）
- **速览页已彻底移除筛选条**（含 `setFilter` 函数与相关状态），并验证概念数为 0 的分区能正确显示空状态
- 速览页 → 详情 → 返回，往返路由正常
- 13 个概念页全部可渲染，各自含 recipe 徽标与相关概念

原型是**静态演示**，数据写死在文件里，不代表真实后端行为。
