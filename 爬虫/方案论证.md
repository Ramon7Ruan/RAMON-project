# 方案论证与数据源调研

> 状态：**留档**（本文档是"为什么这么选"的论证过程，**不是需求源**）
> 日期：2026-09-23
> 定位：**需求在 `PRD.md`**；本文档保留三方案对比、数据源可行性调研、能力边界与分级闸门的论证过程。
> 注意：本文档 §4.1 的目录结构（`tools/crawler/`）已被 `PRD.md` §6.2 的独立目录方案取代；以 PRD 为准。

**修订记录**

| 版本 | 变更 |
|---|---|
| v1 | 初版：范围裁剪 + 三方案对比 + 数据源 + 能力边界表 |
| **v2** | 评估 Ramon 提出的「自动发布 + 事后人工删除」；结论**不推荐**（§1.1），改为**分级闸门 + 事后反馈回路**（§7.4–7.5） |

---

## 1. 先说那处冲突（不绕过去）

**PRD §5.4 定义的内容生产流程是**：

```
起草 YAML → validate-content → 人工复核事实与来源 → build-package → 本地导入验证
```

**架构 §4.3 也写明**：`tools/publish.sh` **刻意不做 CI**，理由是"内容必须人工复核，自动发布会绕过这一步"。

而"爬虫自动获取内容进行更新"这句话，字面读就是**要绕过人工复核**。如果直接把爬虫接到发布链路上，等于推翻上面两条决定。所以第一件事不是设计爬虫，而是**明确自动化到哪一层就停**。

### 调和办法：把"人工复核"从"写"挪到"审 diff"

关键在于——人工复核这一步**不一定要靠手写来保证**。它可以变成：

```
爬虫采集 → 生成候选 + 变更预览（diff）→ 【人工审核 diff】→ 合入 YAML → 门禁 → 发布
```

复核的动作从"从头写一段话"变成"看这份 diff 对不对，勾选采纳"。**复核依然存在，只是成本从 30 分钟降到 3 分钟。**

这条改动的性质：**PRD §5.4 的措辞需要相应修订**（把"人工复核事实与来源"明确为"人工审核变更预览"），而不是被偷偷绕过。按项目的单一定义点原则，这属于需求变更，要改 PRD，不能在别处私自实现。

### 1.1 「自动发布 + 事后人工删除」这个替代思路（评估结论：不推荐）

Ramon 提出：不做事前审核，改成自动发布到 App，浏览时发现问题再人工删除。这个思路触及了真痛点（审核负担），但**"删除"在这个架构下是个错配的工具**。三条理由，按重要性排序。

#### 理由一：发现机制不可靠（最要命的一条）

「审核 diff」是**每次都会发生**的检查；「浏览时发现问题」取决于**你恰好点开哪一条**。

自动发布之后，一条错误内容的存活时间是不确定的——可能你当天就点开，也可能三个月都没碰过它。而**没有任何机制会在你发现之前提醒你**。

#### 理由二：错误类型错配——大多数错误要「改」，不要「删」

| 错误类型 | 你想做的动作 | 「删除」能解决吗 |
|---|---|:---:|
| 数值抄错 0.2 个百分点 | 改 | ❌ |
| 「内容截至」日期没更新 | 改 | ❌ |
| 引错来源 / source 写错 | 改 | ❌ |
| 时间线漏了一条 | 补 | ❌ |
| 因果解读写反 | 改那一段 | ⚠️ 只能整条删掉 |
| 整条事件不值得做 | 删 | ✅ |
| 内容同质化、正确废话 | 删 | ✅ |

能靠"删"解决的只有最后两类。**把"删除"当成主要的纠错手段，覆盖不了大多数错误。**

#### 理由三：删除在「整库替换」模型下不是稳定操作

已核实的技术事实：更新是把**整库**换掉——下载全部概念 → 建新库 → `rename` 原子替换整个 `content.db`。`user.db` 完全不参与。

所以**本地删掉的概念，下次更新会原样回来**。

这个可以绕过：在 `user.db` 里存一份「本地隐藏列表」（`user.db` 从不被替换，所以扛得住更新），UI 层过滤掉。**技术上可行，我不否认。**

但它只解决"它会不会回来"，解决不了理由一和理由二。

#### 一次事实修正：风险不在"新内容"，在"旧内容被改错"

我原本担心"自动发布的错误内容会被学进复习队列"。核实后要修正这个说法：

- **新发布的概念不会自动进复习队列**——新概念 `due_at` 为空、`is_due` 为假，必须你主动「加入待复习」或「标记不懂」才会进
- **真正的风险是**：一个**已经在你复习循环里**的概念（比如「怎么看 PMI」——数据观察类恰恰都是长期复习对象），被自动更新改错了数值，于是它**带着错误数字出现在复习里**，而你很难察觉"这次复习的数值和上次不一样"

也就是说：**自动发布的风险集中在对"已有概念"的静默修改上。** 这正好是交叉验证 + 跳变检测能覆盖的部分——也就是下面要说的「绿级」。

#### 那该怎么办：分级闸门 + 事后反馈（详见 §7.4–7.5）

他的诉求"降低审核负担"是对的，但不该用"取消审核 + 事后删除"来满足，而应该**让大多数更新根本不需要你审**：

- **绿级**：纯数值、双源一致、变化在合理区间 → **自动发布，完全不打扰你**（这是日常更新的多数）
- **黄级**：数值有疑问（单源 / 跳变 / 超容差）→ 发布但标记「待确认」
- **红级**：**任何涉及文字的改动** → 必须人审后才发布

再加上**事后反馈回路**（§7.5）：App 内给每个概念一个「这条有问题」入口，记录进 `user.db`（扛得住更新），下次爬虫运行前读这份清单，**相关问题强制升级为红级**。

这样既拿到了接近全自动的效率，又保留了一个**结构化**的发现渠道——而且这个渠道比"浏览时偶然发现"可靠得多，因为它能覆盖"用着用着觉得不对"这类 diff 看不出来的问题。

**一句话结论：把"事后删除"换成"事后标记 + 分级闸门"。** 「删除/隐藏」可以作为功能保留（让你清掉不想看的内容），但它不该被当成内容纠错手段。

---

## 2. 范围：爬虫只服务「世界热点」，不碰经济学与金融学

这是本计划最重要的一条裁剪建议。

| 分区 | 内容价值来自 | 爬虫能帮上什么 |
|---|---|---|
| 经济学（24） | **讲解机制**：必须含 `curve` / `flow`，`key_points` 必须是结论 | **几乎没有**。这些是存量知识，不随时间变 |
| 金融学（24） | **可计算、可比**：必须含 `formula` / `calc` / `dataviz` | **几乎没有**。同上 |
| 世界热点（20） | **时间、因果、分歧** | **主要在这里** |

理由很直接：经济学与金融学的 48 个概念，难点在"把机制讲成一句能背下来的结论"和"保证 68 个概念的讲解方式组合互不重复"。这两件事是**理解与表达**，不是**采集**。爬来的文字会立刻被门禁拦下（`recipe` 不唯一、`key_points` 不达标、`econ` 缺 `curve`）。

而热点区的价值恰恰是**时效**——内容会过时，正是采集能补上的。并且按 PRD §5.2，热点区只放 20 个是因为"它验证的是更新通道 + 时效标记 + 相关基础概念这条链路"，不是靠数量。

### 热点区内部还要再拆一次

| 专题 | 数量 | 特征 | 可自动化程度 |
|---|---|---|---|
| **数据观察** `data`（如 PMI、社融、通胀、北向资金） | 8 | **机制稳定、数值变动**。页面结构基本不变，只有数字和「截至日期」在变 | **高**——数值、日期、数据图、source 都能自动 |
| **事件解剖** `event`（如美联储降息、OPEC 减产） | 12 | 需要"发生了什么 + 各方怎么看"，因果解读是核心价值 | **低**——只能采集事实骨架，解读必须人工 |

**这个拆分决定了后面所有设计。** 数据观察类是可以做到接近全自动的；事件解剖类最多做到"帮你把事实按时间线摆好"。

---

## 3. 爬虫放在哪里：三个方案

| 方案 | 怎么跑 | App 要改吗 | 是否保留复核闸门 | 评价 |
|---|---|---|---|---|
| **A 构建期爬取** | 爬虫作为**独立工具**在你自己机器上跑，产出候选文件；你审完合入 `content/*.yaml`，走既有链路发布 | **零改动** | ✅ 保留 | **推荐起点** |
| **B 中间服务爬取** | 爬虫跑在 CI / 服务器，定时生成并发布 | 零改动 | ⚠️ 取决于设计 | 只适合**纯数据类**，见 §6 |
| **C App 运行时爬取** | App 自己去抓网页 | **大改** | ❌ 无法保留 | **不推荐**，见下 |

### 为什么明确不推荐方案 C

1. **破坏"离线优先"**——这是架构的第一原则。启动路径里不能有任何一步等网络
2. **渲染层没有 Node 权限**（NF7），抓取必须放在主进程，会引入大量解析代码和失败面
3. **反爬与结构变动**——网页改版时 App 会突然失效，而**你还热修不了**：App 本体不走内容通道更新，只能重新打包发版
4. **CORS / 证书 / 频率限制**这些浏览器侧问题会变成你的维护负担
5. 最要命的：**外部内容未经复核直接进库**，与"内容一律视为不可信"的既定原则正相反

方案 C 唯一的好处是"不需要你在中间做任何事"，但代价是把一个可控的离线工具变成一个随时可能坏的联网客户端。

---

## 4. 爬虫怎么建（方案 A 的具体形态）

### 4.1 目录与职责

```
tools/crawler/
├─ sources/                     每个数据源一个 adapter，互不影响
│  ├─ nbs.ts                    国家统计局
│  ├─ fred.ts                   FRED（圣路易斯联储）
│  ├─ pboc.ts                   中国人民银行
│  └─ safе.ts                   国家外汇管理局
├─ fetch.ts                     统一抓取层：缓存、限速、重试、超时、User-Agent
├─ normalize.ts                 把各源输出归一化为统一结构
├─ propose.ts                   由归一化数据生成「候选概念」
├─ diff.ts                      与现有 content/*.yaml 比对，产出变更预览
└─ cli.ts                       命令入口：crawl / diff / apply
```

**为什么语言可以自由选**：因为它是独立工具，不打包进 App。如果要用 `akshare`（Python 生态最全的财经数据聚合库），完全可以写 Python——App 侧永远只看到 YAML。这是方案 A 的一个隐性好处：**不污染 App 的技术栈**。

### 4.2 Adapter 接口（每个源实现同一套）

```
fetchRaw(cfg)      →  原始响应        // 只管拿数据，不解析业务
normalize(raw)     →  Observation[]   // 统一成 {指标, 期间, 值, 单位, 来源, 发布时间}
healthCheck()      →  ok | broken     // 源是否还活着
```

`Observation` 是唯一的内部交换格式。所有下游（propose / diff / 校验）只认它，不认具体某个源。这样**加一个新源不需要动下游**。

### 4.3 抓取层的六条纪律

| 纪律 | 原因 |
|---|---|
| **优先 API，其次 RSS，最后才解析 HTML** | HTML 结构一变就死；API 有明确的字段语义 |
| 本地缓存（按 URL + 日期） | 开发调试时不要反复打人家服务器 |
| 限速 + 退避重试 | 被拉黑的爬虫等于没有爬虫 |
| 显式超时 | 不能因为一个源卡住整个流程 |
| 失败时**不产出候选**，而不是产出空内容 | 空的数值比没有数值更危险——它会被当成"数据就是 0" |
| 每次抓取记录 `抓取时间 / 源 URL / HTTP 状态` | 溯源要用；出问题时要能定位 |

### 4.4 交叉验证（针对"数据看错"这类错误）

同一指标**至少两个独立源**互校（如 CPI 同比同时取统计局与 FRED 的中国序列）。仅在两者一致（或在容差内）时才产出候选；不一致就标红让人看。

这一条是为了防最隐蔽的错误：**接口通了、数字拿到了、但拿错了序列**（比如拿成累计值而不是当期值）。

---

## 5. 数据源选取

原则：**永远优先一手来源**。央行的原始公告 > 财经媒体转述。

### 第一层：官方统计（最高可信，作为主力）

| 源 | 覆盖 | 接入方式 | 是否需要密钥 |
|---|---|---|---|
| **国家统计局** | GDP、CPI、PPI、PMI、社零、固投、居民收入 | 公开数据接口，**无需鉴权**。新版走 `data.stats.gov.cn/dg/website/publicrelease/web/external`（V2.0，2026-03 更新，UUID 标识 + 时间分片）；旧版 `easyquery.htm` 仍可用（`dbcode=hgjd/hgnd`，`wdcode=zb/sj`） | **否** |
| **FRED**（圣路易斯联储） | 80 万+ 时间序列，覆盖美国与国际（含中国部分指标） | REST API。**额外有发布日历端点**（`fred/releases/dates`）——可用来提前知道"哪天会出新数据" | 是（**免费注册**，带 key 限速 120 次/分钟） |
| **中国人民银行** | M0/M1/M2、社融、LPR、公开市场操作 | 官网发布页 / 数据接口。**无正式开放 API**，需要针对页面结构写 adapter | 否 |
| **国家外汇管理局** | 外汇储备、跨境资金流动 | 官网 | 否 |
| **美联储 / ECB / BIS / IMF** | 政策声明、会议纪要、金融稳定报告 | 官网新闻发布页 / RSS | 否 |

> ⚠️ 一个必须知道的坑：**FRED 的数据会修订**。GDP 在初次发布后还有两次修订，非农就业每月回改前两个月。所以爬虫**必须支持"重拉最近 N 期"**，不能只做"追加新数据"——否则库里会留着已经被官方改掉的旧值。

### 第二层：结构化财经数据

- **东方财富 / 新浪财经**的公开行情与资金流接口（A 股指数、北向资金、板块数据）
- **`akshare`**（Python 库）——它把大量公开源聚合成了统一接口，能大幅减少 adapter 数量

> ⚠️ `akshare` 是**聚合器**，上游 API 挂了它就挂。所以：可以用它加速开发，但**关键指标要直连原始源**，不能全押在聚合器上。

### 第三层：财经媒体（只做线索，不做来源）

媒体的用途**仅限于"发现发生了什么"**——扫描标题，得到"今天有哪些可能值得做成概念的事件"。

**不作为内容来源**，理由三条：
1. **版权**：抓取并再分发正文有法律风险。官方统计与政府公告通常可自由使用（注意署名），但媒体报道不行
2. **可信度**：媒体转述会失真，且容易把观点写成事实
3. **反爬**：主流财经媒体都有反爬，长期维护不划算

若要引用观点，一律**追到当事人/机构的原始出处**（如"某委员在某次会议上的表态"要找会议纪要本身）。

---

## 6. 机器能做到哪一步：能力边界

这张表是整个计划的核心。**它划出自动化与人工的分界线，也是防止"自动化"变成"自动化出错"的护栏。**

| 内容成分 | 能否自动 | 谁来做 | 备注 |
|---|:---:|---|---|
| 指标数值、统计期间 | ✅ | 爬虫 | 需要交叉验证 |
| 「内容截至 YYYY-MM-DD」 | ✅ | 爬虫 | 取数据发布日 |
| `dataviz` 的 `series` | ✅ | 爬虫 | 历史序列可直接生成 |
| `source`（出处） | ✅ | 爬虫 | 自动填源名 + 访问日期 |
| 时间线的**事实条目** | ✅ | 爬虫起草 | "某日发布了什么" |
| `updated_at` | ✅ | 爬虫 | |
| `one_liner`（一句话） | ⚠️ 半自动 | LLM 起草 + 人审 | 必须勾人，且要过门禁 |
| `links`（相关基础概念） | ⚠️ 半自动 | 爬虫提议 + 人确认 | 提议可以是"含 LPR → 关联利率传导"这类规则 |
| **`key_points`（结论）** | ❌ | **人** | 必须是结论、≤30 字、不与 `one_liner` 重复 |
| **因果解读** | ❌ | **人** | "为什么跌"才是热点区的价值 |
| **分歧观点** | ❌ | **人** | 需要判断谁的说法值得并列 |
| **`recipe` 组合** | ❌ | **人** | 全库唯一，是"讲解多样化"的执行机制 |
| `timeline` 的**编排与侧重** | ❌ | **人** | 挑哪几条进时间线是判断 |

**结论**：
- **数据观察类（8 个）**可以做到"爬虫全量起草数值与日期，你只审 diff"
- **事件解剖类（12 个）**最多做到"爬虫把事实骨架按时间摆好，解读你写"

**任何声称"事件解读也能全自动"的方案，都会让热点区变成一堆看起来专业、实际经不起推敲的文字。** 那比不更新更糟。

---

## 7. 内容更新功能怎么设计

### 7.1 核心：App 侧零改动

因为爬虫的产出最终仍然是 `content/*.yaml` → 同一套门禁 → 同一个 `content-dist/` 格式。

**App 完全不需要知道爬虫存在。** 现有三条通路（首启内置 / 联网更新 / 手动导入）、失败矩阵、原子替换、进度保护，全部原样复用。

这是方案 A 最大的价值：**它是一个纯上游的生产工具，不改变任何既有契约。**

### 7.2 候选池 + 人审 diff（新增的一环）

```
爬虫
 └─→ content-candidates/<日期>/
      ├─ changes.md            ← 人看的：变了什么、为什么、够不够可信
      ├─ <concept-id>.yaml     ← 候选内容（完整、可直接替换）
      └─ observations.json     ← 原始抓取结果（留档，便于追责）
          ↓
      【人工审核】← 闸门在这里
          ↓
   npm run crawl:apply         ← 把采纳的候选合入 content/*.yaml
          ↓
   npm run gate                ← 既有门禁，一条不放松
          ↓
   npm run publish:content     ← 既有发布链路，一行不改
```

`changes.md` 应该长这样（人只需要读这一份）：

```
## 世界热点 · 数据观察

### hotspot.data.pmi-reading
- 「内容截至」  2026-09-15 → 2026-09-30
- 最新 PMI        49.8 → 50.2   （重回扩张区间）
- 数据序列      新增 1 个点
- 数据源        国家统计局（2026-09-30 发布）  ✓ 双源一致
- 需要你决定    「一句话」是否需要跟着改？

### hotspot.data.social-financing
- 数值更新       社会融资规模存量同比 8.4% → 8.1%
- ⚠️ 交叉验证不一致：统计局 8.1% vs FRED 中国序列 8.3%（差 0.2pct，超容差）
- 需要你决定    以哪个为准
```

**注意最后一行的价值**：它把"机器不确定的地方"明确摆出来，而不是默默选一个。这正是人工审核该花时间的地方。

### 7.3 发布侧：初期不动

发布仍然手动（`npm run publish:content`），版本号仍然人工指定。

理由：让爬虫先只把"生产"自动化，**别同时动"发布"**。一次只改一个环节，出问题才定位得了。

### 7.4 分级闸门：让大多数更新根本不需要你审

这是替代「事后删除」的正解。核心思路：**不是取消审核，而是让绝大多数更新压根不需要人介入。**

| 级别 | 什么情况 | 处置 | 人的介入 |
|:---:|---|---|---|
| **绿** | 纯数值 / 期间 / `series` / `source` / `updated_at` 变化，**且双源一致**，且数值变化在合理区间（跳变检测通过） | **自动发布** | **零** |
| **黄** | 数值有疑问：只有单源 / 变化超容差 / 交叉验证不一致 | 发布，但在概念上标「待确认」，并在下次 `changes.md` 里置顶 | 看到提示后决定 |
| **红** | **任何涉及文字的改动**：`one_liner`、`key_points`、因果解读、分歧观点、`timeline` 编排、`recipe` | **必须人审后才发布** | 审 diff |

**绿级覆盖了日常更新的绝大多数。** 数据观察类的月度数据更新（PMI、社融、CPI、北向资金…）基本全是绿级——它们只是数字变了。所以你日常几乎不需要做什么。

#### 绿级放行的三条硬护栏（缺一不可）

1. **字段白名单**：只有 `数值 / 期间 / series / source / updated_at` 允许自动改。
   diff 一旦触及 `one_liner`、`key_points` 或任何文字类 block → **立刻降级为红级**
2. **双源一致**：交叉验证不一致 → 降级为黄级（§4.4）
3. **数值跳变检测**：环比变化超阈值（如 CPI 同比变动 > 1pct、社融存量变动 > 2pct）→ 降级为黄级。
   这条专门防"接口通了但拿错了序列"（累计值 vs 当期值）

#### 另外两条必须有的护栏

- **版本号自动递增**：`YYYY.MM.DD` 或 `-N` 后缀，且必须通过既有的「版本号必须递增」守卫
- **可回滚**：绿级自动发布也要留档（本次改了什么、依据哪个源）。发现不对时能一键回退到上一版内容

#### 事件解剖类永远不走绿级

它的价值在因果与分歧判断，机器不碰。**这是本方案与"全自动"之间不可让渡的边界。**

### 7.5 事后反馈回路：把「浏览时发现问题」变成正式机制

这是 Ramon 那个提议里**真正有价值的部分**，只是执行手段要从"删除"换成"标记"。

```
App 内（浏览时）→ 点「这条有问题」→ 写入 user.db（扛得住更新）
                                  ↓
              下次爬虫运行前先读这份清单
                                  ↓
        相关问题【强制升级为红级】→ 必须你确认后才发布
                                  ↓
              修好后从清单里划掉
```

**实现要点**

| 项 | 设计 |
|---|---|
| 存哪 | `user.db` 新增一张表（如 `concept_flags(concept_id, kind, note, at)`）。**必须放 `user.db`**——它从不参与内容替换，能扛住更新 |
| 入口 | 概念页底部加一个低调的「这条有问题」，可选类型：数值有误 / 已过时 / 表述不清 / 其他 |
| 消费 | 爬虫运行前读该表，命中的概念**无论 diff 多干净都升级为红级** |
| 闭环 | 修正后自动或手动解除标记，避免清单越积越多 |
| 附带价值 | 顺带覆盖了"内容过时但我没注意"——可直接标「已过时」，比等 30 天琥珀色提示更主动 |

**这个机制比「事后删除」强在哪**：

1. **可靠**——标记会进入下一次的强制审核，不会因为"后来忘了"而丢失
2. **可累积**——问题清单是资产；而"删除"是一次性动作，删完就没了信息
3. **能覆盖 diff 看不出的问题**——"用着用着觉得不对"这类只有实际使用才暴露的问题，只有这个渠道能捕获
4. **不碰内容模型**——不需要给 `content.db` 加删除语义，也就不需要在"整库替换"里打补丁

#### 关于「删除/隐藏」功能本身

如果你想清掉不想看的内容（比如某条概念对你没用），可以做，形态是**本地隐藏**：

- `user.db` 存 `hidden_concepts`，UI 全站过滤（速览页 / 复习队列 / 搜索）
- 因为 `user.db` 不被替换，**隐藏能扛住内容更新**（不会"删了又回来"）
- 需要一个「已隐藏」管理入口，能看到并恢复

但要分清它的定位：**这是"我不想看这条"，不是"这条内容错了"。** 内容纠错走 §7.5 的标记回路。

---

## 8. 必须处理的七个问题

| # | 问题 | 处理方式 |
|---|---|---|
| 1 | **来源失效**（接口改版、网页改结构） | 每个 adapter 有 `healthCheck`；失败时**不产出候选**并明确报错，而不是产出空值 |
| 2 | **数据修订**（GDP / 社融会回改历史值） | 支持"重拉最近 N 期"，不能只追加。这是硬需求，不是优化 |
| 3 | **拿错序列**（累计值 vs 当期值、同比 vs 环比） | 双源交叉验证 + 数值跳变检测（环比变化超阈值就标红） |
| 4 | **合规与版权** | 只用官方统计与政府公告；媒体内容只做线索不搬运；`source` 自动署名 |
| 5 | **频率与礼貌** | 缓存 + 限速 + 退避；不要高频轮询；公告类数据按发布节奏抓即可 |
| 6 | **门禁必须原样通过** | 爬虫产出不得为自己开后门。`recipe` 唯一性、`key_points` 长度、热点必须有 `timeline` 与 `source` —— 一条不放松 |
| 7 | **可溯源性** | 每次抓取留档原始响应；从最终概念能反查到"哪次抓取、哪个 URL、什么时间" |

---

## 9. 实施阶段

**P-C1 接口可行性验证**（先证明能拿到数据，再动手写工具）
- 逐个手测目标接口的可用性、返回结构、是否需要密钥
- 对每个指标，确认"这条数据在源里叫什么、单位是什么、期间怎么表示"
- 产出：一页接口清单，含 3~5 个真实抓取样例
- **门禁：至少 3 个源能稳定取到真实数据，且双源交叉验证通过**

**P-C2 抓取层 + 三个 adapter**
- `fetch`（缓存/限速/超时/重试）+ `nbs` + `fred` + `pboc`
- 单元测试：抓取失败不产出候选；缓存命中不打网络；限速生效
- **门禁：断网时全部 adapter 干净失败，不产出任何候选**

**P-C3 候选生成 + diff 工具**（本阶段的核心）
- `normalize` → `propose` → `diff` → `changes.md`
- 测试：数值跳变被标红；交叉验证不一致被标出；只改数值时不误报文字变更
- **门禁：手工构造的 5 组变更，`changes.md` 都必须准确描述**

**P-C4 合入链路 + 与既有门禁打通**
- `crawl:apply` 把采纳的候选写回 `content/*.yaml`
- 跑既有 `validate-content`，**确保 0 error**
- **门禁：合入后全库仍 68 个概念、recipe 仍全库唯一、208+ 个测试全过**

**P-C5（可选）纯数据自动发布**
- 字段白名单 + 自动版本号 + 双源一致才放行
- 只在 C1–C4 稳定运行一段时间后才考虑

---

## 10. 需要你拍板的问题

### ① 绿级自动发布的边界（决定自动化程度）

分级闸门（§7.4）已经定了框架，但**绿级的范围**需要你确认：

| 选项 | 绿级包含 | 自动化程度 | 风险 |
|---|---|:---:|---|
| **(a) 只自动改数值与日期** | 纯数值 / 期间 / `series` / `source` / `updated_at` | 中——日常数值更新全自动 | 最低 |
| **(b) (a) + 允许自动**起草**事实性文字** | 加上时间线的「某日发布了什么」这类事实条目 | 较高 | 中——事实条目写错不易察觉 |
| **(c) (a) + LLM 起草解读文字** | 加上 `one_liner` 与因果解读 | 高 | **高**——不推荐，见 §6 |

我的建议：**从 (a) 开始**。跑顺一段时间（比如连续 10 次发布零人工修正）再考虑扩到 (b)。
**(c) 我不建议在任何阶段做**——因果与分歧判断是热点区的立身之本，机器写出来的会是同质化的正确废话，比不更新更糟。

### ② 事后反馈回路要不要现在就做（§7.5）

- **要**：App 加「这条有问题」入口 + `user.db` 加标记表 + 爬虫侧消费该清单。多一个工作包，但发现问题不再靠偶然
- **不要**：先只做 `changes.md` 人审，等你实际用出痛点再加

我的建议：**要**。它是把这个方案从"自动发布"升级为"可自我纠错"的关键，而且成本不高（一张表 + 一个入口）。

### ③ 「删除/隐藏」功能要不要做（§7.5 末）

- **做**：`user.db` 存隐藏列表，全站过滤，有管理入口。定位是"我不想看这条"
- **不做**：不提供删除，只提供标记

我的建议：**先不做**。它解决的不是你最初提的问题（纠错），而且会引入"哪些页面要过滤""隐藏了还算不算进度"这类新的状态交互。等你确实遇到"这条我不想要"的情况再说。

### ④ 爬虫跑在哪

| 选项 | 说明 |
|---|---|
| **本机手动跑**（推荐起点） | `npm run crawl` → 出 `changes.md` → 你审 → `crawl:apply` |
| **本机定时跑** | `launchd` 定时触发采集并生成候选；无需服务器，但要有失败通知 |
| **CI / 服务器** | 需要新基础设施与密钥管理。**只有绿级自动发布才需要它**——因为自动发布必须有个常驻进程来跑 |

**注意 ④ 和 ① 是联动的**：如果绿级要自动发布，就必须有个地方定时跑它（本机 `launchd` 或 CI）。如果绿级只做"生成候选"，那手动跑就够了。

---

**这四项定了，我立刻出 WBS（工作包分解）并开始 P-C1（接口可行性验证——先证明能拿到数据，再写工具）。**

> 顺带说明：本计划**不新增任何依赖到 App**。爬虫拿到多少自动化收益，App 侧一行代码都不用改——这也是我把它设计成"纯上游工具"的原因。
