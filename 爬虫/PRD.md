# 爬虫工具 PRD（工作名 Beacon）

> 状态：**✅ 已评审 · 决策已冻结**（2026-09-23）。本文档定义「要求什么」，不含实现
> 版本：v1.0 · 2026-09-23
> 上游约束：`../PRD.md`（Recall 产品需求，§4.4 热点区、§5 内容规格与生产流程）
> 方案来源：`方案论证.md`（本文档的每条决定，在那份里有完整论证与数据源调研）

---

## 0. 文档分工与决策记录

### 0.1 单一定义点

| 文档 | 管什么 |
|---|---|
| **`爬虫/PRD.md`（本文）** | **要求什么**：用途、信息源、筛选规则、接入方式、独立性要求、验收标准 |
| `爬虫/方案论证.md` | **为什么这么选**：三方案对比、数据源可行性、能力边界的论证过程 |
| `爬虫/开发计划.md`（后续） | **谁在什么阶段做什么**：WBS、阶段门禁、测试计划 |
| `../PRD.md` | Recall 本体需求（热点区的内容规格由它定义，本文档不重复） |
| `../架构设计.md` | Recall 的系统架构与更新链路（本文档只引用，不重述） |

**规则**：需求变更只改本文档。方案论证与开发计划不得重新定义需求。

### 0.2 已确认的决策（来自 2026-09-23 讨论）

| # | 决策 | 来源 |
|---|---|---|
| D1 | 爬虫作为**独立工具**跑在本机（"构建期爬取"），产出候选 → 人审 → 合入内容 → 走既有发布链路 | 方案论证 §3 方案 A |
| D2 | **App 侧零改动**即可接入（内容仍是 `content/*.yaml` → 同一门禁 → 同一 `content-dist/`） | 方案论证 §7.1 |
| D3 | 范围**只服务世界热点**，首版不碰经济学与金融学 | 方案论证 §2 |
| D4 | 用**分级闸门**（绿/黄/红）替代"取消审核"，让多数更新无需人工介入 | 方案论证 §7.4，Ramon 已确认 |
| D5 | 用**事后标记回路**替代"事后删除"（删除不能解决大多数错误类型） | 方案论证 §1.1、§7.5，Ramon 已确认 |
| D6 | 「删除/隐藏」功能**首版不做** | 待确认项 ③ 的建议值 |
| D7 | 首版**绿级也只生成候选，不自动发布**；自动发布列为可选的 C5 阶段 | 本文 §5.7 |
| D8 | 爬虫**成为可复用的独立工具**，语言与依赖不污染 App 技术栈，后续可被其他 app 接入 | **本次新增要求**（Ramon） |

> **D6 / D7 已于 2026-09-23 确认**：D6（删除/隐藏首版不做）与 D7（首版不自动发布）均按建议值执行。
> 全部决策（Q1–Q8）的最终结论与排期见 `开发计划.md` §1.3。

### 0.3 术语表

| 术语 | 含义 |
|---|---|
| **Observation** | 一条原子观测：某指标在某期间的值。源无关，是唯一内部交换格式 |
| **Measurement** | 把 Observation 绑定到「某个概念的某个字段」之后的结果 |
| **Candidate** | 一组 Measurement + 分级（绿/黄/红）+ 判定依据 |
| **ChangeSet** | 一次运行的候选集合 + 人审记录 |
| **Feed** | **面向任意 app 的中性产物**：不含 Recall 语义的观测流（`feed.jsonl`） |
| **L1 / L2 / L3 源** | 官方一手源 / 结构化聚合源 / 媒体线索（只用来发现问题，不作来源） |
| **绿 / 黄 / 红级** | 分别对应：可自动放行 / 发布但标待确认 / 必须人审 |
| **适用性白名单** | 「指标 → 概念 → 字段」三元组的允许清单，不在此表内的一律丢弃 |

---

## 1. 产品定位

### 1.1 一句话

> **一个在本机运行的采集工具，把公开的官方数据变成「可审核的内容变更候选」，并保证这些变更必须通过既有门禁才能进入内容库。**

它解决的不是"内容从哪来"（内容始终是人工定义的），而是**"数值与事实的搬运"**——这部分既枯燥又容易抄错，且恰好是机器最擅长的。

### 1.2 它不是什么

| 不是 | 理由 |
|---|---|
| 不是内容生成器 | `key_points`（结论）、因果解读、分歧观点、`recipe` 组合**永远由人写**（§6.4） |
| 不是 App 的运行时组件 | App 运行时绝不联网抓取（方案论证 §3，明确否决） |
| 不是发布工具 | 它只写 `content/*.yaml`，发布仍走既有 `publish:content` |
| 不是通用爬虫框架 | 只服务"公开官方数据 → 结构化观测"这一件事，不做通用页面抓取 |

### 1.3 目标（每条带可验证判据）

| # | 目标 | 可验证判据 |
|---|---|---|
| **G1** | 消除数值搬运错误 | 热点区 13 处「示意数据」中，**适用于实证数据的部分全部替换为真实数据**，且每个 `dataviz.source` 是可点击的一手出处 |
| **G2** | 让多数更新无需人工介入 | 数据观察类的例行数值更新中，**绿级占比 ≥ 60%**（低于此值说明阈值或源选得不对） |
| **G3** | 绝不引入幻觉数据 | 抓取失败时**产出的候选数为 0**，且流程明确报错；任何情况下不得用旧值填充、估算或插值 |
| **G4** | 不放松任何既有约束 | 爬虫产出的内容通过既有 `validate-content` 时 **0 error**，且全库 68 个概念的 `recipe` 组合仍**全库唯一** |
| **G5** | 出问题可定位、可回退 | 任一最终内容字段都能反查到「哪次运行、哪个 URL、什么时间、什么响应」 |
| **G6** | 可独立演进与复用 | 加一个数据源**不改下游代码**；被第二个 app 接入时**不改 core 与 sources** |

---

## 2. 用途与使用场景

### 2.1 三个用途

**U1 数值与期间刷新**（主要用途）

把一个概念的数值类字段更新到最新期间：指标数值、`dataviz.series`、`source` 里的「截至日期」、`updated_at`。这是日常使用量最大的部分。

**U2 事实骨架采集**（辅助用途）

事件解剖类概念依靠它提供**事实条目**：某机构在某日发布了什么、数值从多少变到多少。这些是时间线的素材，但**时间线的编排与侧重仍由人决定**。

**U3 缺口与异常提示**

- 提示"这条概念的「内容截至」已超期"（比 App 里 30 天琥珀色提示更早、更结构化）
- 提示"某指标本次未取到数据，与预期发布节奏不符"（可能源挂了，也可能数据确实延期）

### 2.2 使用场景

| # | 场景 | 期望行为 |
|---|---|---|
| **S1** | 例行更新：每月统计局发布 PMI 后 | 跑一次工具 → `changes.md` 显示 3 条绿级数值变更 → 确认 → 合入 → 跑门禁 → 发布。全程约 3 分钟 |
| **S2** | 数据可疑：交叉校验不一致 | `changes.md` 把不一致**明确摆出来**（哪个源说多少、差多少、超没超容差），并标黄级，附一句"需要你决定以哪个为准" |
| **S3** | 源失效：某接口改版 | 该源 `healthCheck` 失败 → **不产出任何候选** → 明确报错指出哪个源坏了、HTTP 状态是什么 → 其余源正常工作不受影响 |
| **S4** | 发现内容有误：用 App 时觉得某条不对 | 在 App 内点「这条有问题」→ 该标记写入 `user.db` → 下次跑工具时**该概念强制红级**，必须人审 |
| **S5** | 被其他 app 复用：另一个程序想要这批数据 | 读 `feed.jsonl` 或调 `beacon export --format json`，**无需了解 Recall 的任何概念** |

### 2.3 使用者

| 阶段 | 使用者 |
|---|---|
| 首版 | 仅本人（本机 CLI，人工触发） |
| 后续 | 本人 + 其他 app（作为数据供给方，通过产物文件或 CLI 接入） |

---

## 3. 信息源

### 3.1 三级可信度分层

| 层 | 定义 | 可否作为内容来源 | 用途 |
|---|---|:---:|---|
| **L1 官方一手** | 政府统计机构、央行、监管机构、国际组织的原始发布 | ✅ **主力** | 数值与事实的唯一正式来源 |
| **L2 结构化聚合** | 第三方把公开数据整理成接口（FRED、akshare、行情接口） | ✅ 可用于**补充与交叉验证** | 交叉校验、历史序列补齐、开发提速 |
| **L3 媒体** | 财经媒体报道 | ❌ **仅作线索** | 只扫描标题以发现"值得做成概念的事件"，**不采正文、不作来源** |

**L3 的三条理由**（不把媒体当来源）：版权风险、转述失真、反爬成本。若需引用观点，一律追到当事人/机构的原始出处。

### 3.2 源清单

**已实测确认**（2026-09-23 实测）

| 源 | 覆盖 | 接入方式 | 鉴权 |
|---|---|---|---|
| **国家统计局** | GDP、CPI、PPI、PMI、社零、固投、居民收入、工业企业财务 | 公开数据接口，**无需鉴权**。新版 `data.stats.gov.cn/dg/website/publicrelease/web/external`（V2.0，2026-03 更新）；旧版 `easyquery.htm` 仍可用 | 无 |
| **FRED**（圣路易斯联储） | 80 万+ 时间序列（含美国与部分中国指标） | REST API，**另有发布日历端点** `fred/releases/dates`，可提前知道哪天出新数据 | **免费 key**（限速 120 次/分） |

**候选源**（C1 阶段必须逐条实测，**未实测前不得写入任何内容**）

| 源 | 预期覆盖 | 预期接入方式 | 风险 |
|---|---|---|---|
| 中国人民银行 | M0/M1/M2、社融、LPR、公开市场操作 | 官网发布页，**无正式开放 API** | 需写页面 adapter，结构变动风险 |
| 国家外汇管理局 | 外汇储备、跨境资金流动 | 官网 | 同上 |
| 美国财政部 | 国债收益率曲线（日度） | 官网 / API | — |
| 美联储 | FOMC 声明、会议纪要、点阵图、SEP | 发布页 / RSS | — |
| ECB / BOJ / BIS / IMF | 政策声明、金融稳定报告 | 发布页 / RSS | — |
| CFTC | COT 持仓报告（周度） | 官网数据文件 | 文件格式可能非结构化 |
| 中证指数公司 | 指数估值（用于分位计算） | 官网 | 可能需自算 |
| 东方财富 / 新浪财经 | A 股指数、北向资金、板块资金流 | 公开接口 | 非官方，需交叉验证 |
| **akshare**（Python） | 聚合 100+ 公开源 | Python 库 | **是聚合器**：上游挂它就挂 |
| 中国物流与采购联合会 | PMI 分项 | 与统计局联合发布 | 与统计局**非独立源**，不能互为交叉验证 |
| OPEC / IEA | 产量、库存、能源平衡表 | 官网报告（PDF/Excel） | 多为非结构化 |
| 航运指数（SCFI/BDI） | 运价 | 官网 / 公开接口 | — |

> ⚠️ **`akshare` 的使用边界**：它能把 adapter 数量压到很低，开发提速明显；但**关键指标必须直连原始源**。规则是：`akshare` 只用于探索阶段与 L2 交叉验证，**不允许作为 L1 主源**。

### 3.3 源准入规则（白名单）

每个纳入的源必须全部满足：

| # | 要求 |
|---|---|
| SR1 | 有**明确的一手出处**（机构名 + 可点击 URL + 发布日） |
| SR2 | 许可允许使用（官方统计与政府公告通常可自由使用，需署名；媒体正文不在此列） |
| SR3 | 无需登录、不违反其服务条款、不做任何绕过反爬的行为 |
| SR4 | 实现 `healthCheck()`，能被明确判定为 `ok | broken` |
| SR5 | 在 `docs/数据源清单.md` 中有实测记录（样例响应 + 字段说明），**未记录的源不得启用** |

### 3.4 源 → 概念映射（数据观察类 8 个）

| 概念 | 关键指标 | L1 主源 | 交叉校验手段 |
|---|---|---|---|
| `hotspot.data.pmi-reading` | 制造业/非制造业 PMI、新订单、产成品库存 | 国家统计局（与中采联合） | **同源多口径**：分项加总 vs 综合指数 |
| `hotspot.data.social-financing` | 社融存量/增量、M1/M2 | 中国人民银行 | 跨源：FRED 中国 M2 序列 |
| `hotspot.data.yield-curve-inversion` | 10Y、2Y、3M 收益率与利差 | 美国财政部 | 跨源：FRED |
| `hotspot.data.cftc-positions` | 非商业净持仓（周度） | CFTC | 同源：总持仓 = 各分项之和 |
| `hotspot.data.northbound-flow` | 北向净买入、持股市值 | 交易所公开数据 | 跨源：东方财富接口 |
| `hotspot.data.credit-impulse` | 信用脉冲 | **派生指标**（社融增量 / GDP，12 个月滚动） | 公式可复现 + 与社融同比方向一致性 |
| `hotspot.data.inventory-cycle` | 产成品存货同比、营收同比 | 国家统计局（工业企业财务） | 同源：营收与存货的分项加总 |
| `hotspot.data.valuation-percentile` | 指数 PE 历史分位 | 中证指数 / 东方财富 | **派生**：分位由历史序列自算，需固定窗口与算法 |

**事件解剖类 12 个**以官方公告与统计为源（美联储、央行、BIS、OPEC、FDIC、港交所公告等），**只采事实条目**（"某日发布了什么、数值从多少变到多少"），不采解读。

### 3.5 一个必须承认的现实：交叉验证不是总能靠"双源"

方案论证里最初写的是"同一指标至少两个独立源互校"。**逐条核对后要修正**：不少指标**没有真正独立的第二源**——例如 PMI，统计局与中国物流与采购联合会是**联合发布**，不是两个源；很多中国指标经 FRED 转手，其上游仍是统计局，独立性有限。

所以交叉校验落地为**三种手段，按可用性优先选择**，且**每个指标必须明确声明自己用哪一种**：

| 手段 | 说明 | 适用 |
|---|---|---|
| **C1 跨源对照** | 两个独立发布的源比对 | 有真独立源时（如美债收益率：财政部 vs FRED） |
| **C2 同源多口径互校** | 同比/环比/累计值能否互相推算；分项加总是否等于总量 | 官方统计的常见结构 |
| **C3 派生公式复核** | 派生指标（信用脉冲、估值分位）必须写明公式并可复现 | 所有派生指标 |

**这条修正直接影响绿级定义**：绿级要求的是「**通过该指标声明的交叉校验**」，而不是「必须有双源」。只有单源且无口径互校手段的指标，**默认黄级**。

---

## 4. 数据筛选规则（核心章节）

### 4.1 七层漏斗

数据从"网上取到"到"允许写入内容"，必须逐层通过。任一层不通过，按该层规定处置（**丢弃 / 降级 / 报错**），**不允许跳过**。

```
原始响应
   │
   ▼  F1 源级准入      ← 白名单；未登记的源不采
   ▼  F2 时效          ← 期间有效性、重拉窗口
   ▼  F3 结构校验      ← 字段完整性、单位、期间格式
   ▼  F4 合理性        ← 值域、跳变、序列一致性、派生可复现
   ▼  F5 交叉校验      ← C1 跨源 / C2 同源多口径 / C3 公式复核
   ▼  F6 适用性白名单  ← 「指标→概念→字段」三元组，不在表内即丢弃
   ▼  F7 分级闸门      ← 绿/黄/红，决定是否需人工介入
   │
   ▼  候选（Candidate）
   │
   ▼  既有内容门禁 validate-content（0 error）
   │
   ▼  写入 content/*.yaml
```

**设计意图**：越靠前的层越粗暴（直接丢弃），越靠后的层越谨慎（降级 + 交给人）。**F6 是最重要的一道闸门**——它防止"因为抓到了就顺手塞进去"。

### 4.2 F1 源级准入

| 规则 | 处置 |
|---|---|
| 源必须已在 §3.2 登记，且 `docs/数据源清单.md` 有实测记录 | 未登记 → **丢弃** |
| `healthCheck()` 返回 `broken` | **整体中止该源**，其余源继续；在 `changes.md` 顶部报错 |
| 返回非 2xx | 同 broken；**记录 HTTP 状态码**，不重试超过 3 次 |
| 响应内容与实测记录的结构不符（字段缺失/类型变化） | **丢弃该次抓取**（不是丢弃单字段），报"源结构可能已变更" |

> **铁律**：源失败时**产出 0 条候选**，而不是产出空值。空的数值比没有数值危险得多——它会被下游当成"数据就是 0"。

### 4.3 F2 时效筛选

| 规则 | 处置 |
|---|---|
| 观测期间必须是**已发布的完整期间**（不采进行中的期间） | 丢弃 |
| 观测的发布日必须 ≤ 今天（不采未来日期） | 丢弃 |
| **重拉窗口**：每次运行必须重拉最近 `N` 期（默认 `N = 6`），而非只取最新一期 | 强制，不可配置为 1 |
| 同一期间的数据**允许被新发布覆盖**（数据修订） | 覆盖时**必须生成一条 diff**，不得静默改写 |
| 概念的「内容截至」日期 = 所用数据的**实际发布日**，不是抓取日 | 强制 |

> **`N = 6` 的理由**：官方数据会回改历史值（GDP 两次修订、就业数据回改前两月）。只追加新数据会导致库里长期留着已被官方修正的旧值。

### 4.4 F3 结构校验

| 检查 | 不合格处置 |
|---|---|
| 数值必须能解析为**有限数值**（拒绝 `NaN`、`Infinity`、空串、`"-"`、`"…"`） | 丢弃该观测 |
| 单位必须明确且可映射到规范化单位表（如 `亿元`、`%`、`点`、`bp`） | 丢弃 |
| 期间格式统一为 `YYYY-MM`（月度）/ `YYYY-Qn`（季度）/ `YYYY-MM-DD`（日度） | 丢弃 |
| 同一次运行的**期间序列必须单调且连续**，缺期需显式标注 | 缺期 → 黄级 + 在人审文件里标出 |
| 每条观测必须带 `来源 / URL / 发布日 / 抓取时间` 四项溯源信息 | 缺任一 → 丢弃 |

### 4.5 F4 合理性校验

**值域检查**（超出即**丢弃**，说明"这个数不可能是这个指标"）

| 指标类 | 默认值域 |
|---|---|
| 扩散指数（PMI 等） | `[30, 70]` |
| 价格指数同比（CPI/PPI） | `[-10, 30]` % |
| 存量同比（社融/M2） | `[-10, 50]` % |
| 收益率（%） | `[-5, 30]` |
| 分位（%） | `[0, 100]` |
| 净流入（亿元） | `[-10000, 10000]` |

**跳变检查**（超出即**降黄级**，交人看）

| 指标类 | 默认阈值（环比/同比变动） |
|---|---|
| 扩散指数 | `> 2.0` 个百分点 |
| 价格指数同比 | `> 1.0` 个百分点 |
| 存量同比 | `> 2.0` 个百分点 |
| 收益率 | `> 50` bp（日度） |
| 资金流 | `> 5 倍`历史中位数 |
| 任意序列 | 序列长度变化 ≠ 预期增量 |

**序列一致性检查**（防"拿错序列"，这是最隐蔽的一类错误）

| 检查 | 说明 |
|---|---|
| 口径标记必须显式：累计值 / 当期值、同比 / 环比、季调 / 未季调 | 未声明 → 丢弃 |
| 与上次运行的同名序列**口径标记必须一致**；不一致 → **丢弃并报警** | 防"这次拿成累计值了" |
| 派生指标必须携带**可复现的公式与输入**（C3） | 缺 → 丢弃 |

> **跳变阈值的定位**：它不是"判断数据真假"，而是"找出值得人多看一眼的地方"。所以超阈值只降黄级，不丢弃。

### 4.6 F5 交叉校验

按 §3.5 的三种手段执行。容差默认值：

| 数据类型 | 默认容差 |
|---|---|
| 百分比类 | `0.1` 个百分点 |
| 收益率 | `2` bp |
| 金额类 | `0.5` % |
| 指数点位 | `0.2` % |

| 结果 | 处置 |
|---|---|
| 在容差内 | 通过，**标记为「已交叉校验」**，可进入绿级判定 |
| 超容差 | **黄级**，并在 `changes.md` 写明「哪个源说多少 / 差多少 / 容差多少」 |
| 只有单源、且无 C2 手段可用 | **默认黄级**（不是绿级） |
| C1 与 C2 结论互相矛盾 | **红级**，必须人判 |

### 4.7 F6 适用性白名单（防"抓到就塞"）

**只有登记在「指标 → 概念 → 字段」映射表里的三元组，才允许生成候选。**

映射表放在独立配置文件（`config/mapping.yaml`），形态例如：

```yaml
- indicator: cn.pmi.manufacturing
  concept: hotspot.data.pmi-reading
  field: blocks[1].series[0].points
  transform: none
- indicator: cn.social-financing.stock-yoy
  concept: hotspot.data.social-financing
  field: blocks[3].series[0].points
  transform: none
- indicator: cn.social-financing.increment
  concept: hotspot.data.credit-impulse
  field: blocks[2].series[0].points
  transform: credit-impulse   # 派生算子，必须已注册
```

| 规则 | 处置 |
|---|---|
| 不在映射表里的观测 | **丢弃**（即使数据完全正确） |
| 映射到不存在的概念 | **报错**（配置错误，不是数据错误） |
| 映射到不存在的字段路径 | **报错** |
| 映射会导致 `recipe` 变化（增删 block） | **红级**，必须人审 |
| 需要新增映射 | 人工编辑配置文件（首版不做自动推断） |

**这条规则同时也是一个安全机制**：它让"爬虫能改哪些内容"成为一份**可审计的显式清单**，而不是"代码里写了什么就能改什么"。

### 4.8 F7 分级闸门

| 级别 | 判定条件（全部满足才成立） | 处置 |
|:---:|---|---|
| **绿** | ① 变更仅涉及 `数值 / 期间 / series / source / updated_at`<br>② 通过该指标声明的交叉校验（§3.5）<br>③ 未触发跳变阈值<br>④ 不涉及任何文字字段<br>⑤ **该概念的「待修正」标记为空** | 可自动放行（**首版仍只生成候选**，见 §5.7） |
| **黄** | ① 或 ② 或 ③ 不满足（如单源、超容差、跳变） | 生成候选，**在概念上标「待确认」**，`changes.md` 置顶 |
| **红** | **任何涉及文字的改动**：`one_liner`、`key_points`、因果解读、分歧观点、`timeline` 编排、`links` 的增删改、`recipe`；或该概念有未处理的「待修正」标记 | **必须人审后才能合入**，工具**不生成可直接应用的候选**，只生成"建议 + 现状对照" |

**分级必须写明理由。** 每一条候选都要带 `level_reason`（如"双源一致，变动 0.3pct 未超阈值"），不允许只给级别不给原因。

### 4.9 明确排除的数据（一律不采）

| # | 排除项 | 理由 |
|---|---|---|
| E1 | 媒体转述的数值 | 必须追到一手出处；转述会失真 |
| E2 | 无明确出处的数据（含"业内人士称"） | 不可溯源 |
| E3 | 预测值 / 机构调查值 | **除非该概念本身讲的就是"预测"**（如 CFTC 持仓反映预期），且必须显式标注为预测 |
| E4 | 需要登录、付费墙、或违反其服务条款的源 | 合规 |
| E5 | 抓不到时的替代值：旧值填充、估算、插值、"约" | **宁缺勿造**（G3） |
| E6 | 图片/PDF 中的数值（首版不做 OCR） | 引入不可控误差 |
| E7 | 社媒、论坛、自媒体内容 | 可信度不可控 |
| E8 | 与热点区无关的指标（如个股、非公开舆情） | 范围（§2.1、方案论证 §2） |

---

## 5. 与现有 App 的接入

### 5.1 接入面只有两个，且方向严格单向

| 方向 | 载体 | 说明 |
|---|---|---|
| 爬虫 → App | **`content/*.yaml`** | 间接：合入 YAML → 既有门禁 → 既有发布链路 → App 更新通道 |
| App → 爬虫 | **`flags.json`（待修正清单）** | 文件交换，非直连数据库 |

**爬虫不直连 App 的任何数据库，也不写 `content-dist/`。**

理由：`content-dist/` 是发布产物，必须由 `publish:content` 生成（它要保证版本号递增与 sha256 自洽）。爬虫直接写它会绕过版本守卫与门禁。

### 5.2 接入路径

```
content/*.yaml  ←──────────────  beacon apply（合入采纳的候选）
      │                                ▲
      │                                │ 人工审核
      ▼                        ┌───────┴────────┐
validate-content（既有门禁）   │  changes.md    │ ← beacon run 产出
      │                        │  候选 YAML      │
      ▼                        │  observations  │
build-package → content-dist   └────────────────┘
      │                                ▲
      ▼                                │
publish:content → GitHub + jsDelivr     │ 读 flags.json（App 导出）
      │                                │
      ▼                                │
   Recall App ──「这条有问题」──────────┘
```

### 5.3 为什么 App 侧"零改动"就能接入

因为爬虫的产出**就是人手写的那份格式**。所以以下既有机制**全部原样复用，一行不改**：

| 既有机制 | 为何不受影响 |
|---|---|
| 内容质量门禁（11 类检查） | 爬虫产出与人写的走同一道门禁，**不为自己开后门** |
| 三条更新通路（首启内置 / 联网 / 手动导入） | 对它们而言内容仍是"一份新的 `concepts.jsonl`" |
| 失败矩阵（无网/超时/sha256/解析/门禁） | 完全不感知爬虫 |
| 原子替换 + `user.db` 永不覆盖 | 同上 |
| 版本守卫（版本号必须递增） | 同上 |

**这是本方案最大的价值**：它把"自动化"限制在一个纯上游位置，**不动任何既有契约**。

### 5.4 对 App 侧的接口要求（三项，由 Recall 侧排期实现）

| # | 接口要求 | 具体形态 |
|---|---|---|
| **A1** | **待修正标记的存储** | `user.db` 新增一张表：`concept_flags(concept_id, kind, note, at)`。<br>**必须放 `user.db`**——它从不参与内容替换，标记才能扛住更新 |
| **A2** | **标记入口** | 概念页底部一个低调的「这条有问题」，可选类型：数值有误 / 已过时 / 表述不清 / 其他。可附一句话说明 |
| **A3** | **导出待修正清单** | 设置页可导出 `flags.json`（含 `schemaVersion` / 导出时间 / 每条标记的概念 id、类型、说明）。<br>约定一个默认路径供爬虫默认读取 |

**可选（不阻塞首版）**

| # | 项 | 说明 |
|---|---|---|
| A4 | 概念页显示「待确认」标记 | 对应黄级，让"这条数值待核实"在 App 里可见 |
| A5 | 导出时自动带出 `source` | 便于爬虫核对出处 |

> **交换方式为什么用文件而不是直连 `user.db`**：直连会造成两个问题——① schema 一变爬虫就崩；② 与 App 的 WAL 写产生锁竞争风险。文件交换还有个额外好处：**其他 app 也能复用同一个机制**（§7.5）。

### 5.5 兼容性承诺

| 场景 | 约定 |
|---|---|
| 内容 Schema 变化 | 由 `schemaVersion` 标识；爬虫产出的 `schemaVersion` 必须与 App 支持的一致，否则**拒绝产出**并报错 |
| 门禁规则加严 | 爬虫必须跟着改（**不允许**因为"爬虫生成的"就放宽规则） |
| 爬虫先于 App 升级 | 允许；爬虫可以只支持旧 schema |
| App 先于爬虫升级 | 允许；爬虫检测到 `schemaVersion` 不匹配时明确报错，不猜 |

### 5.6 App 侧的三处边界（不得越界）

1. **App 不感知爬虫**：代码里不出现任何"爬虫/采集"分支，不检查"内容是否来自爬虫"
2. **App 不自动合入**：即使候选是绿级，合入动作也在本机 CLI，不在 App 内
3. **App 不因内容来自爬虫而区别对待**：门禁、失败矩阵、版本守卫对来源一视同仁

### 5.7 首版不自动发布（D7）

**首版（C1–C4）绿级也只生成候选，由人执行合入。**

理由：一次只自动化一个环节。"生产自动化"与"发布自动化"同时上线，出问题无法定位是哪个环节的锅。且自动发布需要常驻进程（`launchd` 或 CI），是额外的基础设施。

**C5（可选，需再次确认后才做）**：启用绿级自动发布。前置条件与护栏：

| 前置 | 说明 |
|---|---|
| C1–C4 稳定运行 | 例如连续 10 次运行的绿级候选**零人工修正** |
| 常驻运行位置 | 本机 `launchd` 或 CI（需你决定） |
| 护栏①：字段白名单 | 只允许改 `数值/期间/series/source/updated_at` |
| 护栏②：双源/交叉校验通过 | 不通过一律降级 |
| 护栏③：跳变检测通过 | 超阈值降级 |
| 护栏④：版本号自动递增 | 必须能通过既有版本守卫 |
| 护栏⑤：可一键回滚 | 自动发布也必须留档（改了什么、依据哪个源） |

**事件解剖类永不进入自动发布。**

---

## 6. 独立工具定位（可更新、可复用）

### 6.1 独立性五条

| # | 要求 | 理由 |
|---|---|---|
| I1 | **独立目录** `爬虫/`，不把源码散落到 App 的 `app/`、`src/`、`tools/` 下 | 边界清晰，可整体抽出为独立仓库 |
| I2 | **独立依赖**：自己的依赖清单与虚拟环境，**不进入 App 的 `node_modules` 与 `package.json`** | 不污染 App 技术栈，也让语言可自由选择 |
| I3 | **独立运行**：不依赖 Node/npm，不依赖 App 已安装 | 换机器也能跑 |
| I4 | **独立版本号**：与 App 版本无关，各自演进 | 两者的变更节奏完全不同 |
| I5 | **不依赖父项目的相对路径** | 所有输入输出路径通过**参数或配置文件**传入，不得硬编码 `../content/`。这是将来能抽出成独立仓库、并被其他 app 使用的前提 |

**语言选择**：建议 **Python**（`akshare` 生态最全、数据处理库成熟）。这是 I2 带来的自由——**App 侧永远只看到 YAML**。

### 6.2 三层架构

```
爬虫/
├─ PRD.md                    ★ 需求源（本文件）
├─ 方案论证.md                 为什么这么选（含数据源调研）
├─ 开发计划.md                （后续）WBS + 阶段门禁 + 测试计划
├─ README.md                  （后续）工具的安装与使用
├─ docs/
│  ├─ 数据源清单.md            （后续）每个源的实测记录（准入依据）
│  └─ 数据契约.md              （后续）Observation / Feed 的 JSON Schema
└─ beacon/                   （后续）工具本体
   ├─ core/                  源无关：抓取纪律、归一化、七层漏斗、分级、契约
   ├─ sources/               每个源一个 adapter（可插拔）
   ├─ consumers/             消费者适配器：recall 是第一个
   ├─ config/                源清单、映射表、阈值表（改配置不改代码）
   └─ cli/                   命令入口
```

**三层的依赖方向是单向的**：

```
sources  ──→  core  ──→  consumers
（取数）    （校验+分级）   （翻译成目标 app 的格式）
```

### 6.3 core 层零绑定规则（硬约束）

**`core/` 目录下不得出现任何特定 app 的专有词汇。** 具体到 Recall，禁止出现：

`recipe`、`key_points`、`one_liner`、`blocks`、`concept`、`domain`、`track`、`hotspot`、Recall 的概念 id 形态

**这条由一个源码扫描测试强制**（对 `core/` 目录做词表扫描，命中即测试失败）。

理由：一旦 core 里出现 `recipe`，这个工具就永久绑死在 Recall 上了，"给其他 app 用"就变成每次都要改核心代码。

### 6.4 数据契约

四层结构，逐层增加绑定关系。**只有最后一层才含 Recall 语义**：

| 层 | 形态 | 含 Recall 语义 | 用途 |
|---|---|:---:|---|
| **Observation** | `{indicator, period, value, unit, source:{name,url,published_at}, fetched_at, caliber:{...}, provenance}` | ❌ | 唯一内部交换格式 |
| **Feed** | `feed.jsonl` = Observation 流 + `feed.meta.json` | ❌ | **面向任意 app 的中性产物** |
| **Measurement** | Observation + `{concept, field_path, transform}` | ✅ | 绑定到具体概念的字段 |
| **Candidate / ChangeSet** | Measurement 集 + 分级 + 判定依据 + 人审状态 | ✅ | 人审与合入的工作单元 |

**`contractVersion`**：契约带版本号。任何破坏性变更（删字段、改语义）必须递增，并在 `docs/数据契约.md` 写明迁移方式。

> **设计要点**：`Feed` 是"可被其他 app 接入"的**唯一必需产物**。它不含任何 Recall 概念，因此一个全新的 app 只要会读 JSONL 就能用。

### 6.5 可更新性（改配置不改代码）

| 想改什么 | 改哪里 | 是否需要动代码 |
|---|---|---|
| 加一个数据源 | 新增 `sources/<name>` + 在 `config/sources.yaml` 登记 | 需要（新增 adapter，但不改下游） |
| 调整某个源的字段映射 | `config/mapping.yaml` | **不需要** |
| 调整阈值（值域/跳变/容差） | `config/thresholds.yaml` | **不需要** |
| 调整重拉窗口 `N` | `config/settings.yaml` | **不需要** |
| 支持一个新的目标 app | 新增 `consumers/<app>` | 需要（新增适配器，但不改 core 与 sources） |
| 新增一个指标 | 源侧加字段 + 映射表登记 | 视情况 |

**关键承诺**：**加一个源不需要改下游；加一个消费者不需要改上游。** 这条在第 7 节有对应的验收项。

### 6.6 面向其他 app 的接入方式（三条）

| # | 方式 | 契约 | 适用 |
|---|---|---|---|
| **X1** | **读产物文件** | `feed.jsonl` + `feed.meta.json`（含 `contractVersion`、生成时间、覆盖的指标清单） | 最简，任何语言/任何 app 都能用 |
| **X2** | **调 CLI** | `beacon export --format json --indicators <...> --since <date>` → stdout 输出 Feed | 需要按需取数的 app；也可被脚本/其他 Agent 调用 |
| **X3** | **本地接口（后续）** | 本地 MCP server / `localhost` HTTP，暴露"取观测"能力 | Agent 类应用直接调用；**首版不做** |

**新增一个 app 的完整步骤**（证明复用成本低）：

1. 新增 `consumers/<app>/`，实现"把 Feed 翻译成该 app 的格式"
2. 在配置里登记该消费者与它的映射表
3. **不改 `core/`，不改任何 `sources/`**

### 6.7 与 Recall 当前文档体系的关系

- 本工具**不修改** `../PRD.md`、`../架构设计.md` 的任何既有契约
- 本工具**新增**一种内容生产方式，但**不新增**任何内容通路
- Recall 侧唯一需要配合的是 §5.4 的三项接口要求（标记表 + 入口 + 导出），排期在 Recall 侧决定

---

## 7. 非功能需求

| # | 需求 | 判据 |
|---|---|---|
| NF-C1 | **绝不编造** | 无数据时产出 0 条候选；代码中不存在插值/填充/估算路径 |
| NF-C2 | **幂等** | 相同输入（同一批响应）重复运行，产出完全一致 |
| NF-C3 | **可重入** | 中断后重跑不产生重复候选、不损坏候选目录 |
| NF-C4 | **不写内容库** | 全程不触碰 `content.db` / `user.db`（`flags.json` 也只读） |
| NF-C5 | **不触碰发布产物** | 不写 `content-dist/`、不调发布脚本、不推送远端 |
| NF-C6 | **缓存与礼貌** | 按 `URL + 日期` 缓存；限速 + 退避重试；超时可配 |
| NF-C7 | **可溯源** | 每条候选能反查到 URL、HTTP 状态、抓取时间、原始响应留档 |
| NF-C8 | **密钥不入库** | API key 从环境变量或本地 `.env` 读取；`.gitignore` 排除；日志中脱敏 |
| NF-C9 | **无网可测** | 单元测试基于录制的响应 fixture 运行，不依赖网络 |
| NF-C10 | **失败隔离** | 单个源失败不影响其他源；失败只降级本源的产出 |
| NF-C11 | **可读的人审产物** | `changes.md` 是纯文本 Markdown，**不打开代码就能审** |
| NF-C12 | **资源占用** | 常规运行内存 < 500MB、单次运行 < 5 分钟（不含首次全量拉取） |
| NF-C13 | **跨平台** | macOS 优先，但不使用 macOS 专有命令（便于将来在 Linux/CI 运行） |

---

## 8. 明确不做（首版 Out of Scope）

| # | 不做 | 理由 |
|---|---|---|
| N1 | **App 运行时抓取** | 破坏离线优先；App 本体无法热修（方案论证 §3） |
| N2 | 采集经济学 / 金融学的内容 | 那 48 个概念是"理解与表达"，不是采集（D3） |
| N3 | 自动生成 `key_points` / 因果解读 / 分歧观点 / `recipe` | 热点区的立身之本，机器写出来是同质化的正确废话 |
| N4 | 用 LLM 起草解读文字 | 同上；且引入"看着对但经不起推敲"的风险 |
| N5 | 搬运媒体正文 | 版权 + 可信度 + 反爬 |
| N6 | 自动发布（C1–C4 阶段） | 一次只自动化一个环节（§5.7） |
| N7 | 「删除 / 隐藏」概念功能 | 解决的不是纠错问题（D6） |
| N8 | OCR 识别 PDF/图片中的数值 | 引入不可控误差 |
| N9 | 分布式 / 大规模抓取 | 数据量极小（20 个概念），不需要 |
| N10 | 多用户 / 服务化 / 账号体系 | 本机单人工具 |
| N11 | 自动推断指标→概念映射 | 映射表必须显式可审计（§4.7） |
| N12 | 通用爬虫框架 / 可视化抓取配置 | 只服务这一件事，不做平台 |
| N13 | 自动改写 `timeline` 的编排与侧重 | 挑哪几条进时间线是判断（§3.4、§6.4） |

---

## 9. 验收标准

| # | 验收项 | 判据 |
|---|---|---|
| **A-C1** | 源可行性 | 至少 3 个源能稳定取到真实数据，且每个源在 `docs/数据源清单.md` 有实测记录 |
| **A-C2** | 交叉校验生效 | 构造超容差数据 → 必被标黄级，且 `changes.md` 写明两源数值与差值 |
| **A-C3** | **失败不产假数据** | 断网 / 源 502 / 结构变化 三种情况下，**候选数均为 0**，且明确报错指出原因 |
| **A-C4** | 值域拦截 | 构造超值域数值（如 PMI = 95）→ 必被丢弃，不进入候选 |
| **A-C5** | 跳变拦截 | 构造跳变数值（如 CPI 同比变动 3pct）→ 必被降为黄级 |
| **A-C6** | 映射表闸门 | 手工加一条不在映射表内的正确观测 → **必须被丢弃** |
| **A-C7** | 分级准确性 | 5 组手工构造的变更，分级（绿/黄/红）必须全部正确；**任何涉及文字的变更必须是红级** |
| **A-C8** | 门禁不放松 | 爬虫产出的内容跑既有 `validate-content` → **0 error**；全库 68 个概念、`recipe` 组合仍全库唯一 |
| **A-C9** | 待修正标记生效 | 概念在 `flags.json` 中有标记 → 该概念本次**强制红级**，无论数据多干净 |
| **A-C10** | 幂等与可重入 | 同一批响应跑两次 → 候选逐字节一致；中断后重跑不产生重复 |
| **A-C11** | core 零绑定 | `core/` 目录词表扫描（`recipe`/`key_points`/`one_liner`/`blocks`/…）→ **0 命中** |
| **A-C12** | 加源不改下游 | 新增一个源（第 4 个）不需要修改 `core/` 与既有 `sources/`（以 diff 证明） |
| **A-C13** | 加消费者不改上游 | 新增一个假消费者把 Feed 转成 CSV，不需要修改 `core/` 与任何 `sources/` |
| **A-C14** | 切分输出 | 生成 `feed.jsonl`，其内容**不含任何 Recall 专有字段**（`recipe`/`key_points`/概念 id 形态） |
| **A-C15** | 溯源完整 | 任取一条候选，能反查到 URL、HTTP 状态、抓取时间与原始响应 |
| **A-C16** | 密钥安全 | 仓库内 `grep` 不到任何 key；日志中 key 已脱敏 |
| **A-C17** | 独立运行 | 在**未安装 Node/npm** 的环境（或断网 + 无 App）下能跑完核心流程（用缓存 fixture） |
| **A-C18** | 人审产物可读 | `changes.md` 不打开代码就能读懂：变了什么、依据哪个源、哪里需要人决定 |

**硬门禁**（不通过则不得进入下一阶段）：**A-C3**（失败不产假数据）、**A-C8**（门禁不放松）、**A-C11**（core 零绑定）。

---

## 10. 决策状态（Q1–Q8 全部定案）

> **本节只保留结论**；每项的排期影响见 `开发计划.md` §1.1–§1.4。

| # | 问题 | 结论 |
|---|---|---|
| Q1 | 工具命名与 CLI 命令名 | 工作名 **Beacon**，CLI `beacon`。**可后改**，不影响架构 |
| Q2 | 语言 | **Python**（理由见 §6.1） |
| Q3 | 「删除 / 隐藏」功能 | **首版不做**。定位问题：它解决的不是纠错，纠错走 §7.5 的标记回路 |
| Q4 | C5 自动发布是否做、跑在哪 | **本版不实施**，仅登记为可选项。等 P-C4 稳定运行后再定常驻位置 |
| Q5 | 事件解剖类"事实条目"的采集粒度 | 在 **P-C3** 设计时定，结论写入本文档或独立设计文档 |
| Q6 | `flags.json` 默认读取路径 | 默认 `~/Library/Application Support/Recall/exports/flags.json`，**必须可被 CLI 参数覆盖**（I5）。最终路径在 FB-3 与 Recall 侧确认 |
| Q7 | 首版指标范围 | **4 个**：PMI、社融、收益率曲线、估值分位。**对原建议的一处收紧**：`northbound-flow` 移出首版（它依赖 L2 非官方源，应先用官方源把校验框架立起来）。理由见 `开发计划.md` §1.2 |
| Q8 | 数据源可用性看板 | **首版不做**。用 `docs/数据源清单.md` 手工记录；源只有 4 个，看板是过度工程 |

**决策之后仍有不确定的地方**（不构成阻塞，但要知道）：

| 项 | 不确定性 | 何时消除 |
|---|---|---|
| 源可行性 | 4 个主源中可能有无法程序化获取的（尤其中证指数与央行） | P-C1 实测后 |
| 绿级占比 | PRD G2 要求 ≥60%，但在诚实降级交叉验证后**可能达不到** | P-C3 首轮真实运行后 |
| YAML 保序改写 | 可能做不到"不改一个字节" | P-C4 往返测试后。若不达标，退化为"只产出候选、不直接写回" |

---

## 附录 A：数据观察类 8 个概念的源与派生说明

| 概念 | 主源 | 是否派生 | 派生公式（如适用） | 默认分级 |
|---|---|:---:|---|:---:|
| `hotspot.data.pmi-reading` | 国家统计局 | 否 | — | 黄（单源，仅 C2 口径互校） |
| `hotspot.data.social-financing` | 中国人民银行 | 否 | — | 绿（有 C1 跨源） |
| `hotspot.data.yield-curve-inversion` | 美国财政部 | 否 | 利差 = 长端 − 短端 | 绿（有 C1 跨源） |
| `hotspot.data.cftc-positions` | CFTC | 否 | 净持仓 = 多头 − 空头 | 绿（C2 分项加总） |
| `hotspot.data.northbound-flow` | 交易所公开数据 | 否 | 净买入 = 买入 − 卖出 | 绿（有 C1 跨源） |
| `hotspot.data.credit-impulse` | 央行社融 + 统计局 GDP | **是** | 信用脉冲 = Δ(社融增量 / GDP)，12 个月滚动 | 黄（派生，需 C3 复核） |
| `hotspot.data.inventory-cycle` | 国家统计局 | 否 | 剪刀差 = 存货同比 − 营收同比 | 黄（单源 + C2） |
| `hotspot.data.valuation-percentile` | 中证指数 / 东方财富 | **是** | 分位 = 当前 PE 在近 N 年序列中的百分位（窗口与算法须固定） | 黄（派生） |

> **这份表的分布本身就说明了一件事**：现阶段多数指标只能到黄级。**绿级不是起点，是靠交叉校验能力逐步挣来的。** 这也意味着首版的日常体验更接近"每次看几行 diff"，而不是"全自动"——这是符合现实的预期。

## 附录 B：热点区待替换的「示意数据」

初步统计：`content/hotspot/hotspot.yaml` 中 **13 处** `dataviz` 的 `source` 标注为示意数据。

**但不是全部都要换。** 要区分两类：

| 类型 | 特征 | 处置 |
|---|---|---|
| **机制示意型** | 图形用来表达形状/关系（如"倒挂长什么样""剪刀差的走向"） | **保留示意**，但 `source` 必须明确写"示意数据，用于说明…" |
| **实证数据型** | 图形用来表达"现在实际是多少" | **必须替换为真实数据**，附一手出处与发布日期 |

**C1 阶段的第一件事就是逐条分类这 13 处**，并产出「保留示意 / 替换真实」的清单。这个分类结果同时是 **G1 的可验证判据**。

---

## 附录 C：与 Recall 现有文档的引用关系

| 本文提到的 | 依据 |
|---|---|
| 热点区必须含 `timeline`、`source` 必填 | `../PRD.md` §4.4 |
| 内容生产流程与人工复核 | `../PRD.md` §5.4 |
| 68 个概念与 9 个专题分布 | `../PRD.md` §5.1 |
| `content.db` / `user.db` 双库分离、原子替换 | `../架构设计.md` §4 |
| 三层闸门的论证、数据源调研过程 | `方案论证.md` §1.1、§4–§7 |
